# 罠01：一覧APIの暗黙の打ち切り

> **一行でいうと**
> 「検索したら0件でした」は、「そんなものは存在しない」ではなく「そこを見ていなかっただけ」かもしれない。

**この罠の危険度：★★★★★（最高）**
理由は3つあります。エラーが出ません。0件という結果が「答え」に見えます。そして、原因は自分が書いたコードの中にあります。

---

## 目次

1. [この記事は誰のためのものか](#1-この記事は誰のためのものか)
2. [予備知識：APIと「一覧」とは何か](#2-予備知識apiと一覧とは何か)
3. [何をしようとしていたのか](#3-何をしようとしていたのか)
   - [3.1 「貧困線」とは何か](#31-貧困線とは何か)
4. [投げかけ①：「貧困線の表を探して」](#4-投げかけ貧困線の表を探して)
5. [返ってきた答え：0件](#5-返ってきた答え0件)
6. [ここで危うく間違えるところだった](#6-ここで危うく間違えるところだった)
7. [立ち止まる理由になった、たった一つの矛盾](#7-立ち止まる理由になったたった一つの矛盾)
8. [犯人探し：4つの仮説を順番に潰す](#8-犯人探し4つの仮説を順番に潰す)
9. [真犯人：`max_page = 50`](#9-真犯人max_page--50)
10. [なぜ気づけなかったのか](#10-なぜ気づけなかったのか)
11. [対処①：上限を上げる（これだけでは不十分）](#11-対処上限を上げるこれだけでは不十分)
12. [対処②：打ち切りを検出して、必ず知らせる](#12-対処打ち切りを検出して必ず知らせる)
13. [対処③：一覧をファイルに保存する](#13-対処一覧をファイルに保存する)
14. [直ったことをどう確かめたか](#14-直ったことをどう確かめたか)
15. [おまけ：直しても残った小さな謎](#15-おまけ直しても残った小さな謎)
16. [あなたのAPIでも同じことが起きる](#16-あなたのapiでも同じことが起きる)
17. [チェックリスト](#17-チェックリスト)
18. [まとめ](#18-まとめ)
19. [付録A：完成したコード全文](#付録a完成したコード全文)
20. [付録B：用語集](#付録b用語集)
21. [付録C：よくある質問](#付録cよくある質問)

---

## 1. この記事は誰のためのものか

この記事は、**公的統計をプログラムで取ってきて分析したい人**に向けて書いています。インドネシアの統計を扱った経験から書いていますが、**この罠はインドネシア固有のものではありません**。日本の e-Stat でも、世界銀行でも、OECD でも、GitHub API でも、まったく同じ形で起こります。

前提知識はほとんど要りません。プログラムを書いたことがなくても、「インターネットで検索したら結果が10件ずつ出てきて、下に『次のページ』ボタンがある」という経験があれば十分です。この罠は、まさにその「次のページ」の話だからです。

コードは R で書いていますが、**言語は関係ありません**。Python でも JavaScript でも Excel のマクロでも、同じ罠にかかります。コードの部分は読み飛ばしても、話の筋は追えるように書きました。

そして、この記事にはもう一つの目的があります。**生成AI（ChatGPT や Claude のようなもの）と一緒にプログラムを書くときに、何が起きるか**を具体的に記録しておくことです。この罠は、生成AIと人間の共同作業のなかで生まれ、そして見逃されました。その経緯も包み隠さず書きます。

---

## 2. 予備知識：APIと「一覧」とは何か

### 2.1 APIは「決まった形で質問すると、決まった形で答えてくれる窓口」

インドネシアの統計庁（BPS：Badan Pusat Statistik といいます。日本でいう総務省統計局のようなところです）は、統計データをホームページで公開しています。人間はブラウザで見ればいいのですが、何百個もの表を扱うとなると、手作業では無理です。

そこで用意されているのが **API** です。API とは、ざっくりいえば「**決まった形式で質問を投げると、決まった形式で答えが返ってくる窓口**」のことです。

たとえば、こういう文字列（URL）を投げます。

```
https://webapi.bps.go.id/v1/api/list/model/var/lang/eng/domain/0000/key/【鍵】
```

すると、こういう答えが返ってきます（実際はもっと長いです）。

```json
{
  "status": "OK",
  "data": [
    { "page": 1, "pages": 182, "total": 1816, "per_page": 10 },
    [
      { "var_id": 1, "title": "Population by Province" },
      { "var_id": 2, "title": "..." }
    ]
  ]
}
```

人間がブラウザでポチポチ見る代わりに、プログラムがこの窓口に自動で質問を投げて、答えを集めてくる。それが今回やろうとしていたことです。

### 2.2 「一覧」は、図書館の目録カードのようなもの

BPS の API には、いくつか種類の質問があります。そのうち一番はじめに使うのが「**一覧**（list）」です。

これは図書館でいえば **目録カード** にあたります。図書館に行って「貧困について書かれた本はどこですか」と聞くとき、いきなり書架を全部歩き回る人はいません。まず目録を引きます。

BPS の API も同じです。

1. まず**一覧**を取って、「どんな統計表があるか」のカタログを手に入れる
2. カタログの中から「貧困線」の表を探し、その**番号**（var_id）を調べる
3. その番号を指定して、**中身のデータ**を取りに行く

今回の罠は、**この1番目の段階**で起きました。目録が不完全だったのです。しかも、不完全だと誰も教えてくれませんでした。

### 2.3 ページング（改頁）とは

ここが今回の話の核心です。

インターネットの検索結果を思い出してください。「1,816件見つかりました」と出ても、画面に一度に全部は出てきません。10件ずつ表示されて、下に「次へ」があります。これを **ページング**（改頁、pagination）といいます。

なぜそうなっているかというと、1,816件を一度に送ると通信量が大きくなり、サーバーにも負担がかかるからです。だから小分けにして渡す。とても合理的な仕組みです。

BPS の API も同じで、**1ページあたり10件**です。全部で1,816件あるなら、182ページあることになります。

つまり、**カタログを丸ごと手に入れたければ、182回質問を投げなければいけない**。ここに罠が潜んでいました。

---

## 3. 何をしようとしていたのか

具体的な目的を書いておきます。ここが分かっていないと、なぜこの罠が致命的だったのかが伝わらないからです。

インドネシアの家計の暮らしぶりを、州ごとに比べたいと考えていました。「ジャカルタの人は月にいくら使っているか」「東ヌサトゥンガラの人はいくらか」といった比較です。

ところがここで問題があります。**同じ1万円でも、物価の高い場所と安い場所では価値が違う**のです。東京で1万円使うのと、地方で1万円使うのとでは、買えるものの量が違います。ジャカルタは物価が高いので、支出額をそのまま比べると「ジャカルタの人は豊かだ」と過大評価してしまいます。

そこで、**地域ごとの物価の目安**が必要になりました。そこで登場するのが「貧困線」です。

### 3.1 「貧困線」とは何か

聞き慣れない言葉だと思うので、少し丁寧に説明します。**この記事の後半を読むうえで、必ずしも必要な知識ではありません**が、「なぜそんなに必死に探していたのか」が分かると、話が追いやすくなります。

#### 貧困線とは「ここから下は貧困」という線

**貧困線**（英語で poverty line、インドネシア語で Garis Kemiskinan）とは、

> **1人が1か月生きていくのに、最低限これだけは必要だ、という金額**

のことです。この金額より少ない支出しかできていない人を「貧困層」と数えます。日本でいえば、生活保護の基準額が近い考え方です。

#### どうやって決めているのか

インドネシア統計庁（BPS）は、貧困線を**2つの部分の足し算**で計算しています。

| 部分 | 中身 |
|---|---|
| **食料の部分**（Garis Kemiskinan Makanan） | 1日に**2,100キロカロリー**を摂るのに必要な食費。実際にその地域の人がよく食べている52品目（米、魚、卵、油、砂糖など）の値段から計算する |
| **食料以外の部分**（Garis Kemiskinan Bukan Makanan） | 住居、衣服、教育、保健、交通など、最低限必要な暮らしの費用 |

**この2つを足したものが貧困線**です。1人あたり・1か月あたりの金額で表されます。

ここが大事なのですが、**この計算は州ごとに、しかも都市部と農村部で別々に行われます**。理由は単純で、同じ「米1キロ」でも値段が違うからです。物価の高い場所では、同じ2,100キロカロリーを摂るのにより多くのお金が要ります。

#### だから「地域の物価」の代わりになる

ここが今回の作業で貧困線を欲しがった理由です。

**貧困線は「同じ内容の生活を買うのに必要な金額」なので、物価が高い場所ほど高くなります。**

つまり、州ごとの貧困線を並べると、それは実質的に**州ごとの物価水準の目安**になっているわけです。統計学の用語では、こういうものを**空間デフレータ**（地域間の物価差を調整するための数値）と呼びます。

実際、集めたデータを見ると、こうなっていました（2013年、1人あたり月額、単位はルピア）。

| 州 | 貧困線 |
|---|---|
| バンカ・ブリトゥン州（Bangka Belitung） | 427,081 |
| 東カリマンタン州（Kalimantan Timur） | 417,902 |
| アチェ州（Aceh） | 348,172 |
| 中部カリマンタン州（Kalimantan Tengah） | 307,698 |
| 西ジャワ州（Jawa Barat） | 276,825 |
| ゴロンタロ州（Gorontalo） | 233,942 |

**高いところと低いところで、1.8倍ほどの開き**があります。この差を無視して支出額を比べると、物価の高い州の人を「豊か」と読み違えてしまいます。

使い方はとても簡単で、割り算するだけです。

```
実質的な暮らしぶり ＝ その州の1人あたり支出 ÷ その州の貧困線
```

こうすると「貧困線の何倍の暮らしをしているか」という単位のない数字になり、州どうしを公平に比べられます。

#### 「貧困線」と「貧困率」は別物

よく混同されるので、区別を書いておきます。

| | 意味 | 単位 | 例 |
|---|---|---|---|
| **貧困線** | 貧困かどうかを分ける**境界の金額** | ルピア／人／月 | 400,000ルピア |
| **貧困率（P0）** | 貧困線より**下にいる人の割合** | ％ | 9.8％ |

**線そのもの**と、**線の下にいる人の数**。まったく別の統計です。今回探していたのは前者、つまり「線」のほうでした。

#### 注意：完全な物価指数ではない

念のため書いておくと、貧困線は物価指数（消費者物価指数のようなもの）**そのものではありません**。あくまで「最低限の生活」の費用なので、豊かな層が買うものの値段は反映していません。それでも、州別の物価指数が整備されていない状況では、**手に入るなかで最も素直な代用品**です。この割り切りをした、ということも記録しておきます。

### 3.2 ほしかったもの

まとめると、ほしかったのは

> **州ごと・年ごとの貧困線。とくに2011年のもの。**

でした。2011年にこだわる理由は、分析に使う家計調査の個票データが2011年のものだからです。2011年の支出を2013年の物価で割っても、意味のある数字にはなりません。

---

## 4. 投げかけ①：「貧困線の表を探して」

ここからは、生成AIに実際にどう頼み、何が返ってきたかを順に書きます。

> **注記**：以下の「投げかけ」は、実際のやりとりの要約です。一字一句そのままではありませんが、内容は変えていません。

最初の投げかけはこうでした。

> **人間**：貧困線の2010〜2012年のデータが取れない。表195は2013年からしか無いようだ。他に貧困線の表がないか探してほしい。

これに対して、AIは診断用のスクリプトを書きました。中身はこういう流れです。

```r
# 1. 動的表の一覧（カタログ）を取ってくる
v <- bps_list_var_all()

# 2. その一覧から「poverty line」を含むものを探す
cand <- bps_find(v, "poverty line")

# 3. 見つかったものそれぞれについて、収録年を調べる
for (i in seq_len(nrow(cand))) {
  ys <- bps_list_th(cand$var_id[i])
  ...
}
```

考え方としては何もおかしくありません。**カタログを取る → 検索する → 詳細を調べる**。図書館で本を探すのと同じ手順です。

---

## 5. 返ってきた答え：0件

実行結果はこうでした。

```
########## 3. 貧困線を含む他の表を探す ##########

--- 動的表で 'poverty line' を含むもの ---
  該当なし（lang='eng' なので英語で探していることに注意）

--- 静的表で 'poverty line' を含むもの ---
    table_id                                                    title
438     1494 Number Of Poor People, Percentage of Poor People and
             The Poverty Line, 1970-2017
```

動的表からは **0件**。静的表から1件だけ。

しかも、この「0件」には**もっともらしい説明までついていました**。「lang='eng' なので英語で探していることに注意」。つまりAIは、こう言っているわけです。

> 英語のカタログを英語で検索したのだから、検索語の問題ではないはずです。0件ということは、そういう表が無いのでしょう。

この説明は、**一見すると筋が通っています**。ここが恐ろしいところです。エラーメッセージなら「何かがおかしい」と分かります。しかし「0件」は、**そのまま答えとして受け取れてしまう**のです。

---

## 6. ここで危うく間違えるところだった

もしこのまま進んでいたら、どうなっていたでしょうか。

出てきた唯一の候補は、静的表1494「Number Of Poor People, Percentage of Poor People and The Poverty Line, 1970-2017」でした。1970年から2017年まで、48年分。年数だけ見れば立派です。

しかし、この表のタイトルには **`by Province`（州別）が入っていません**。おそらく全国の数字だけです。全国の数字は、地域ごとの物価の目安には**まったく使えません**。ジャカルタと東ヌサトゥンガラの違いを見たいのに、全国平均を見ても何も分からないからです。

つまり、この時点での結論はこうなるはずでした。

> 州別の貧困線は2013年からしか無い。2011年のデータには使えない。あきらめて別の方法を考えよう。

**これは完全な誤りでした。** 実際には、2011年を含む州別の貧困線は存在していたのです。しかも、後で分かったことですが、**2004年から2025年まで22年分**もありました。

もしここであきらめていたら、研究の一部が丸ごと成立しなくなっていました。「データが無いので分析できませんでした」という結論を、**存在するデータを見落としたまま**書いていたことになります。

これが、この罠の恐ろしさです。**間違った結論が、正しい手順を踏んだように見える形で出てくる。**

---

## 7. 立ち止まる理由になった、たった一つの矛盾

では、なぜ間違いに気づけたのか。

きっかけは、**たった一つの矛盾**でした。

出力をよく見ると、こう書いてあります。

```
--- 動的表で 'poverty line' を含むもの ---
  該当なし
```

しかし、思い出してください。この作業は、そもそも

> 貧困線の**表195**は2013年からしか無いようだ

というところから始まっています。**表195は貧困線の表です。実在します。すでに使っています。**

ということは、こうなるはずです。

- 表195は貧困線の表 → タイトルには当然「poverty line」が入っているはず
- そのカタログを「poverty line」で検索した → 0件

**この2つは両立しません。** どちらかが間違っています。

そして、表195が実在することは分かりきっています。すでにそこからデータを取り、2013年から2025年の値を手にしているのですから。

したがって、**間違っているのは検索の側**です。

ここが分岐点でした。人間が判断したのは、次の一点だけです。

> **実在するものが検索に出てこないなら、疑うべきは検索語ではなく、検索している対象（＝カタログ）のほうだ。**

言い方を変えると、こうなります。

- 「0件」は**データについての事実**ではない
- 「0件」は**自分の手続きについての事実**かもしれない

この2つを区別する習慣が、この記事でいちばん伝えたいことです。

---

## 8. 犯人探し：4つの仮説を順番に潰す

「検索がおかしい」と決まったら、次は**どこがおかしいか**です。思いつく仮説を並べて、順番に潰していきます。

### 仮説A：検索語のつづりが違う

`"poverty line"` ではなく `"Poverty Line"` かもしれない。大文字小文字の区別で外している可能性。

**確認方法**：検索関数の中身を読む。

```r
bps_find <- function(df, pattern) {
  key <- apply(df, 1, paste, collapse = " ")
  hit <- df[grepl(pattern, key, ignore.case = TRUE), , drop = FALSE]
  ...
}
```

`ignore.case = TRUE` と書いてあります。大文字小文字は区別していません。**仮説Aは却下。**

### 仮説B：語の間に別の語が入っている

BPS のタイトルは `Poverty Line (Rupiah/Kapita/Month) By Province and Area` のようになっているかもしれません。だとすると「poverty line」は連続していますが、もし `Poverty and Line` のような形なら一致しません。

**確認方法**：検索語を短くして再実行する。`"poverty"` だけで探す。

これは有効な確認です。実際、後でこの方法も使いました。ただし、この時点では次の仮説のほうが強い証拠を持っていました。

### 仮説C：カタログの取り方が間違っている（言語の指定など）

`lang = "eng"` で取った一覧に、そもそも表195が入っていないのではないか。

**確認方法**：カタログの中に表195があるかどうかを、直接調べる。これが決定打になります。

```r
any(as.character(v$var_id) == "195")
```

これは「vという表のリストの中に、var_idが195のものがあるか」を YES/NO で答えさせる命令です。結果はこうでした。

```
v に var 195 が入っているか:  FALSE
```

**入っていませんでした。**

検索語の問題ではありません。そもそも**探す対象の中に、探し物が入っていなかった**のです。

図書館の比喩でいえば、目録カードを一生懸命めくっていたけれど、**そのカードの束自体が、蔵書の一部しか含んでいなかった**、ということです。

### 仮説D：カタログが途中で切れている

では、なぜ入っていないのか。ここで一覧の件数を数えます。

```r
cat("v  の件数:", nrow(v), "\n")
```

```
v  の件数: 490
```

**490件。**

ここで、§2.3 のページングの話を思い出してください。1ページ10件です。490件ということは、**49ページ分**しか取っていないことになります。

そして、カタログを取る関数の定義を見ると、こう書いてありました。

```r
bps_list_var_all <- function(domain = "0000", lang = "eng", max_page = 50) {
```

`max_page = 50`。**50ページで打ち切る、と書いてある。**

犯人が見つかりました。

---

## 9. 真犯人：`max_page = 50`

問題の関数を、全文で見てみましょう。長く見えますが、やっていることは単純です。

```r
bps_list_var_all <- function(domain = "0000", lang = "eng", max_page = 50) {
  # (1) まず1ページ目を取る
  first <- bps_list_page("var", domain, lang, page = 1)
  if (!identical(first$status, "OK")) {
    cat("status:", first$status, " message:", first$message, "\n")
    return(NULL)
  }

  # (2) 1ページ目に入っている「全部で何ページあるか」を読む
  info  <- bps_pageinfo(first)
  pages <- suppressWarnings(as.integer(info$pages))
  if (is.na(pages)) pages <- 1

  # (3) ★ここが問題★ 上限と比べて、小さいほうを採用する
  pages <- min(pages, max_page)
  cat("総ページ数:", pages, " 総件数:", info$total, "\n")

  # (4) 2ページ目から順に取って、くっつけていく
  out <- bps_rows(first)
  if (pages > 1) {
    for (p in 2:pages) {
      r <- bps_list_page("var", domain, lang, page = p)
      out <- rbind(out, bps_rows(r))
      cat("\r取得中 ", p, "/", pages, sep = "")
    }
    cat("\n")
  }
  out
}
```

問題は **(3) の1行**です。

```r
pages <- min(pages, max_page)
```

`min` は「小さいほうを取る」という意味です。サーバーは「182ページあります」と正直に教えてくれているのに、コードは「でも上限は50だから、50にしよう」と**勝手に切り詰めている**わけです。

### なぜ上限を置いていたのか

擁護しておくと、上限を置くこと自体は悪いことではありません。もしサーバーが壊れていて `pages` に途方もない数（たとえば1億）が入っていたら、プログラムは永遠に止まりません。**暴走を防ぐための安全装置**として、上限を置くのは定石です。

問題は上限を置いたことではなく、**上限に引っかかったことを誰にも知らせなかった**ことです。

### 何が起きていたかを、数字で書く

| 項目 | 値 |
|---|---|
| 1ページあたりの件数 | 10件 |
| サーバーが申告した総件数 | **1,816件** |
| サーバーが申告した総ページ数 | **182ページ** |
| コードが取りに行ったページ数 | **50ページ** |
| 実際に手元にあった件数 | **490件** |
| 見えていなかった件数 | **約1,326件（全体の約73%）** |
| 貧困線の表（var 195）の位置 | **698番目** |

**カタログの4分の1しか見ていなかった。** そして、探していた表は、見ていない4分の3のほうにありました。

698番目ということは、70ページ目あたりです。50ページで打ち切られていたら、**絶対に見つかりません**。

### この打ち切りは「静か」だった

決定的なのはここです。

- エラーは出ません
- 警告も出ません
- 「切りましたよ」という表示もありません
- 表示されるのは `総ページ数: 50 総件数: 1816` という行だけ

よく見れば、この行は変です。「総ページ数50」なのに「総件数1816」。1ページ10件なら、50ページでは500件にしかならないはずです。**この行自体が矛盾を叫んでいた**のですが、画面を流れていく無数の出力の一行に過ぎず、誰も止まって考えませんでした。

（後述しますが、この矛盾する表示は、後で「警告を出す」形に直しました。）

---

## 10. なぜ気づけなかったのか

技術的な原因は分かりました。ここからは、**なぜ人間もAIも見逃したのか**を考えます。この記事の主題はこちらです。

### 理由1：エラーではなく「答え」の形をしていた

プログラムが止まってくれれば、人は必ず調べます。しかしこの罠は止まりません。「0件でした」という、**完成した答え**の形で出てきます。

「0件」は日常的にありうる結果です。「該当なし」は珍しくありません。だから疑う理由がありません。

### 理由2：「無いこと」は証明しにくい

哲学的な話に聞こえるかもしれませんが、実務的な問題です。

- 「ある」ことの証明は簡単です。1つ見せればいい
- 「無い」ことの証明は難しい。**全部見たと言い切れなければならない**

「検索して0件」は、「全部見たけれど無かった」という主張です。しかし実際には「一部だけ見て、その中には無かった」でした。この差は、出力の見た目には現れません。

### 理由3：上限値を書いたのは、AI自身だった

ここが今回いちばん重要な発見です。

`max_page = 50` という値は、外から与えられたものではありません。**数日前に、AI自身が書いたコードの中の既定値**です。

生成AIは、コードを書くときに合理的な既定値を選びます。「一覧を全ページ取る関数を書いて」と言われたとき、暴走を防ぐために上限を入れるのは、むしろ良い設計判断です。

問題は、**その後です。**

AIは「poverty lineが0件だった」という結果を見たとき、自分が数日前に書いた `max_page = 50` を疑いませんでした。それは自分にとって「決まったこと」だったからです。代わりに、検索語の言語（英語かインドネシア語か）を疑いました。もっともらしい方向ですが、外れていました。

**AIは、知識が足りなくて間違えたのではありません。自分が置いた前提を疑わなかったから間違えたのです。**

そして人間の側も、そのコードを一行ずつ読んではいませんでした。数千行のコードを読み切ることは現実的ではないからです。

### 理由4：確認する手段が用意されていなかった

「一覧が完全かどうか」を確かめる仕組みが、どこにもありませんでした。作った側（AI）も、使う側（人間）も、「取れている」と暗黙に思い込んでいました。

**思い込みは、確認手段が無いところに住み着きます。**

---

## 11. 対処①：上限を上げる（これだけでは不十分）

いちばん簡単な対処は、上限を上げることです。

```r
bps_list_var_all <- function(domain = "0000", lang = "eng", max_page = 1000) {
```

50 を 1000 にしました。1ページ10件なので、1万件までは取れます。BPS の統計表は現在1,816件なので、当面は十分です。

**しかし、これでは根本的な解決になりません。** 理由は2つです。

1. **将来また同じことが起きる。** BPS が統計表を増やし続けて1万件を超えたら、まったく同じ罠に、まったく同じ形でかかります。しかも今度は、原因を知っている人がもういないかもしれません。
2. **他人が使うときに同じ罠にかかる。** 誰かが「通信量を減らしたい」と考えて `max_page = 20` にしたら、その瞬間に同じ事故が再発します。しかも、その人は自分が何をしたのか気づきません。

**上限そのものが悪いのではありません。「上限に当たったことが分からない」ことが悪いのです。**

だから、次の対処が本命です。

---

## 12. 対処②：打ち切りを検出して、必ず知らせる

打ち切りが起きたとき、**必ず目に見える形で知らせる**ようにしました。

### 書いたコード

```r
# --- ページ打ち切りを黙って起こさないための共通チェック -----------------
# 一覧系は1ページ10件。max_page を超えると **エラーにならず静かに切れる**。
# 切れた一覧に対して bps_find() を掛けると「該当なし」と出るが、
# それは「無い」ではなく「見ていない」。この2つを取り違えると、
# 実在する表を探して何時間も無駄にする。
# 実例: max_page=50（＝500件）で打ち切られた一覧に対して
#   bps_find(v, "poverty line") が0件を返した。var 195 は実在するのに。
.bps_pages <- function(info, max_page, what) {
  pages <- suppressWarnings(as.integer(info$pages))
  if (is.na(pages)) pages <- 1
  cat("総ページ数:", pages, " 総件数:", info$total, "\n")

  if (pages > max_page) {
    warning(sprintf(
      "%s の一覧が打ち切られます: 全%dページ中%dページだけ取得します。
  max_page を上げてください（この一覧に対する検索結果は不完全です）",
      what, pages, max_page), call. = FALSE)
    cat("[!!] 一覧が不完全です（", max_page, "/", pages, "ページ）\n", sep = "")
  }
  min(pages, max_page)
}
```

### 一行ずつ説明します

```r
pages <- suppressWarnings(as.integer(info$pages))
```
サーバーが教えてくれた総ページ数を、数値に変換します。`suppressWarnings` は「数値に変換できなくても文句を言わないで」という意味です。

```r
if (is.na(pages)) pages <- 1
```
変換できなかった場合（サーバーが総ページ数を教えてくれなかった場合）は、とりあえず1ページとして扱います。

```r
cat("総ページ数:", pages, " 総件数:", info$total, "\n")
```
**打ち切る前の、本当の総ページ数**を表示します。ここが元のコードとの大きな違いです。元のコードは `min` で切り詰めた**後**の値を表示していました。だから「総ページ数: 50 総件数: 1816」という、矛盾しているのに矛盾に見えない表示になっていたのです。

```r
if (pages > max_page) {
```
本当のページ数が上限より大きい、つまり**打ち切りが起きる**場合だけ、次の処理に進みます。

```r
  warning(sprintf("..."), call. = FALSE)
  cat("[!!] 一覧が不完全です（", max_page, "/", pages, "ページ）\n", sep = "")
```
警告を2種類出します。**わざと2種類にしています。**

- `warning()` は R の正式な警告です。まとめて後で表示されることがあり、大量の出力の中では見落としやすい
- `cat()` は、その場で画面に出ます。`[!!]` という記号を付けて、目に飛び込むようにしました

「大事なことは2回言う」を、コードでやっているわけです。

```r
  min(pages, max_page)
```
最後に、実際に取りに行くページ数を返します。ここは元のコードと同じです。**打ち切ること自体は変えていません。変えたのは「黙って切らない」ことだけです。**

### 呼び出す側

```r
bps_list_var_all <- function(domain = "0000", lang = "eng", max_page = 1000) {
  first <- bps_list_page("var", domain, lang, page = 1)
  if (!identical(first$status, "OK")) {
    cat("status:", first$status, " message:", first$message, "\n")
    return(NULL)
  }
  info  <- bps_pageinfo(first)
  pages <- .bps_pages(info, max_page, "動的表(var)")   # ← ここが変わった

  out <- bps_rows(first)
  if (pages > 1) {
    for (p in 2:pages) {
      r <- bps_list_page("var", domain, lang, page = p)
      out <- rbind(out, bps_rows(r))
      cat("\r取得中 ", p, "/", pages, sep = "")
    }
    cat("\n")
  }
  out
}
```

元は3行だったところが1行になり、しかも警告付きになりました。静的表を取る関数（`bps_list_static_all`）にも、同じ1行を入れました。**同じ罠は、必ず似た場所に複数あります。**

### 設計の考え方

ここでの判断は、次のように整理できます。

| やり方 | 良い点 | 悪い点 |
|---|---|---|
| 上限を無くす | 打ち切りが起きない | サーバー異常時に暴走する |
| 上限に当たったらエラーで止める | 絶対に見落とさない | 部分的な一覧でも使いたい場面で困る |
| **上限に当たったら警告して続ける** | **暴走せず、見落としもしない** | 警告を無視されたら意味がない |

3番目を選びました。**「安全装置は残す。ただし、作動したことは必ず伝える」**という考え方です。

これは、コードだけの話ではありません。ヒューズが飛んだのに何も表示されないブレーカーは危険です。飛んだら、飛んだと分かるようにする。それだけのことです。

---

## 13. 対処③：一覧をファイルに保存する

もう一つ、地味ですが効く対処を入れました。

### 問題

一覧の取得には数分かかります。182ページぶんの通信をするからです。R を再起動するたびに取り直していたら、時間の無駄です。

そこで、変数 `v` に入れて使い回していました。しかし、これには別の問題があります。

- **その `v` がいつ取ったものか分からない**
- **完全なものか、打ち切られたものか分からない**
- **R を閉じたら消える**

実際、今回の事故のとき、セッションの中には**打ち切られた古い `v`** が残っていました。診断スクリプトには「`v` が無ければ取り直す」と書いてありましたが、`v` は「有った」のです。中身が不完全なだけで。

```r
if (!exists("v")) {
  v <- bps_list_var_all()
}
```

この書き方は、**「有るかどうか」しか見ていません。「正しいかどうか」は見ていません。** これも同じ種類の罠です。

### 対処：ファイルに保存して、いつ取ったか分かるようにする

```r
#' 一覧をディスクにキャッシュして再利用する
#'
#' 一覧の取得は1ページ10件のページ送りなので、数千件あると数分かかる。
#' セッション変数（`v` や `st`）に置くだけだと、R を再起動するたびに
#' 取り直しになるうえ、**いつ取った一覧なのか・完全なのかが分からなくなる**。
#' ファイルに落としておけば、内容を後から確かめられる。
#'
#' @param refresh TRUE で取り直す。BPS が表を追加した可能性があるときに使う。
bps_catalog <- function(model = c("var", "statictable"), refresh = FALSE,
                        dir = "out", domain = "0000", lang = "eng") {
  model <- match.arg(model)
  f <- file.path(dir, sprintf("catalog_%s_%s_%s.rds", model, domain, lang))

  if (!refresh && file.exists(f)) {
    out <- readRDS(f)
    cat("[i] キャッシュを使用:", f, " (", nrow(out), "件)\n")
    return(out)
  }

  out <- if (model == "var") bps_list_var_all(domain = domain, lang = lang)
         else                bps_list_static_all(domain = domain, lang = lang)

  if (!is.null(out)) {
    dir.create(dir, showWarnings = FALSE, recursive = TRUE)
    saveRDS(out, f)
    cat("[i] 保存しました:", f, " (", nrow(out), "件)\n")
  }
  out
}
```

これで、一覧は `out/catalog_var_0000_eng.rds` というファイルになります。

良い点は3つです。

1. **後から中身を確かめられる。** ファイルを開いて件数を数えれば、完全かどうか分かる
2. **ファイルの更新日時が「いつ取ったか」を記録する。** 半年後に「この一覧、いつのだっけ」となっても、日時を見れば分かる
3. **件数が必ず表示される。** 読み込むたびに `(1710件)` と出るので、490件だったらすぐ気づく

3番目が地味に効きます。**毎回、目に入る場所に数字を置く**というのは、罠を防ぐうえでとても有効です。

---

## 14. 直ったことをどう確かめたか

修正しました、で終わりにはしません。**直ったことを確かめる**必要があります。

### 確認その1：件数が増えたか

```r
v  <- bps_catalog("var",         refresh = TRUE)
st <- bps_catalog("statictable", refresh = TRUE)
```

出力：

```
--- 動的表 ---
総ページ数: 182  総件数: 1816
取得中 182/182
[i] 保存しました: out/catalog_var_0000_eng.rds  ( 1710 件)

--- 静的表 ---
総ページ数: 72  総件数: 720
取得中 72/72
[i] 保存しました: out/catalog_statictable_0000_eng.rds  ( 710 件)
```

| | 修正前 | 修正後 |
|---|---|---|
| 動的表 | 490件 | **1,710件** |
| 静的表 | 490件 | **710件** |

3.5倍になりました。しかも「総ページ数: 182」と正直に表示されています。

### 確認その2：探していた表が入っているか

これが本命の確認です。

```r
cat("v に var 195 が入っているか: ",
    any(as.character(v$var_id) == "195"), "\n")
```

```
v に var 195 が入っているか:  TRUE
```

**入りました。** そして、その正体を表示させると：

```
--- var 195 の一覧上の姿 ---
    var_id                                                   title
698    195 Poverty Line (Rupiah/Kapita/Month) By Province and Area
```

**698番目**。50ページ（＝500件）で打ち切られていたら、絶対に届かない位置です。

タイトルは `Poverty Line (Rupiah/Kapita/Month) By Province and Area` ── まぎれもなく「poverty line」が入っています。検索語は最初から正しかったのです。

### 確認その3：本来の目的は達成できたか

完全な一覧に対して、あらためて検索します。今度は「poverty line」と決め打ちせず、**「poverty」だけ**で広く探しました（決め打ちで取り逃がした経験の直後なので、慎重になっています）。

```
########## 3. 動的表: 'poverty' を含むもの ##########
28 件
```

**0件が28件になりました。**

その中には、こういう表が並んでいました。

```
var 182   Poverty Line (Rupiah/Kapita/Month) By Area
var 192   Percentage of Poor Population (P0) by Province and Area
var 195   Poverty Line (Rupiah/Kapita/Month) By Province and Area
var 503   Poverty Gap Index by Province and Area
var 505   Food Poverty Line (Rupiah/Kapita/Month) By Province and Area
var 506   Non-Food Poverty Line (Rupiah/Kapita/Month) By Province and Area
var 624   Garis Kemiskinan Menurut Kabupaten/Kota
```

最後の `var 624` に注目してください。「Garis Kemiskinan Menurut Kabupaten/Kota」── これはインドネシア語で「県・市別の貧困線」という意味です。**この表の中に、探していた2011年のデータが入っていました。**

最初の検索が0件だったせいで、あやうくこの表に一生たどり着かないところでした。

（なお、この `var 624` にも別の罠が待っていました。英語で検索していたために、インドネシア語のタイトルを取り逃がしかけたのです。それは「罠02：カタログの二言語混在」で扱います。）

### 確認その4：警告が本当に出るか

安全装置は、**作動することを確かめて初めて安全装置**です。試しに上限を小さくして呼んでみます。

```r
v_short <- bps_list_var_all(max_page = 5)
```

```
総ページ数: 182  総件数: 1816
[!!] 一覧が不完全です（5/182ページ）
警告メッセージ:
動的表(var) の一覧が打ち切られます: 全182ページ中5ページだけ取得します。
  max_page を上げてください（この一覧に対する検索結果は不完全です）
```

**出ました。** これで、将来誰かが上限を下げても、黙って壊れることはありません。

---

## 15. おまけ：直しても残った小さな謎

正直に書いておきます。**完全には説明がついていないことが1つあります。**

サーバーは「総件数1,816件」と言っています。しかし、182ページ全部を取ってきて数えると **1,710件**でした。**106件足りません。**

静的表も同じで、720件のはずが710件。**10件足りません。**

これは今回の「打ち切り」とは別の問題です。おそらく、ページによって返ってくるデータの列の構成が違い、結合するときに落ちているのだと思われます。実際、行を結合する関数はこう書かれています。

```r
bps_rows <- function(res) {
  d <- res$data
  if (is.null(d)) return(NULL)
  body <- if (length(d) == 2 && is.list(d[[2]])) d[[2]] else d
  tryCatch(
    do.call(rbind, lapply(body, function(x) as.data.frame(x, stringsAsFactors = FALSE))),
    error = function(e) NULL          # ← ここで黙って NULL を返している
  )
}
```

`tryCatch(..., error = function(e) NULL)` は、「エラーが起きたら NULL を返す」という意味です。つまり、**結合に失敗したページは、丸ごと黙って捨てられます**。

……お気づきでしょうか。**これも同じ罠です。** 失敗が黙殺されている。

この記事の主題である「打ち切り」を直したのに、**すぐ隣に同じ形の罠が残っていた**わけです。今回の作業では貧困線の表が見つかったので先に進みましたが、これは未解決の課題として記録しておきます。

**教訓：`tryCatch` で黙って NULL を返す実装は、それ自体が罠の温床です。** 少なくとも、捨てたページ番号を表示すべきでした。

（この件は、今後 `bps_rows` を直すときに、失敗したページを記録して最後にまとめて報告する形に変える予定です。）

---

## 16. あなたのAPIでも同じことが起きる

ここまでインドネシアの統計APIの話をしてきましたが、**この罠はどのAPIにもあります**。ページングを使っていないAPIのほうが珍しいからです。

### よくあるページングの方式

| 方式 | 見た目 | 「終わり」の判定 |
|---|---|---|
| ページ番号方式 | `?page=3` | 総ページ数を見る／空が返るまで |
| オフセット方式 | `?offset=200&limit=100` | 返った件数が limit 未満なら終わり |
| カーソル方式 | `?cursor=abc123` | 次のカーソルが返ってこなくなったら終わり |
| Link ヘッダ方式 | HTTPヘッダに `rel="next"` | next が無くなったら終わり |

方式は違っても、罠の形は同じです。**「途中でやめたのに、やめたと言わない」**。

### 具体例

- **e-Stat（日本の政府統計）**：`getStatsList` は既定で取得件数に上限があり、`limit` と `startPosition` で続きを取ります。件数を数えずに使うと、一部だけ見て「無い」と判断しかねません
- **世界銀行 API**：既定は1ページ50件。`per_page` を指定しないと、200カ国のデータを取ったつもりが50件で終わります
- **GitHub API**：既定30件、最大100件。`Link` ヘッダを見ないと続きが取れません。「リポジトリのIssueを全部取った」と思っていたら最新30件だけ、というのは非常によくある事故です
- **OECD / Eurostat**：クエリが大きすぎると、エラーではなく**切り詰められた結果**が返ることがあります

### 共通する見分け方

**返ってきた件数が、キリのいい数と一致していたら疑ってください。**

- 10件ちょうど
- 50件ちょうど
- 100件ちょうど
- 500件ちょうど（＝10件×50ページ）

これらは「ちょうどそれだけあった」のではなく、「そこで切られた」可能性が高いのです。実際、今回の作業では、別の場面でも同じ判断が役に立ちました。年の一覧を取る関数が**きっかり10件**を返してきたとき、「10年分しか無い」ではなく「10件で切られている」と疑い、全ページを取るように直したところ、**19年分**ありました。

コードのコメントには、こう書き残しています。

```r
#' @note **この一覧も1ページ10件でページ送りされる。**
#'   ちょうど10件返ってきたら「それで全部」ではなく「切られている」と疑うこと。
```

---

## 17. チェックリスト

新しいAPIを使い始めるとき、この順に確認してください。5分で終わります。

### 取得する前に

- [ ] このAPIはページングするか。1ページ何件か
- [ ] 総件数を教えてくれるか（`total`、`count`、`X-Total-Count` ヘッダなど）
- [ ] 自分のコードに上限（`max_page`、`limit`、ループ回数）はあるか
- [ ] その上限は、総件数より大きいか

### 取得した直後に

- [ ] **総件数と、実際に手元にある件数を突き合わせたか**（これが最重要）
- [ ] 件数がキリのいい数（10、50、100、500…）になっていないか
- [ ] 打ち切りが起きたら警告が出る作りになっているか

### 検索して0件だったとき

- [ ] その一覧は完全か（件数を確認したか）
- [ ] **実在すると分かっているものが、その一覧に入っているか**を試したか
- [ ] 検索語を短くしても0件か
- [ ] 大文字小文字、表記ゆれ、別言語の可能性はないか

**3番目のチェック（実在するものを使った動作確認）を強くおすすめします。** 探し物が見つからないとき、まず「見つかるはずのもの」で試す。これは検索が壊れているかどうかを、一発で切り分けられます。

体温計が壊れているかを確かめたいなら、熱がある人ではなく、**健康な人**で測ってみればいい。それと同じ考え方です。

---

## 18. まとめ

### 何が起きたか

貧困線の表を探して0件が返り、「そんな表は無い」と結論しかけました。実際には、カタログを取る関数の上限（50ページ）に引っかかって、**全体の27%しか見ていなかった**だけでした。探していた表は698番目にありました。

### なぜ危なかったか

- エラーが出なかった
- 0件という「答え」の形をしていた
- もっともらしい説明（言語の問題では？）まで付いていた
- あきらめていたら、研究の一部が成立しなかった

### 何で気づいたか

**たった一つの矛盾です。** 「表195は実在する。しかし検索に出ない」。この2つは両立しません。

### どう直したか

1. 上限を50から1000に上げた（応急処置）
2. **打ち切りが起きたら必ず警告を出すようにした**（本命）
3. 一覧をファイルに保存し、件数が毎回目に入るようにした（再発防止）

### 一般化した教訓

> **「検索して0件」は、データについての事実ではなく、自分の手続きについての事実かもしれない。**

もう一つ、生成AIと一緒に作業する人へ。

> **AIは、知識が足りなくて間違えるのではなく、自分が置いた前提を疑わない形で間違える。**

`max_page = 50` を書いたのはAI自身でした。そして、0件という結果を見たとき、その値を疑いませんでした。**人間側の確認作業は、AIの知識を補うためではなく、AIの前提を外から破るために必要**なのです。

---

## 付録A：完成したコード全文

そのままコピーして使えます。BPS API 以外でも、考え方は流用できます。

```r
# =====================================================================
# 一覧の取得（打ち切りを黙って起こさない版）
# =====================================================================

# 1ページぶんを取る
bps_list_page <- function(model = "var", domain = "0000", lang = "eng", page = 1) {
  bps_request("list", "model", model, "lang", lang,
              "domain", domain, "page", page)
}

# 応答から行の部分を取り出す
bps_rows <- function(res) {
  d <- res$data
  if (is.null(d)) return(NULL)
  body <- if (length(d) == 2 && is.list(d[[2]])) d[[2]] else d
  tryCatch(
    do.call(rbind, lapply(body, function(x) as.data.frame(x, stringsAsFactors = FALSE))),
    error = function(e) NULL
  )
}

# 応答からページ情報（total, pages）を取り出す
bps_pageinfo <- function(res) {
  d <- res$data
  if (is.null(d) || length(d) < 1) return(NULL)
  d[[1]]
}

# --- 打ち切りを検出して知らせる（この記事の本命） ---------------------
.bps_pages <- function(info, max_page, what) {
  pages <- suppressWarnings(as.integer(info$pages))
  if (is.na(pages)) pages <- 1
  cat("総ページ数:", pages, " 総件数:", info$total, "\n")
  if (pages > max_page) {
    warning(sprintf(
      "%s の一覧が打ち切られます: 全%dページ中%dページだけ取得します。
  max_page を上げてください（この一覧に対する検索結果は不完全です）",
      what, pages, max_page), call. = FALSE)
    cat("[!!] 一覧が不完全です（", max_page, "/", pages, "ページ）\n", sep = "")
  }
  min(pages, max_page)
}

# 動的表の一覧を全ページ取得する
bps_list_var_all <- function(domain = "0000", lang = "eng", max_page = 1000) {
  first <- bps_list_page("var", domain, lang, page = 1)
  if (!identical(first$status, "OK")) {
    cat("status:", first$status, " message:", first$message, "\n")
    return(NULL)
  }
  info  <- bps_pageinfo(first)
  pages <- .bps_pages(info, max_page, "動的表(var)")

  out <- bps_rows(first)
  if (pages > 1) {
    for (p in 2:pages) {
      r <- bps_list_page("var", domain, lang, page = p)
      out <- rbind(out, bps_rows(r))
      cat("\r取得中 ", p, "/", pages, sep = "")
    }
    cat("\n")
  }
  out
}

# 静的表の一覧も同じ作りにする
bps_list_static_all <- function(domain = "0000", lang = "eng", max_page = 1000) {
  first <- bps_list_page("statictable", domain, lang, page = 1)
  if (!identical(first$status, "OK")) {
    cat("status:", first$status, " message:", first$message, "\n")
    return(NULL)
  }
  info  <- bps_pageinfo(first)
  pages <- .bps_pages(info, max_page, "静的表(statictable)")

  out <- bps_rows(first)
  if (pages > 1) {
    for (p in 2:pages) {
      r <- bps_list_page("statictable", domain, lang, page = p)
      out <- rbind(out, bps_rows(r))
      cat("\r取得中 ", p, "/", pages, sep = "")
    }
    cat("\n")
  }
  out
}

# 一覧をファイルに保存して再利用する
bps_catalog <- function(model = c("var", "statictable"), refresh = FALSE,
                        dir = "out", domain = "0000", lang = "eng") {
  model <- match.arg(model)
  f <- file.path(dir, sprintf("catalog_%s_%s_%s.rds", model, domain, lang))
  if (!refresh && file.exists(f)) {
    out <- readRDS(f)
    cat("[i] キャッシュを使用:", f, " (", nrow(out), "件)\n")
    return(out)
  }
  out <- if (model == "var") bps_list_var_all(domain = domain, lang = lang)
         else                bps_list_static_all(domain = domain, lang = lang)
  if (!is.null(out)) {
    dir.create(dir, showWarnings = FALSE, recursive = TRUE)
    saveRDS(out, f)
    cat("[i] 保存しました:", f, " (", nrow(out), "件)\n")
  }
  out
}

# 一覧から名前で絞り込む
bps_find <- function(df, pattern) {
  if (is.null(df)) { cat("一覧が取れていません\n"); return(invisible(NULL)) }
  key <- apply(df, 1, paste, collapse = " ")
  hit <- df[grepl(pattern, key, ignore.case = TRUE), , drop = FALSE]
  cols <- intersect(c("var_id", "table_id", "title", "sub_id", "subj"), names(hit))
  if (length(cols)) hit[, cols, drop = FALSE] else hit
}
```

### 使い方

```r
# 1回目：取得して保存（数分かかる）
v <- bps_catalog("var")

# 2回目以降：一瞬で読み込む
v <- bps_catalog("var")
#> [i] キャッシュを使用: out/catalog_var_0000_eng.rds  ( 1710 件)

# BPS が表を追加したかもしれないとき
v <- bps_catalog("var", refresh = TRUE)

# 検索する（英語とインドネシア語の両方で）
bps_find(v, "poverty line|garis kemiskinan")
```

---

## 付録B：用語集

| 用語 | 意味 |
|---|---|
| **API** | プログラムから使うための窓口。決まった形で質問すると、決まった形で答えが返る |
| **エンドポイント** | APIの中の、個別の窓口。「一覧をください」「データをください」など用途ごとに分かれている |
| **ページング（改頁）** | 大量の結果を小分けにして返す仕組み。1ページ10件など |
| **カタログ／一覧** | どんなデータがあるかの目録。図書館の目録カードにあたる |
| **動的表（var）** | BPS の統計表のうち、数値をJSONで返してくれるもの |
| **静的表（statictable）** | BPS の統計表のうち、ファイルとして配布されるもの。実体はHTML |
| **var_id** | 動的表につけられた番号。貧困線（州別）は195 |
| **JSON** | データをやりとりするための書式。`{ "名前": "値" }` の形 |
| **既定値（デフォルト）** | 指定しなかったときに自動的に使われる値。`max_page = 50` の 50 がそれ |
| **キャッシュ** | 一度取ったものを保存しておいて、次から使い回す仕組み |
| **`tryCatch`** | R の命令。エラーが起きたときの逃げ道を書ける。逃げ道で黙ると罠になる |

---

## 付録C：よくある質問

### Q1. 上限を最初から無くしておけばよかったのでは？

無限ループの危険があります。サーバーが壊れて `pages` に異常な値を返した場合、プログラムは止まらなくなります。**上限は残したまま、当たったら知らせる**というのが、今回採った答えです。

### Q2. 総件数が返ってこないAPIではどうすればいいですか？

「空のページが返るまで取り続ける」しかありません。その場合も、**何ページ取ったかを必ず記録して表示**してください。そして、上限に当たった場合は同じように警告します。

### Q3. AIに任せるのが悪いということですか？

いいえ。この作業全体は、AIとの協働なしには数日で終わりませんでした。**AIが悪いのではなく、AIの出力を確かめる仕組みが要る**、というだけの話です。人間が書いたコードにも `max_page = 50` は入りえます。違いは、人間なら自分の書いた既定値を覚えていることがある、という程度です。

### Q4. どうすればこの罠を最初から避けられましたか？

「取得件数と総件数を突き合わせる」という習慣が最初からあれば、避けられました。今回それを学んだので、次のAPIでは最初からやります。**罠を踏むことは避けられませんが、同じ罠を二度踏むことは避けられます。** そのために、この記事を書いています。

### Q5. 他の罠も知りたいのですが

この記事は連載の1本目です。同じ作業で見つかった罠を、順にまとめていきます。

1. **一覧APIの暗黙の打ち切り**（この記事）
2. カタログの二言語混在 ── 英語で検索すると、インドネシア語の表題を取り逃がす
3. 年の必須指定と3年上限 ── エラー応答からの仕様復元
4. 連結キーの桁数不定 ── キーを「分解しない」設計
5. 派生変数・期間の未指定 ── 全国合計が2倍になる
6. 行は存在するが値が空 ── 可用性は行数で判定してはいけない
7. 静的表の実体がHTML ── Excelと称するファイルが開けない
8. 州名の表記ゆれと脚注記号 ── 地図の一部が灰色になる
9. 行政区画の変更（33→34→38州） ── 足せる量と足せない量
10. 公表基準の断絶（3月調査と9月調査） ── 検算が失敗したとき、何を疑うか

---

## この記事について

この記事は、日本計算機統計学会シンポジウムでの報告「検算駆動による生成AIとの協働 ── インドネシア公的統計WebAPIからの州別パネル構築とShinyによる可視化」の補足資料として作成しました。

本文中のコード、出力、数値は、すべて実際の作業記録に基づいています。うまくいかなかった過程も、そのまま残しています。

### 生成AIの利用について

**本記事の文章は、生成AI（Claude Opus 5、推論設定「高」）が、久保田による指示（プロンプト）と複数回のやりとりを経て生成したものです。** 構成・記述・コード解説はAIによる出力であり、掲載しているコード、コンソール出力、数値は、いずれも実際の作業記録に基づいています。内容は著者が確認したうえで公開しています。

この記事の主題そのものが「生成AIと協働してデータを取得した記録」であるため、記事の生成過程を隠す理由がありません。念のため明記しておきます。

- 著者：久保田貴文（多摩大学）
- 連絡先：kubota@tama.ac.jp
- Web：https://takafumikubota.jp/

この作業で用いた統計は、インドネシア中央統計庁（BPS）が公開するWebAPIから取得しました。
Layanan ini menggunakan API Badan Pusat Statistik (BPS).
（本サービスはインドネシア中央統計庁のAPIを使用しています。）
