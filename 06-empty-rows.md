# 罠06：行は存在するが、値が空

> **一行でいうと**
> 「そこにデータがあるか」を、**行の有無**で判定してはいけない。
> **値の有無**で判定する。この2つは、同じではない。

**この罠の危険度：★★★★☆**
特徴は、**処理の順序を1行入れ替えるだけで結果が変わる**ことです。コードのどこにも間違いが見当たらないのに、答えが違う。そういう種類の罠です。

---

## 目次

1. [前回までのあらすじ](#1-前回までのあらすじ)
2. [症状：2010年と2011年だけ、貧困率が消えた](#2-症状2010年と2011年だけ貧困率が消えた)
3. [最初の誤診：「2012年からしか無いのだろう」](#3-最初の誤診2012年からしか無いのだろう)
   - [3.5 この誤診は、なぜ致命的だったか](#35-この誤診はなぜ致命的だったか)
4. [数えてみる ── 行数と非NA数を分けて出す](#4-数えてみる--行数と非na数を分けて出す)
5. [見えたもの：枠はあるが、中身が無い](#5-見えたもの枠はあるが中身が無い)
6. [なぜこうなるのか](#6-なぜこうなるのか)
7. [真因は、処理の順序だった](#7-真因は処理の順序だった)
8. [直し方：1行を上に移す](#8-直し方1行を上に移す)
9. [もう1つの症状：「1 行」](#9-もう1つの症状1-行)
10. [欠測には、3つの状態がある](#10-欠測には3つの状態がある)
11. [直ったことをどう確かめたか](#11-直ったことをどう確かめたか)
12. [あなたのAPIでも同じことが起きる](#12-あなたのapiでも同じことが起きる)
13. [チェックリスト](#13-チェックリスト)
14. [まとめ](#14-まとめ)
15. [付録A：コード全文](#付録aコード全文)
16. [付録B：よくある質問](#付録bよくある質問)

---

## 1. 前回までのあらすじ

- [罠01](01-silent-truncation.md)：カタログが500件で静かに切れていた
- [罠02](02-bilingual-catalog.md)：英語で絞ると、インドネシア語の表題が消えた
- [罠03](03-year-parameter.md)：エラーから、年の必須指定と3年上限が判明した
- [罠04](04-composite-key.md)：キーを切り分けず、組み立てる設計にした
- [罠05](05-derived-dimensions.md)：派生変数・期間を選ばずに足すと、合計が2倍になる

[罠05](05-derived-dimensions.md) で、期間（3月／9月）を明示的に選ぶ仕組みを入れました。**貧困率の合計は正しくなりました。**

ところが、その直後に別のことが起きます。

---

## 2. 症状：2010年と2011年だけ、貧困率が消えた

期間の指定を入れて貧困率を取り直し、年ごとの状態を確認しました。

```
   year n_prov n_rows  total n_na
1  2012     33     33 511.86    0
2  2013     33     33 495.32    0
3  2014     33     33 482.71    0
...
14 2025     33     33 320.55    0
```

**2012年から2025年まで、14年分。きれいにそろっています。**

州数も行数も33で一致（[罠05](05-derived-dimensions.md) の不変量を通っています）。欠測もゼロ。

**しかし、2010年と2011年がありません。**

要求は2010〜2025年の16年分で出していました。しかし手元に残ったのは14年分でした。

---

## 3. 最初の誤診：「2012年からしか無いのだろう」

このとき、最初に頭に浮かんだ解釈はこれでした。

> 州別の貧困率は、2012年からしか公表されていないのだろう。

**もっともらしい解釈です。** 実際、統計というのはどこかで始まるものですし、この作業ではすでに「州別の貧困線は2013年から」という事実に当たっています（[罠02](02-bilingual-catalog.md)）。「貧困率も2012年からか」と思うのは自然です。

そして、この解釈には**証拠らしきもの**もありました。取得のログです。

```
取得中 [1/6] th=110;111;112 ... 702 行
```

2010〜2012年のチャンクは、**702行ちゃんと返ってきています**。エラーではありません。にもかかわらず、最終的に残ったのは2012年だけ。

つまり、「行は返ってきたが、2010年と2011年は使える形になっていなかった」ということです。これを「データが無いから」と読むのは、**一見すると筋が通っています**。

**しかし、これは誤りでした。**

BPS は2010年も2011年も、州別の貧困率を公表しています。**取れなかったのは、こちらの手続きのせいでした。**

---

## 3.5 この誤診は、なぜ致命的だったか

先に進む前に、**もし「2012年からしか無い」で納得していたら何が起きたか**を書いておきます。

失われるのは2年分です。16年のうち2年なので、割合でいえば12.5%。**それだけなら大した話ではありません。**

問題は、**その2年のうちの1つが2011年だった**ことです。

この作業全体の目的は、2011年の家計調査（SUSENAS）を使った分析を支える州別指標をそろえることでした。つまり**2011年こそが本命の年**です。

貧困率は、その分析で使う予定の指標のひとつでした。もし「2011年の州別貧困率は公表されていない」と結論していたら、

- 2011年の分析から、貧困率という変数が丸ごと落ちる
- あるいは「2012年の値で代用する」という妥協をする
- そして論文には「2011年のデータが利用できないため」と書く

**存在するデータを見落としたまま、その旨を論文に書いてしまう。** これは[罠01](01-silent-truncation.md) で危うくやりかけたことと、まったく同じ構図です。

しかも今回は、誤診のほうが**もっともらしく見えました**。

- 702行ちゃんと返ってきている（＝通信は成功している）
- エラーも警告も無い
- 「2012年から公表が変わった」という事実は実際に存在する（年1回→年2回）
- したがって「2012年から」という説明は、**半分正しい**

**半分正しい説明が、いちばん危険です。** 完全に間違った説明なら矛盾が出ますが、半分正しい説明は他の事実と整合してしまいます。実際「2012年に公表形態が変わった」は真であり、だからこそ「2012年から始まった」も真らしく聞こえました。

> **もっともらしい説明を思いついたときこそ、数えて確かめる。**

---

## 4. 数えてみる ── 行数と非NA数を分けて出す

誤診を避けられたのは、[罠01](01-silent-truncation.md) 以来の習慣のおかげです。

> **「無い」と結論する前に、数える。**

そこで、**期間ごとに、行が何本あって、そのうち値が入っているものが何本あるか**を出しました。

```r
raw |>
  bps_only_province() |>
  group_by(year, turtahun_label) |>
  summarise(n = n(), n_ok = sum(!is.na(value)), .groups = "drop")
```

たった1行の集計ですが、**`n` と `n_ok` を分けている**ことが肝心です。

- `n` … 行が何本あるか
- `n_ok` … そのうち、**値が入っている**行が何本あるか

これを分けずに `n()` だけ見ていたら、この罠は解けませんでした。

---

## 5. 見えたもの：枠はあるが、中身が無い

出てきたのが、これです。

```
   year         turtahun_label   n  n_ok
1  2010               Annually  38    33     ← 値がある
2  2010     Semester 1 (March)  38     0     ← 行はあるが空
3  2010 Semester 2 (September)  38     0     ← 行はあるが空
4  2011               Annually  38    33     ← 値がある
5  2011     Semester 1 (March)  38     0
6  2011 Semester 2 (September)  38     0
7  2012               Annually  38     0     ← 逆に、こちらが空
8  2012     Semester 1 (March)  38    33
9  2012 Semester 2 (September)  38    33
```

**すべての年・すべての期間に、38行ずつあります。** 行数だけ見れば、どれも同じ顔をしています。

ところが `n_ok` を見ると、まったく違います。

| 年 | Annually | 3月 | 9月 |
|---|---|---|---|
| 2010 | **33** | 0 | 0 |
| 2011 | **33** | 0 | 0 |
| 2012 | 0 | **33** | **33** |

**2011年までは「年次」に、2012年からは「3月・9月」に入っている。**

つまりデータは**存在していました**。ただ、こちらが「3月を使う」と指定していたので、2010年と2011年については**空っぽの箱を選んでいた**だけです。

そして空っぽの箱を選べば、当然その年は落ちます。**「データが無い」ように見えるわけです。**

---

## 6. なぜこうなるのか

BPS を責める話ではないので、事情を整理しておきます。

インドネシアの貧困統計は、**2011年までは年1回、2012年から年2回（3月・9月）**の公表に変わりました。調査の頻度が上がったわけで、統計としては改善です。

問題は、**APIの表が「最新の形」で作られている**ことです。

現在の表は「3月・9月」という枠を持っています。そこに過去のデータを流し込むと、2011年以前には3月・9月の区別が存在しないので、**枠だけが残り、中身が空になります**。そして年1回だった時代の値は「Annually」という別の枠に入っています。

これは、データベースの設計として珍しいことではありません。

```
年 × 期間 × 地域
```

という直積で表を作れば、**存在しない組合せの枠も自動的にできます**。存在しないものを「行ごと消す」ほうが、実は手間がかかります。

**つまり、空の行は「バグ」ではなく「設計の帰結」です。** だから直りません。こちらが対処するしかありません。

---

## 7. 真因は、処理の順序だった

では、こちらの何が悪かったのか。

当時のコードは、こういう順序でした。

```r
# ① 期間を選ぶ
labs <- unique(x$turtahun_label[nzchar(x$turtahun_label)])
if (length(labs) > 1) {
  ... turtahun_pick から選ぶ ...
}

# ② 欠測を落とす
x <- filter(x, !is.na(value))
```

**「期間を選ぶ」が先、「欠測を落とす」が後。**

この順序だと、①の段階で「どの期間が使えるか」を判断するとき、**空の行も『使える』と数えてしまいます**。

2010年について考えてみます。①の時点で見えている期間は、

```
Annually / Semester 1 (March) / Semester 2 (September)
```

**3つとも「ある」ように見えます。** 行が存在するからです。中身が空であることは、この時点では見ていません。

そこで指定どおり「Semester 1 (March)」を選び、**そのあとで**②の欠測落としを実行すると、**全部消えます**。

結果、2010年は0行になり、パネルから脱落します。

### 順序を入れ替えると

逆にすると、こうなります。

```r
# ① 欠測を落とす
x <- filter(x, !is.na(value))

# ② 期間を選ぶ
labs <- unique(x$turtahun_label[nzchar(x$turtahun_label)])
...
```

2010年について、①の段階で空の行が消えます。残るのは「Annually」だけ。

②で期間の候補を数えると、**候補は「Annually」1つしかありません**。優先順位のリスト（`c("Semester 1 (March)", "Annually")`）から、使えるものを選べば「Annually」になります。

**2010年のデータが取れました。**

### コードの差は1行の移動だけ

変更点は、`filter(!is.na(value))` を数行上に移しただけです。**追加も削除もありません。**

しかしこの1行の位置で、16年分そろうか14年分になるかが決まりました。

> **同じ処理でも、順序が違えば別の答えを出す。**

コードには、なぜこの順序なのかを書き残しました。**順序の理由は、コードを読んでも分かりません。** 書かなければ、次に誰かが「読みやすさ」を理由に元に戻します。

```r
# --- 【重要】欠測を先に落とす --------------------------------------
# BPS は「行はあるが値が空」というケースを返す。たとえば貧困率の
# 2010-2011 は、3月・9月の行が存在するのに中身は空で、実データは
# Annually にしか入っていない。
# 期間の選択を「行があるか」で判定すると空の期間を掴んでしまうので、
# **欠測を落としてから** どの期間が使えるかを判定する。
x <- filter(x, !is.na(value))
```

---

## 8. 直し方：1行を上に移す

改めて、正しい順序を整理します。

```
1. 州の行だけ残す
2. 派生変数を選ぶ（Total など）
3. ★ 欠測を落とす ★          ← ここ
4. 使える期間を判定して選ぶ
5. 重複ガード（1州×1年＝1行）
```

**3が4より先。** これだけです。

### なぜ2（派生変数）は先でいいのか

派生変数の選択（Total を選ぶ、など）は、欠測落としより**前**にあります。矛盾しないでしょうか。

しません。理由は、**派生変数は「指定するもの」で、期間は「判定するもの」だから**です。

- 派生変数：使うものを外から指定する。存在しなければ0行になり、それはそれで気づける
- 期間：**年によって使えるものが変わる**ので、データを見て判定する必要がある

**「データを見て判定する処理」の前に、判定を狂わせるものを取り除く。** これが順序の原則です。

### 副次的な効果

この修正には、思わぬ効果もありました。

**年によって違う期間が使われた場合に、それが表示される**ようになったのです。

```
[i] 年によって使った期間が違います:
   year               pick
1  2010           Annually
2  2011           Annually
3  2012 Semester 1 (March)
4  2013 Semester 1 (March)
...
```

これは[罠05](05-derived-dimensions.md) で入れた表示ですが、修正前は**そもそも2010・2011年が脱落していたので出番がありませんでした**。

そして、この表示が出たことで、新たな事実が見えます。

> **2011年までと2012年からで、使っている調査時点が違う。**

これは**分析上、無視できない事実**です。年1回公表だった時代の値と、3月時点の値を、同じ系列として並べていいのか。この問いは、後の[罠10](README.md)で正面から扱うことになります。

**1行の移動が、データを16年に戻し、同時に系列の非同質性を可視化しました。**

---

## 9. もう1つの症状：「1 行」

貧困率が直った直後、**よく似ているが違う症状**に当たりました。貧困線（[罠02](02-bilingual-catalog.md) で見つけた表）を2010年から取ろうとしたときです。

```
取得中 [1/6] th=110;111;112 ... 1 行
取得中 [2/6] th=113;114;115 ... 234 行
```

**1行。**

貧困率のときは702行返ってきて、中身が空でした。今回は**行そのものが1つしかありません**。

### 「1行」の意味

[罠04](04-composite-key.md) で見たとおり、この作業のパーサは行数を

```
地域の数 × 派生変数の数 × 年の数 × 期間の数
```

で決めています。これが1になるということは、**すべての次元が1つずつ**だったということです。

そして、次元が空だったときは既定値で1行作る仕組みになっています（[罠04 §10](04-composite-key.md)）。つまり、

> **「1行」は「1件のデータがあった」ではなく、「応答の中身が空だった」という合図。**

`status` は `OK` です。エラーではありません。しかし**中身が何も入っていません**。

### 貧困率との違い

この2つは、症状は似ていますが**意味がまったく違います**。

| | 貧困率 2010-2011 | 貧困線 2010-2012 |
|---|---|---|
| 行数 | 702（普通） | **1**（＝空） |
| 3月・9月の値 | 空 | ── |
| 年次の値 | **ある** | ── |
| 結論 | **別の期間に入っている** | **表に存在しない** |
| 対処 | 順序を直す | **別の表を探す** |

貧困率は「探し方が悪かった」。貧困線は「本当に無い」。

**同じ『取れない』でも、次にやるべきことが正反対です。** 前者で別の表を探し始めたら時間の無駄ですし、後者で順序をいじっても永久に取れません。

そして実際、貧困線については別の表（県別の表）に2011年の値があり、そこから取り直すことになりました（[罠02](02-bilingual-catalog.md)）。

---

## 10. 欠測には、3つの状態がある

ここまでを整理すると、「データが無い」には**3つの異なる状態**があります。

| 状態 | 行数 `n` | 非NA数 `n_ok` | 意味 | 次にやること |
|---|---|---|---|---|
| **A. 枠すら無い** | 0（または1＝空応答） | 0 | その年・その切り口は、**表に存在しない** | 別の表を探す |
| **B. 枠はあるが空** | >0 | **0** | 枠は用意されているが、**未公表か、別の切り口に入っている** | 他の切り口を探す |
| **C. 値がある** | >0 | >0 | 使える | 進む |

**この3つを区別せずに「無い」と言うのが、この罠の本体です。**

そして、区別するために必要なものは、たった1つです。

> **`n` と `n_ok` を、常に並べて出すこと。**

どちらか一方しか見ていないと、A と B が区別できません。

- `n` だけ見ている → B を「ある」と誤認する（今回の罠）
- `n_ok` だけ見ている → A と B が同じに見える（次の手を間違える）

### 実装としてどうするか

この作業では、パネルの状態を出す関数に最初から両方を入れました（[罠05](05-derived-dimensions.md) で `n_rows` を足したのも同じ発想です）。

```r
check_panel <- function(df) {
  df |>
    group_by(year) |>
    summarise(
      n_prov  = n_distinct(prov_name),
      n_rows  = n(),                    # 重複の検出用
      total   = sum(value, na.rm = TRUE),
      n_na    = sum(is.na(value)),      # 欠測の検出用
      .groups = "drop"
    ) |>
    arrange(year)
}
```

そして、診断のときは切り口ごとにも数えます。

```r
# 「行はあるが空」を見抜くための定型
raw |>
  group_by(year, turvar_label, turtahun_label) |>
  summarise(n = n(), n_ok = sum(!is.na(value)), .groups = "drop")
```

**この3行は、新しい表に触るたびに必ず走らせる定型**になりました。

---

## 11. 直ったことをどう確かめたか

### 確認その1：16年そろったか

```
   year n_prov n_rows  total n_na
1  2010     33     33 553.99    0
2  2011     33     33 545.23    0
3  2012     33     33 511.86    0
...
16 2025     33     33 320.55    0
```

**2010年と2011年が戻りました。** 16年、全部33州、欠測ゼロ。

### 確認その2：戻ってきた値は妥当か

年が増えただけでは足りません。**中身が正しいか**を見ます。

貧困率は、この期間を通じて**低下傾向**にあるはずです。全国の加重平均を並べると、

```
2010 → 2011 → 2012 → ... → 2025
13.3%  12.5%  11.7%        ...  8.6%
```

**単調に下がっています。** そして、戻ってきた2010・2011年が、2012年以降の系列と**滑らかにつながっています**。

もし空の行を掴んだままだったり、間違った期間の値を入れていたら、ここに段差が出るはずです。段差が無いことは、値が妥当であることの間接的な証拠になります。

（逆に言えば、**段差があったら疑う**。この考え方が[罠10](README.md)で決定的な役割を果たします。）

### 確認その3：どの年にどの期間を使ったかが表示されるか

```
[i] 年によって使った期間が違います:
   year               pick
1  2010           Annually
2  2011           Annually
3  2012 Semester 1 (March)
...
```

**表示されました。** これが出ないまま16年そろったら、それはそれで怖い。「なぜ2010年が取れたのか」が分からないからです。

> **直った理由が見える形で直すこと。** 直った結果だけでは、次に壊れたときに戻れません。

---

## 12. あなたのAPIでも同じことが起きる

「枠はあるが中身が無い」は、**時系列データを扱うAPIでは、ほぼ確実に遭遇します**。

### よくある発生源

| 場面 | 何が起きるか |
|---|---|
| 調査の頻度が変わった | 古い年に、新しい区分の空行が残る |
| 項目が新設された | 新設前の年が空になる |
| 項目が廃止された | 廃止後の年が空になる |
| 地域が新設された | 新設前の年が空になる（→ [罠09](README.md)） |
| 集計方法が変わった | 移行期に両方の枠ができ、片方が空 |
| 秘匿処理 | 標本が少ない地域が空になる |

最後の「秘匿」は特に注意が要ります。**空の理由が「小さすぎて公表できない」ときは、その空自体が情報**です。0として扱ってはいけません。

### 具体例

- **e-Stat（日本）**：調査年次によって項目が変わる表で、該当しない年のセルが空文字や「－」で返ります。数値に変換すると NA になりますが、行は存在します
- **Eurostat**：加盟前の年について、国×年の枠は存在するが値が無い
- **世界銀行**：ほとんどの指標で、国×年の全組合せが返ってきます。値の無いところは `null`
- **各国の統計局**：定義変更の前後で、新旧両方の系列コードが同時に存在し、片方が空

### 判定の原則

> **「その年のデータがあるか」を、行の有無で判定しない。**
> **値の有無（非NA数）で判定する。**

そして、その判定を**他の処理より先に**行う。

---

## 13. チェックリスト

### 新しい表に触ったとき

- [ ] 年 × 切り口ごとに、**`n` と `n_ok` を並べて**出したか
- [ ] `n > 0` だが `n_ok == 0` の組合せはないか（＝枠だけの行）
- [ ] `n` が極端に小さい（1など）チャンクはないか（＝空応答）
- [ ] 年によって、値の入っている切り口が変わっていないか

### 処理の順序について

- [ ] **データを見て判定する処理**の前に、欠測を落としているか
- [ ] 順序に意味がある箇所に、**理由をコメントで書いた**か
- [ ] 順序を入れ替えたら結果が変わる箇所を、把握しているか

### 「無い」と結論する前に

- [ ] 枠すら無いのか、枠はあるが空なのかを区別したか
- [ ] 空なら、**他の切り口に入っていないか**探したか
- [ ] 本当に無いなら、**別の表を探したか**（→ [罠02](02-bilingual-catalog.md)）

---

## 14. まとめ

### 何が起きたか

貧困率の2010・2011年が取れませんでした。「2012年からしか無いのだろう」と誤診しかけましたが、**行数と非NA数を分けて数えたところ**、3月・9月の行は38本あるのに値が全部空で、実データは「年次」の行に入っていました。

原因は、**期間を選んでから欠測を落としていた**ことです。空の行も「使える期間」として数えてしまい、空っぽの箱を選んでいました。

### どう直したか

**`filter(!is.na(value))` を数行上に移しただけ**です。追加も削除もありません。それで14年分が16年分に戻りました。

### 一般化した教訓

> **「そこにデータがあるか」を、行の有無で判定してはいけない。値の有無で判定する。**

> **同じ処理でも、順序が違えば別の答えを出す。**
> データを見て判定する処理の前に、判定を狂わせるものを取り除く。

> **「無い」には3つの状態がある。**
> 枠すら無い／枠はあるが空／値がある。この3つで、次にやることが変わる。

### 生成AIとの協働という観点から

この罠は、**AIも人間も、コードを読んでいるだけでは絶対に見つけられません。** どちらの順序でも、コードは自然に読めます。エラーも警告も出ません。

見つかったのは、**実行して、出力を数えたから**です。

そして数え方が肝心でした。`n()` だけなら「どの期間も38行ある」で終わっていました。`n_ok` を並べたから、違いが見えました。

> **検算は「走らせること」ではなく「何を数えるか」で決まる。**

この連載を通じて、AIが書く検算は「動かして結果を出す」ところまでは自動でできます。しかし**何を数えるべきかを決めるのは、その表の事情を知っている側**です。「行はあるが空かもしれない」と疑えるかどうかが、`n_ok` という1列の有無を分けました。

---

## 付録A：コード全文

```r
# =====================================================================
# 「行はあるが値が空」への対処
# =====================================================================

to_stats_rows <- function(df, indicator, turvar_pick = "Total", unit_mult = 1,
                          turtahun_pick = NULL) {

  # ------------------------------------------------------------------
  # 1. 州の行だけ残す
  # ------------------------------------------------------------------
  x <- bps_only_province(df)

  # ------------------------------------------------------------------
  # 2. 派生変数(turvar)を選ぶ
  #    これは「指定するもの」なので、判定より前でよい
  # ------------------------------------------------------------------
  if (!is.null(turvar_pick) && any(nzchar(x$turvar_label))) {
    x <- filter(x, turvar_label == turvar_pick)
  }

  # ------------------------------------------------------------------
  # 3. 【重要】欠測を先に落とす
  # ------------------------------------------------------------------
  # BPS は「行はあるが値が空」というケースを返す。たとえば貧困率の
  # 2010-2011 は、3月・9月の行が存在するのに中身は空で、実データは
  # Annually にしか入っていない。
  # 期間の選択を「行があるか」で判定すると空の期間を掴んでしまうので、
  # **欠測を落としてから** どの期間が使えるかを判定する。
  #
  # ★この1行の位置で、16年そろうか14年になるかが決まる。
  #   読みやすさを理由に下へ動かさないこと。
  # ------------------------------------------------------------------
  x <- filter(x, !is.na(value))

  # ------------------------------------------------------------------
  # 4. 期間(turtahun)を判定して選ぶ
  #    turtahun_pick は優先順位のベクトル。年ごとに
  #    「使える中でいちばん優先度の高いもの」を選ぶ。
  # ------------------------------------------------------------------
  labs <- unique(x$turtahun_label[nzchar(x$turtahun_label)])
  if (length(labs) > 1) {
    if (is.null(turtahun_pick)) {
      stop("この表は期間が複数あります。turtahun_pick を指定してください:\n  ",
           paste(labs, collapse = "\n  "),
           "\n(年2回公表の指標では必須。指定しないと合計が倍になります)")
    }

    chosen <- x |>
      distinct(year, turtahun_label) |>
      group_by(year) |>
      summarise(pick = {
        p <- turtahun_pick[turtahun_pick %in% turtahun_label]
        if (length(p)) p[1] else NA_character_
      }, .groups = "drop")

    bad <- chosen |> filter(is.na(pick))
    if (nrow(bad) > 0) {
      cat("[!] 指定した期間がどれも無い年（落とします）:",
          paste(bad$year, collapse = ", "), "\n")
    }
    # 年によって違う期間を使ったなら、必ず見せる（黙って混ぜない）
    if (length(unique(chosen$pick[!is.na(chosen$pick)])) > 1) {
      cat("[i] 年によって使った期間が違います:\n")
      print(as.data.frame(chosen))
    }

    x <- x |>
      inner_join(chosen, by = "year") |>
      filter(!is.na(pick), turtahun_label == pick) |>
      select(-pick)
  }

  out <- x |>
    mutate(prov_name = normalize_prov(region),
           indicator = indicator,
           value     = value * unit_mult) |>
    select(prov_name, indicator, year, value) |>
    arrange(year, prov_name)

  # ------------------------------------------------------------------
  # 5. 重複ガード（不変量）: 1州×1年＝1行
  # ------------------------------------------------------------------
  dup <- out |> count(prov_name, year) |> filter(n > 1)
  if (nrow(dup) > 0) {
    cat("[!] 1州×1年に複数行が残っています(先頭10件):\n")
    print(as.data.frame(head(dup, 10)))
    stop("重複があります。turvar_pick / turtahun_pick を見直してください。")
  }

  out
}
```

### 診断の定型（新しい表に触るたびに走らせる）

```r
# --- 「行はあるが空」を見抜く ---------------------------------------
# n と n_ok を必ず並べて出す。
#   n = 0 (または応答が1行)  → 枠すら無い。別の表を探す
#   n > 0 かつ n_ok = 0      → 枠はあるが空。他の切り口を探す
#   n > 0 かつ n_ok > 0      → 使える
diagnose <- function(raw) {
  raw |>
    bps_only_province() |>
    dplyr::group_by(year, turvar_label, turtahun_label) |>
    dplyr::summarise(n = dplyr::n(),
                     n_ok = sum(!is.na(value)),
                     .groups = "drop") |>
    dplyr::arrange(year, turvar_label, turtahun_label)
}

# 使い方
raw <- bps_get_data(192, 2010:2013)     # 貧困率
print(as.data.frame(diagnose(raw)))
```

---

## 付録B：よくある質問

### Q1. 空の行を、最初から全部落としてはいけないのですか？

**落としてよいですし、この記事の対処はまさにそれです。** ただし「最初から」には注意が要ります。

空の行にも、情報がある場合があります。

- **秘匿**による空（標本が少なくて公表できない）
- **調査対象外**による空（その年はその地域を調査していない）
- **構造的な空**（[罠05](05-derived-dimensions.md) の「ジャカルタに農村部が無い」など）

これらは「値が無い」という事実そのものが結果です。落とす前に、**なぜ空なのかを1度は確かめる**べきです。

今回のように「別の切り口に入っている」と分かったなら、落として構いません。

### Q2. `n_ok` の代わりに `n_na` ではだめですか？

同じことです。`n_ok = n - n_na` なので、どちらでも判定できます。

ただし**並べて出す**なら `n` と `n_ok` のほうが読みやすいと感じました。「38本中33本に値がある」は直感的ですが、「38本中5本が欠測」は一拍考える必要があります。

好みの問題ですが、**どちらか一方だけを出さない**ことが重要です。

### Q3. 順序が重要な処理を、どうやって守ればいいですか？

3つあります。

1. **コメントに理由を書く**（今回やったこと）
2. **関数に分ける**：`drop_missing_then_pick_period()` のように、順序を名前に埋め込む
3. **検査を置く**：期間を選ぶ処理の冒頭で `stopifnot(!any(is.na(x$value)))` と書いておけば、順序が入れ替わった瞬間に止まる

3番目がいちばん強力です。**コメントは読まれないことがありますが、検査は必ず実行されます。**

今回は 1 だけにしましたが、規模が大きくなれば 3 を入れるべきでしょう。

### Q4. 他の罠も知りたいのですが

この記事は連載の6本目です。

1. [一覧APIの暗黙の打ち切り](01-silent-truncation.md)
2. [カタログの二言語混在](02-bilingual-catalog.md)
3. [年の必須指定と3年上限](03-year-parameter.md)
4. [連結キーの桁数不定](04-composite-key.md)
5. [派生変数・期間の未指定](05-derived-dimensions.md)
6. **行は存在するが値が空**（この記事）
7. 静的表の実体がHTML ── Excelと称するファイルが開けない
8. 州名の表記ゆれと脚注記号 ── 地図の一部が灰色になる
9. 行政区画の変更（33→34→38州） ── 足せる量と足せない量
10. 公表基準の断絶（3月調査と9月調査） ── 検算が失敗したとき、何を疑うか

---

## この記事について

この記事は、日本計算機統計学会シンポジウムでの報告「検算駆動による生成AIとの協働 ── インドネシア公的統計WebAPIからの州別パネル構築とShinyによる可視化」の補足資料として作成しました。

本文中のコード、出力、数値は、すべて実際の作業記録に基づいています。うまくいかなかった過程も、そのまま残しています。

### 生成AIの利用について

**本記事の文章は、生成AI（Claude Opus 5、推論設定「高」）が、久保田による指示（プロンプト）と複数回のやりとりを経て生成したものです。** 構成・記述・コード解説はAIによる出力であり、掲載しているコード、コンソール出力、数値は、いずれも実際の作業記録に基づいています。内容は著者が確認したうえで公開しています。

この記事の主題そのものが「生成AIと協働してデータを取得した記録」であるため、記事の生成過程を隠す理由がありません。念のため明記しておきます。

- 著者：久保田貴文（多摩大学）
- 連絡先：kubota@tama.ac.jp
- Web：https://takafumikubota.jp/

この作業で用いた統計は、インドネシア中央統計庁（BPS）が公開するWebAPIから取得しました。
Layanan ini menggunakan API Badan Pusat Statistik (BPS).
（本サービスはインドネシア中央統計庁のAPIを使用しています。）
