# 罠08：州名の表記ゆれと脚注記号 ── 地図の一部が灰色になる

> **一行でいうと**
> 「どの年も33州そろっている」ことと、「**年をまたいで同じ33州である**」ことは、別のことである。
> 前者だけを検査していると、後者が壊れていても気づけない。

**この罠の危険度：★★★★☆**
検査を通ってしまうのが厄介です。年ごとの州数を数える限り、どの年も33で正常。**壊れていることは、地図を描くまで分かりませんでした。**

---

## 目次

1. [前回までのあらすじ](#1-前回までのあらすじ)
2. [症状：2020年から、地図に穴があく](#2-症状2020年から地図に穴があく)
3. [なぜ地図が灰色になるのか](#3-なぜ地図が灰色になるのか)
4. [犯人：同じ州に、2つの名前](#4-犯人同じ州に2つの名前)
5. [表記ゆれのカタログ](#5-表記ゆれのカタログ)
6. [対処①：別名表をつくる](#6-対処別名表をつくる)
7. [別名表だけでは、なぜ足りないのか](#7-別名表だけではなぜ足りないのか)
8. [対処②：部分一致の保険](#8-対処部分一致の保険)
9. [対処③：冪等にする](#9-対処冪等にする)
10. [対処④：検査を足す ── 本命はここ](#10-対処検査を足す--本命はここ)
11. [脚注記号とHTMLタグ](#11-脚注記号とhtmlタグ)
12. [そもそも名前で結合すべきではない](#12-そもそも名前で結合すべきではない)
13. [直ったことをどう確かめたか](#13-直ったことをどう確かめたか)
14. [あなたのデータでも同じことが起きる](#14-あなたのデータでも同じことが起きる)
15. [チェックリスト](#15-チェックリスト)
16. [まとめ](#16-まとめ)
17. [付録A：コード全文](#付録aコード全文)
18. [付録B：よくある質問](#付録bよくある質問)

---

## 1. 前回までのあらすじ

- [罠01](01-silent-truncation.md)〜[罠03](03-year-parameter.md)：カタログと通信の問題
- [罠04](04-composite-key.md)〜[罠06](06-empty-rows.md)：数値の読み取りの問題
- [罠07](07-static-table-html.md)：静的表（HTML）の読み取り

ここまでで、**数値は正しく取れるようになりました。** 全国合計も公表値と一致します。

今回は、その数値を**地図と結びつける**段階の話です。

---

## 2. 症状：2020年から、地図に穴があく

16年分の人口地図を1枚ずつ描き出したところ、警告が出ました。

```
Warning messages:
1: 2020年: 値が結合できない州があります: Yogyakarta
2: 2021年: 値が結合できない州があります: Yogyakarta
3: 2022年: 値が結合できない州があります: Yogyakarta
...
```

そして描かれた地図を見ると、**ジャワ島の中央部が、2020年以降だけ灰色**になっていました。

2019年までは色がついています。2020年から灰色。**ある年を境に、1つの州だけが欠測になっている。**

### この症状の性質

ここで注目すべきは、**警告が出た**ことです。

これまでの罠（[罠01](01-silent-truncation.md)〜[罠06](06-empty-rows.md)）は、どれも黙って壊れました。今回は自分で書いた警告が鳴っています。

なぜ鳴らせたかというと、**地図を描く関数に「結合できなかった州を報告する」処理を入れておいた**からです。

```r
miss <- setdiff(GEOM$prov_name, d$prov_name)
if (length(miss) > 0) {
  warning(y, "年: 値が結合できない州があります: ",
          paste(miss, collapse = ", "))
}
```

**この5行が無ければ、灰色の地図をそのまま見過ごしていた可能性は高い。** 33州のうち1州が灰色でも、インドネシアの地図は島が多いので、目立ちません。

> **結合は、失敗しても止まらない。だから、失敗を自分で数える必要がある。**

---

## 3. なぜ地図が灰色になるのか

地図を描く処理は、こうなっています。

```r
GEOM |> left_join(d, by = "prov_name")
```

- `GEOM` … 州の形（境界データ）。33行
- `d` … その年の値。33行
- `by = "prov_name"` … **州の名前で突き合わせる**

`left_join` は「左（地図）の行を全部残し、右（値）から一致するものを持ってくる」という結合です。

一致しなければ、**値が `NA` になります。エラーは出ません。**

そして地図では、`NA` は灰色で描かれます（`na.value = "grey85"`）。

**つまり灰色は「値が無い」という意味ですが、その原因は2つあります。**

| 原因 | 意味 |
|---|---|
| 本当にデータが無い | 統計として存在しない |
| **名前が一致しなかった** | データはあるが、結びつかなかった |

**この2つは、地図の上ではまったく同じ見た目です。**

（前者の例が[罠05](05-derived-dimensions.md) の「ジャカルタに農村部が無い」で、こちらは構造的に正しい灰色でした。）

---

## 4. 犯人：同じ州に、2つの名前

原因を探すため、2019年と2020年で州名の一覧を比べました。

```r
setdiff(names_2020, names_2019)
setdiff(names_2019, names_2020)
```

出てきたのがこれです。

```
2019年まで : DI YOGYAKARTA
2020年から : DAERAH ISTIMEWA YOGYAKARTA
```

**同じ州です。**

「Daerah Istimewa Yogyakarta」は「ジョグジャカルタ特別州」という意味で、`DI` はその略称です。日本でいえば「東京都」と「東京」の関係に近い。

### なぜ年で変わったのか

これは BPS の気まぐれではありません。**表が違うから**です。

この作業では、人口を2つの表から取っていました。

| 期間 | 表 | 州名の表記 |
|---|---|---|
| 2015–2019年 | 動的表 1886 | `DI YOGYAKARTA` |
| 2020–2025年 | 動的表 2790 | `DAERAH ISTIMEWA YOGYAKARTA` |

2020年に人口推計の基準が変わった（SUPAS2015ベース → SP2020ベース）ため、**新しい系列は別の表**になっています。そして**表が違えば、表記も違いました**。

**どちらの表も正しい**のです。片方は略称、片方は正式名称を使っているだけです。

---

## 5. 表記ゆれのカタログ

Yogyakarta を直したあと、**同じ種類の問題を体系的に探しました**。結果、この作業だけで次のパターンに当たりました。

### パターン1：略称と正式名称

| 表記 | 正式 |
|---|---|
| `DI YOGYAKARTA` | `DAERAH ISTIMEWA YOGYAKARTA` |
| `DKI JAKARTA` | `DAERAH KHUSUS IBUKOTA JAKARTA` |
| `KEP. RIAU` | `KEPULAUAN RIAU` |
| `KEP. BANGKA BELITUNG` | `KEPULAUAN BANGKA BELITUNG` |

`KEP.` は `Kepulauan`（諸島）の略です。**点があったりなかったり**もします（`DKI JAKARTA` / `DKI. JAKARTA`）。

### パターン2：頭文字だけの略記

静的表1276（都市人口比率）では、こうなっていました。

```
N T B
N T T
```

**`Nusa Tenggara Barat` と `Nusa Tenggara Timur`** です。しかも**文字の間に空白が入っています**。

紙の表で列幅が足りないときの、ごく普通の処理です。人間なら文脈で分かります。

### パターン3：脚注記号が名前に付く

```
Kalimantan Utara 1)
```

`1)` は脚注番号です。表の下に「1) Data Still Joins East Kalimantan Province（このデータはまだ東カリマンタン州に含まれています）」と書いてあります。

**注記としては親切ですが、名前の一部になってしまっています。**

### パターン4：HTMLタグが混ざる

動的表の応答で、州の行がこう返ってくることがありました。

```html
<b>ACEH</b>
```

太字にするための指定です。人間がブラウザで見るぶんには問題ありませんが、**文字列としては `ACEH` と別物**です。

### パターン5：セル内の改行

[罠07](07-static-table-html.md) で触れたものです。

```
Sumatera
  Utara
```

---

**5パターン。しかもこれは、11指標を取っただけで遭遇した数です。**

指標を増やせば、まだ出てくるでしょう。**「全部の表記を列挙して対応する」という方針では、終わりがありません。**

---

## 6. 対処①：別名表をつくる

まず素直な対処です。「この表記が来たら、この名前にする」という対応表を作ります。

```r
prov_alias <- c(
  "DKI JAKARTA"                  = "Jakarta Raya",
  "DKI. JAKARTA"                 = "Jakarta Raya",
  "JAKARTA"                      = "Jakarta Raya",
  "DI YOGYAKARTA"                = "Yogyakarta",
  "D.I. YOGYAKARTA"              = "Yogyakarta",
  "DI. YOGYAKARTA"               = "Yogyakarta",
  "KEP. BANGKA BELITUNG"         = "Bangka Belitung",
  "KEPULAUAN BANGKA BELITUNG"    = "Bangka Belitung",
  "KEP. RIAU"                    = "Kepulauan Riau",
  "KEPULAUAN RIAU"               = "Kepulauan Riau",
  ...
)
```

**寄せ先を「GADM の表記」に統一**しました。GADM は世界の行政境界データの標準的な提供元で、この作業の地図データもそこから来ています。**地図側の表記に、統計側を合わせる**という方針です。

逆（統計側に地図を合わせる）でもよいのですが、統計は表が増えるたびに表記が増える一方、**地図は1つしかない**ので、地図を基準にするほうが安定します。

そして、比較の前に**大文字化と空白の正規化**を通します。

```r
key <- x |>
  str_squish() |>          # 前後と連続空白を整理（改行もここで潰れる）
  str_to_upper()
```

これで「大文字小文字の違い」「余分な空白」「セル内改行」は、別名表に載せる必要がなくなります。**正規化で吸収できるものは、表に載せない。** 表が短いほど、保守が楽になります。

---

## 7. 別名表だけでは、なぜ足りないのか

**別名表は、書いた分しか効きません。**

Yogyakarta の件では、`DI YOGYAKARTA` は表に載っていました。しかし `DAERAH ISTIMEWA YOGYAKARTA` は載っていませんでした。**新しい表を追加した瞬間に、新しい表記が現れたからです。**

つまり、

> **別名表は「過去に見た表記」の記録であって、「これから来る表記」には効かない。**

指標を1本追加するたびに、新しい表記が出る可能性があります。そのたびに地図が灰色になり、警告を読んで、表に1行足す。**この繰り返しは、いつまでも終わりません。**

もう少し強い仕組みが要ります。

---

## 8. 対処②：部分一致の保険

そこで、**表記が揺れやすい州に限って、キーワードでの判定**を足しました。

```r
# --- 部分一致による保険 ---------------------------------------------
# 完全一致の別名表だけだと、表ごとの表記ゆれを取りこぼす。
# 実例: var 1886 は "DI YOGYAKARTA"、var 2790 は
#       "DAERAH ISTIMEWA YOGYAKARTA" を返す。前者だけ別名表にあったため
#       2020-2025 の Yogyakarta が地図で灰色になった。
# 揺れの余地が大きい3州は、キーワードを含むかどうかで拾う。
out[grepl("YOGYAKARTA",      key, fixed = TRUE)] <- "Yogyakarta"
out[grepl("JAKARTA",         key, fixed = TRUE)] <- "Jakarta Raya"
out[grepl("BANGKA BELITUNG", key, fixed = TRUE)] <- "Bangka Belitung"
```

**「YOGYAKARTA という語を含むなら、それは Yogyakarta 州」**という判定です。

`DI YOGYAKARTA` でも `DAERAH ISTIMEWA YOGYAKARTA` でも `D.I. YOGYAKARTA` でも、**全部拾えます**。まだ見ぬ表記にも効きます。

### なぜ3州だけなのか

部分一致は強力ですが、**乱用すると誤爆します**。

たとえば `RIAU` で部分一致させると、`RIAU`（リアウ州）と `KEPULAUAN RIAU`（リアウ諸島州）の**両方に当たってしまいます**。これは別の州なので、致命的です。

そこで、次の条件を満たす州にだけ適用しました。

- **正式名称が長く、略され方が多様**（Yogyakarta、Jakarta）
- **他の州名と部分一致しない**（`YOGYAKARTA` を含む別の州は無い）

**強い道具ほど、適用範囲を絞る。** 部分一致を全州に広げれば「楽」になりますが、`Riau` と `Kepulauan Riau` のような組で必ず事故が起きます。

### 略記への対処

`N T B` のような頭文字略記は、部分一致では拾えません。別の処理を足しました。

```r
# 静的表は州名を略記することがある。実例: 1276 は
#   "N T B" / "N T T"（Nusa Tenggara Barat / Timur）
# 空白と点を取り除いた形で判定する。
k2 <- gsub("[ .]", "", key)
out[k2 %in% c("NTB", "NUSATENGGARABARAT")] <- "Nusa Tenggara Barat"
out[k2 %in% c("NTT", "NUSATENGGARATIMUR")] <- "Nusa Tenggara Timur"
```

**空白と点を全部取り除いてから比較する**ことで、`N T B` も `N.T.B.` も `NTB` も、同じものとして扱えます。

---

## 9. 対処③：冪等にする

もう1つ、地味ですが効いた設計があります。

> **`normalize_prov()` に、すでに正規化済みの名前を渡しても、同じ結果になるようにする。**

数学でいう**冪等**（べきとう、idempotent）です。`f(f(x)) = f(x)`。

```r
# 既に正しい名前を渡しても同じ結果になる(冪等)ので、
# 作成済みの CSV に後から掛け直しても安全。
```

### なぜこれが効くのか

Yogyakarta の問題が見つかった時点で、**CSVはすでに書き出し済み**でした。取得からやり直すと、通信が数十回発生します。

しかし冪等なら、**書き出したCSVを読んで、州名の列にもう一度 `normalize_prov()` を掛けるだけ**で直ります。

```r
# fix_yogyakarta.R（要約）
df <- read_csv("out/population_33_2010_2025.csv")
df$prov_name <- normalize_prov(df$prov_name)   # 掛け直すだけ
write_csv(df, "out/population_33_2010_2025.csv")
```

通信ゼロ、数秒で完了しました。

### 冪等にするための注意

冪等を壊しやすいのは、**「変換の結果」が「変換の入力」として別の意味を持つ場合**です。

たとえば `str_to_title()`（先頭だけ大文字にする）を素朴に適用すると、`Jakarta Raya` → `Jakarta Raya` で問題ありません。しかし、もし「大文字の名前だけ変換する」という条件を付けていたら、2回目は変換されず、結果が変わります。

この実装では、**入力を必ず大文字化してから判定する**ので、何回掛けても同じ経路を通ります。

> **後から掛け直せる関数は、作業のやり直しコストを桁で下げる。**

これは、**間違いが見つかることを前提にした設計**です。間違いは必ず見つかります。そのとき安く直せるかどうかが効いてきます。

---

## 10. 対処④：検査を足す ── 本命はここ

さて、ここからがこの記事の主題です。

**なぜ、この問題は検査をすり抜けたのか。**

これまでの記事で作った検査は、これでした（[罠05](05-derived-dimensions.md)）。

```r
check_panel <- function(df) {
  df |>
    group_by(year) |>
    summarise(n_prov = n_distinct(prov_name),
              n_rows = n(),
              total  = sum(value, na.rm = TRUE),
              n_na   = sum(is.na(value)),
              .groups = "drop")
}
```

Yogyakarta が壊れているときの出力を見てみます。

```
   year n_prov n_rows      total n_na
1  2019     33     33  268074600    0
2  2020     33     33  270203917    0
3  2021     33     33  272682600    0
```

**すべて正常です。**

- 州数は33 ✅
- 行数も33 ✅
- 合計も公表値と一致 ✅
- 欠測ゼロ ✅

**どこにも異常がありません。**

理由は単純で、**この検査は「年ごとに」数えているから**です。2019年に33州、2020年にも33州ある。それぞれの年の中では、何も間違っていません。

問題は**年をまたいだとき**にだけ現れます。2019年の33州と2020年の33州が、**同じ33州ではない**のです。

> **「各年で正しい」ことと、「年をまたいで整合している」ことは、別の性質である。**

これは、この連載でいちばん重要な発見の1つでした。

### 見落としの構造

[罠05](05-derived-dimensions.md) で「不変量を1つ決めれば、原因を知らなくても捕まえられる」と書きました。それは正しいのですが、**不変量が守っている範囲の外**は、当然守られません。

`check_panel()` が守っているのは「**1つの年の中の整合性**」です。年をまたいだ整合性は、別に検査しなければなりません。

**強い検査を1つ持っていると、「これで守れている」と感じてしまう。** そこが盲点でした。

### 足した検査

```r
#' 年をまたいだ州名の一貫性を調べる
#'
#' check_panel() は **年ごとに** 州数を数えるので、ある年だけ表記が違っても
#' 「どの年も33州」で通ってしまう。年をまたいで名前がそろっているかは別に見る。
#' 境界データと結合して初めて気づく、という事故を防ぐための関数。
check_names_across_years <- function(df) {
  tab <- table(df$prov_name, df$year) > 0
  n_years <- ncol(tab)
  partial <- rownames(tab)[rowSums(tab) < n_years]

  if (length(partial) == 0) {
    cat("[OK] 全州が全", n_years, "年に出現\n")
  } else {
    cat("[!] 一部の年にしか出ない州名(表記ゆれの疑い):\n")
    for (nm in partial) {
      yrs <- colnames(tab)[tab[nm, ]]
      cat("  ", nm, " → ", paste(range(as.integer(yrs)), collapse = "-"),
          " (", sum(tab[nm, ]), "年)\n", sep = "")
    }
  }
  invisible(partial)
}
```

やっていることは単純です。**州名 × 年のクロス表を作り、全年に現れない名前を報告する。**

壊れているデータに掛けると、こうなります。

```
[!] 一部の年にしか出ない州名(表記ゆれの疑い):
  Daerah Istimewa Yogyakarta → 2020-2025 (6年)
  Di Yogyakarta → 2010-2019 (10年)
```

**一目で分かります。** 同じ州が2つの名前で、期間が重なっていません。

### もう1つの検査：参照リストとの差分

さらに、**想定している州の一覧**と突き合わせる関数も作りました。

```r
#' 想定した州集合と実データの差分を出す(表記ゆれの発見用)
diff_prov <- function(df, reference) {
  list(
    "データにあって参照に無い" = setdiff(unique(df$prov_name), reference),
    "参照にあってデータに無い" = setdiff(reference, unique(df$prov_name))
  )
}
```

```r
diff_prov(pop33, PROV33)
```

```
$データにあって参照に無い
[1] "Daerah Istimewa Yogyakarta"

$参照にあってデータに無い
[1] "Yogyakarta"
```

**両方向を出す**のが肝心です。片方だけだと、「余計なものが入った」のか「あるべきものが無い」のかが分かりません。**両方出れば、対応関係が見えます。**

---

## 11. 脚注記号とHTMLタグ

残り2つのパターンにも対処しました。どちらも、**比較の前に落とす**という同じ方針です。

### 脚注記号

```r
# --- 脚注マーカーを落とす -------------------------------------------
# 静的表は州名に脚注番号を付けることがある。
#   実例: "Kalimantan Utara 1)" （1) は「東カリマンタンに含まれる」の注）
# これを落とさないと別の州として扱われ、畳み込みから漏れる。
key <- gsub("\\s*[0-9]+\\s*\\)\\s*$", "", key)   # 末尾の "1)" 等
key <- gsub("[*†‡]+$", "", key)          # * † ‡
key <- str_squish(key)
```

**末尾に限定**しているのが要点です（正規表現の `$`）。州名の途中にたまたま数字と括弧があっても壊しません。

`*` `†` `‡` も落としています。脚注記号として使われる文字は、番号だけではありません。

### HTMLタグ

こちらは、[罠07](07-static-table-html.md) のパーサ側で落としました。

```r
# ラベルには HTML タグが入ることがある(州行が "<b>ACEH</b>" のように太字指定)。
# 州名の突き合わせに使うので、タグと余分な空白を落としてから使う。
.bps_clean_label <- function(x) {
  x <- gsub("<[^>]*>", "", x)
  x <- gsub("&nbsp;", " ", x, fixed = TRUE)
  trimws(gsub("[[:space:]]+", " ", x))
}
```

**どこで落とすか**は、少し考えました。

- 名前の正規化（`normalize_prov`）で落とす
- 応答の解析（`bps_parse_data`）で落とす

後者を選びました。理由は、**HTMLタグは「州名の表記ゆれ」ではなく「応答の形式の問題」だから**です。州名以外のラベル（指標名、期間名）にも同じことが起こりえます。

> **問題は、それが発生する層で処理する。**

名前の正規化に何でも詰め込むと、その関数が「何をするものか」が曖昧になります。

---

## 12. そもそも名前で結合すべきではない

正直に書いておくと、**ここまでの対処は、すべて対症療法**です。

本来、地域データを結合するときは**名前ではなくコードを使う**べきです。

BPS には地域コードがあります（`1100` = Aceh、`3400` = Yogyakarta）。このコードは表をまたいでも変わりません。**コードで結合すれば、表記ゆれの問題は原理的に起きません。**

### なぜコードを使わなかったのか

**地図データ（GADM）に、BPSの地域コードが入っていなかったから**です。

GADM は世界共通の行政境界データで、国ごとの統計コードは持っていません。持っているのは名前（`NAME_1`）と、GADM独自のIDだけです。

したがって、

```
BPSの統計（BPSコードあり） ←→ GADMの地図（BPSコードなし）
```

**どこかで一度、名前による突き合わせが必要**でした。

### では、どうすべきだったか

理想は、**名前で突き合わせるのを1回だけにする**ことです。

1. 最初に一度だけ、名前で「BPSコード ↔ GADM名」の対応表を作る
2. **その対応表を、目視で確認して固定する**
3. 以降のすべての結合は、**コードで行う**

こうすれば、新しい表を追加しても、名前の揺れは影響しません。BPSコードは表をまたいで一定だからです。

今回の実装は、**毎回名前で突き合わせている**ので、表が増えるたびにリスクが増えます。11指標では持ちましたが、**これ以上増やすなら作り直すべき**でしょう。

（実際、県レベルへ拡張しようとしたとき、この問題が壁になりました。県は514もあるので、名前での突き合わせは現実的ではありません。BPSコードを持つ境界データを探すところからやり直す必要があります。）

> **対症療法で回っているうちに、根治の設計を用意しておく。**

この記事に書いた対処は、**11指標という規模だから成立している**ことを明記しておきます。

---

## 13. 直ったことをどう確かめたか

### 確認その1：年をまたいだ一貫性

```r
check_names_across_years(pop33)
```

```
[OK] 全州が全16年に出現
```

**33州すべてが、16年すべてに現れています。**

### 確認その2：参照リストとの差分

```r
diff_prov(pop33, PROV33)
```

```
$データにあって参照に無い
character(0)

$参照にあってデータに無い
character(0)
```

**両方向とも空。** 過不足ゼロです。

### 確認その3：地図に灰色が無いか

16枚を描き直し、警告が出ないことを確認しました。

```
（警告なし）
```

そして目視でも、**2020年以降のジャワ島中央部に色がついている**ことを確認しました。

**この目視は省けません。** 「警告が出ない」は「結合できた」ことしか保証しません。**間違った州に結合されていても、警告は出ない**からです（幸い、名前の対応は目で確認できる規模でした）。

### 確認その4：他の指標にも掛ける

Yogyakarta は人口で見つかりましたが、**他の10指標にも同じ問題があるかもしれません**。

全指標に `check_names_across_years()` を掛けました。結果、静的表から取った2指標（カロリー・タンパク質、都市化率）で、それぞれ別の表記ゆれが見つかりました（`Kalimantan Utara 1)` と `N T B` / `N T T`）。

> **1つ見つかったら、同じ検査を全部に掛ける。**

これは[罠03](03-year-parameter.md) の「同じ罠は似た場所に複数ある」と同じ作法です。

---

## 14. あなたのデータでも同じことが起きる

**名前で結合するところには、必ずこの問題があります。**

### 日本の例

| 表記ゆれ | 例 |
|---|---|
| 略称／正式 | `東京` / `東京都` |
| 旧字体 | `檜原村` / `桧原村` |
| 数字 | `四日市市` / `４日市市` |
| 全角半角 | `ABC株式会社` / `ＡＢＣ株式会社` |
| 法人格の位置 | `株式会社ABC` / `ABC株式会社` / `(株)ABC` |
| 市町村合併 | `さいたま市` ← 浦和市＋大宮市＋与野市 |
| 政令市の区 | `横浜市` / `横浜市西区` |

**e-Stat の市区町村コード**は、この問題を避けるために用意されています。コードで結合できるなら、必ずコードを使うべきです。

### 国際比較の例

| 表記ゆれ | 例 |
|---|---|
| 国名 | `Korea, Rep.` / `South Korea` / `KOR` / `410` |
| 国名の変更 | `Swaziland` → `Eswatini`（2018年） |
| 分裂 | `Sudan` → `Sudan` + `South Sudan`（2011年） |

**ISO 3166 のコード**を使うのが定石ですが、**時点によってコードの意味が変わる**ものもあります（→ [罠09](README.md)）。

### 共通する対処

1. **正規化**（大文字化、空白圧縮、記号除去）で吸収できるものは吸収する
2. **別名表**で既知のゆれに対応する
3. **既知の集合との照合**で、未知のゆれを検出する
4. **時点をまたいだ一貫性**を、別に検査する
5. できるなら、**名前ではなくコードで結合する**

---

## 15. チェックリスト

### 名前で結合する前に

- [ ] **コードで結合できないか**を先に検討したか
- [ ] 名前で結合するなら、**それは1回だけ**にできないか
- [ ] 突き合わせる両側の名前の集合を、目で見て比べたか

### 正規化するとき

- [ ] 大文字小文字、前後の空白、連続空白を吸収しているか
- [ ] **セル内改行**を潰しているか
- [ ] **脚注記号**（末尾の `1)`、`*`、`†`）を落としているか
- [ ] **HTMLタグ**が混ざっていないか
- [ ] その関数は**冪等**か（2回掛けても同じ結果か）

### 部分一致を使うとき

- [ ] **他の名前と部分一致しない**ことを確認したか（`Riau` と `Kepulauan Riau`）
- [ ] 適用範囲を、必要な対象だけに絞ったか

### 検査

- [ ] **年（時点）をまたいだ一貫性**を検査しているか
- [ ] 参照リストとの差分を、**両方向**で出しているか
- [ ] 結合できなかったものを**報告している**か
- [ ] 1つ見つけたら、**同じ検査を全指標に掛けた**か

---

## 16. まとめ

### 何が起きたか

2020年から、地図のジャワ島中央部が灰色になりました。原因は、人口の系列が2020年に別の表へ切り替わり、**同じ州の表記が `DI YOGYAKARTA` から `DAERAH ISTIMEWA YOGYAKARTA` に変わった**ことでした。

名前で結合していたため、一致せず、値が `NA` になっていました。

### なぜ検査をすり抜けたか

年ごとの検査（州数33、行数33、合計一致、欠測ゼロ）は、**すべて正常**でした。

> **「各年で正しい」ことと、「年をまたいで整合している」ことは、別の性質である。**

### どう直したか

1. 別名表に表記を追加（既知のゆれ）
2. **部分一致の保険**（未知のゆれにも効く。ただし3州に限定）
3. 略記・脚注記号・HTMLタグの除去
4. `normalize_prov()` を**冪等**にし、書き出し済みCSVに掛け直せるようにした
5. **`check_names_across_years()`**：年をまたいだ一貫性の検査（本命）
6. `diff_prov()`：参照リストとの差分を両方向で

### 一般化した教訓

> **強い検査を1つ持っていると、「これで守れている」と感じてしまう。**
> 検査が守っている範囲の外は、当然守られない。

> **後から掛け直せる関数（冪等）は、やり直しのコストを桁で下げる。**
> 間違いは必ず見つかる。安く直せるかどうかが効く。

> **対症療法で回っているうちに、根治の設計を用意しておく。**
> 名前での結合は、規模が小さいうちしか持たない。

### 生成AIとの協働という観点から

この罠には、協働ならではの側面がありました。

**Yogyakarta の問題を見つけたのは、人間です。**「地図の一部が灰色だ」と気づいたのは、描かれた絵を見た人間でした。AIが出力した警告文は画面に流れていましたが、**16枚の地図を生成する大量のログの中で、警告は埋もれていました**。

一方、**原因を特定し、5種類の表記ゆれを体系的に洗い出し、検査関数を書いたのはAI**です。「同じ問題が他の指標にもないか」と全指標に検査を掛ける作業も、AIのほうが速い。

> **異常に気づくのは絵を見た人間、原因を突き止めて再発を防ぐのはAI。**

この分担は、この作業を通じて何度も繰り返されました。そして重要なのは、**気づくためには絵が要る**ということです。数字の列だけを見ていたら、この罠は残ったままだったはずです。

**可視化は成果物であると同時に、検算の道具でもありました。**

---

## 付録A：コード全文

```r
# =====================================================================
# prov_crosswalk.R : 州名の正規化
# =====================================================================

suppressPackageStartupMessages({
  library(dplyr); library(tibble); library(stringr)
})

# ---------------------------------------------------------------------
# 別名表（寄せ先は GADM level 1 の表記）
# ---------------------------------------------------------------------
# 統計は表が増えるたびに表記が増えるが、地図は1つしかない。
# よって「地図側の表記に統計側を合わせる」方針にする。
# ---------------------------------------------------------------------
prov_alias <- c(
  "DKI JAKARTA"                  = "Jakarta Raya",
  "DKI. JAKARTA"                 = "Jakarta Raya",
  "JAKARTA"                      = "Jakarta Raya",
  "DI YOGYAKARTA"                = "Yogyakarta",
  "D.I. YOGYAKARTA"              = "Yogyakarta",
  "DI. YOGYAKARTA"               = "Yogyakarta",
  "KEP. BANGKA BELITUNG"         = "Bangka Belitung",
  "KEPULAUAN BANGKA BELITUNG"    = "Bangka Belitung",
  "KEP. RIAU"                    = "Kepulauan Riau",
  "KEPULAUAN RIAU"               = "Kepulauan Riau",
  "NUSA TENGGARA BARAT"          = "Nusa Tenggara Barat",
  "NUSA TENGGARA TIMUR"          = "Nusa Tenggara Timur",
  "PAPUA BARAT DAYA"             = "Papua Barat Daya",
  "PAPUA PEGUNUNGAN"             = "Papua Pegunungan",
  "PAPUA TENGAH"                 = "Papua Tengah",
  "PAPUA SELATAN"                = "Papua Selatan"
)

#' BPS 等の州名表記を GADM level 1 表記に正規化する
#'
#' @note **冪等**。既に正規化済みの名前を渡しても同じ結果になるので、
#'   書き出し済みの CSV に後から掛け直しても安全。
normalize_prov <- function(x) {
  key <- x |>
    str_squish() |>          # 前後と連続空白を整理（改行もここで潰れる）
    str_to_upper()

  # --- 脚注マーカーを落とす -------------------------------------------
  # 実例: "Kalimantan Utara 1)"（1) は「東カリマンタンに含まれる」の注）
  # 落とさないと別の州として扱われ、畳み込みから漏れる。
  # 末尾に限定する（$）。名前の途中の数字は壊さない。
  key <- gsub("\\s*[0-9]+\\s*\\)\\s*$", "", key)   # 末尾の "1)" 等
  key <- gsub("[*†‡]+$", "", key)          # * † ‡
  key <- str_squish(key)

  hit <- match(key, names(prov_alias))
  out <- ifelse(!is.na(hit), unname(prov_alias[hit]), str_to_title(key))

  # --- 部分一致による保険 ---------------------------------------------
  # 完全一致の別名表だけだと、表ごとの表記ゆれを取りこぼす。
  # 実例: var 1886 は "DI YOGYAKARTA"、var 2790 は
  #       "DAERAH ISTIMEWA YOGYAKARTA" を返す。前者だけ別名表にあったため
  #       2020-2025 の Yogyakarta が地図で灰色になった。
  #
  # ★他の州名と部分一致しない3州にだけ適用する。
  #   "RIAU" で部分一致させると Kepulauan Riau にも当たるので不可。
  out[grepl("YOGYAKARTA",      key, fixed = TRUE)] <- "Yogyakarta"
  out[grepl("JAKARTA",         key, fixed = TRUE)] <- "Jakarta Raya"
  out[grepl("BANGKA BELITUNG", key, fixed = TRUE)] <- "Bangka Belitung"

  # 静的表は州名を略記することがある。実例: 1276 は
  #   "N T B" / "N T T"（Nusa Tenggara Barat / Timur）
  # 空白と点を取り除いた形で判定する。
  k2 <- gsub("[ .]", "", key)
  out[k2 %in% c("NTB", "NUSATENGGARABARAT")] <- "Nusa Tenggara Barat"
  out[k2 %in% c("NTT", "NUSATENGGARATIMUR")] <- "Nusa Tenggara Timur"

  out
}

# 全国行の判定（CSV に混ぜないためのフィルタ用）
is_national <- function(x) {
  str_squish(str_to_upper(x)) %in% c("INDONESIA", "NATIONAL", "TOTAL")
}

# ---------------------------------------------------------------------
# 検査
# ---------------------------------------------------------------------

#' 年をまたいだ州名の一貫性を調べる
#'
#' check_panel() は **年ごとに** 州数を数えるので、ある年だけ表記が違っても
#' 「どの年も33州」で通ってしまう。年をまたいで名前がそろっているかは別に見る。
#' 境界データと結合して初めて気づく、という事故を防ぐための関数。
check_names_across_years <- function(df) {
  tab <- table(df$prov_name, df$year) > 0
  n_years <- ncol(tab)
  partial <- rownames(tab)[rowSums(tab) < n_years]

  if (length(partial) == 0) {
    cat("[OK] 全州が全", n_years, "年に出現\n")
  } else {
    cat("[!] 一部の年にしか出ない州名(表記ゆれの疑い):\n")
    for (nm in partial) {
      yrs <- colnames(tab)[tab[nm, ]]
      cat("  ", nm, " → ", paste(range(as.integer(yrs)), collapse = "-"),
          " (", sum(tab[nm, ]), "年)\n", sep = "")
    }
  }
  invisible(partial)
}

#' 想定した州集合と実データの差分を出す(表記ゆれの発見用)
#' @note **両方向を出す**。片方だけだと対応関係が見えない。
diff_prov <- function(df, reference) {
  list(
    "データにあって参照に無い" = setdiff(unique(df$prov_name), reference),
    "参照にあってデータに無い" = setdiff(reference, unique(df$prov_name))
  )
}
```

### 応答のラベルからHTMLタグを落とす（罠07のパーサ側）

```r
# ラベルには HTML タグが入ることがある(州行が "<b>ACEH</b>" のように太字指定)。
# 州名の突き合わせに使うので、タグと余分な空白を落としてから使う。
# ★ここで落とすのは、これが「名前の表記ゆれ」ではなく
#   「応答の形式の問題」だから。指標名や期間名にも同じことが起きる。
.bps_clean_label <- function(x) {
  x <- gsub("<[^>]*>", "", x)
  x <- gsub("&nbsp;", " ", x, fixed = TRUE)
  trimws(gsub("[[:space:]]+", " ", x))
}
```

### 書き出し済みCSVを直す（冪等性の活用）

```r
# fix_yogyakarta.R
# normalize_prov() は冪等なので、取得し直さずに掛け直すだけで直る。
library(readr); library(dplyr)
source("R/prov_crosswalk.R")

for (f in list.files("out", pattern = "\\.csv$", full.names = TRUE)) {
  df <- read_csv(f, show_col_types = FALSE)
  if (!"prov_name" %in% names(df)) next
  before <- length(unique(df$prov_name))
  df$prov_name <- normalize_prov(df$prov_name)
  after <- length(unique(df$prov_name))
  if (before != after) {
    cat(basename(f), ": 州名", before, "→", after, "\n")
    write_csv(df, f)
  }
  check_names_across_years(df)
}
```

---

## 付録B：よくある質問

### Q1. 部分一致を全州に適用してはいけませんか？

**危険です。** `Riau` と `Kepulauan Riau`（リアウ州とリアウ諸島州）のように、**片方が他方の部分文字列になっている**組があるからです。

適用するなら、**長い名前から順に判定して、当たったら抜ける**という書き方が必要です。ただし、それでも新しい州が追加されたときに順序が崩れる危険があります。

**3州に絞る**という判断は、「強い道具は範囲を絞って使う」という考え方によるものです。

### Q2. `check_names_across_years()` は、正当な理由で名前が現れない場合も警告しますか？

**します。** たとえば[罠09](README.md) で扱う「2012年に新設された州」は、2010・2011年には存在しません。この関数は、それも「一部の年にしか出ない」として報告します。

これは**わざとそうしています**。表記ゆれと新設州は、出力の見た目が同じだからです。**どちらであるかは、人間が判断すべきこと**です。

警告が出たら、次のどちらかを確認します。

- 同じ州の別表記が、期間を分けて存在する → **表記ゆれ**
- その名前が、ある年から現れる（対になる名前が無い） → **新設州**

### Q3. 冪等でない正規化関数の例は？

「まだ変換していないものだけ変換する」という条件を持つものです。たとえば、

```r
# 冪等でない例
if (grepl("^[A-Z ]+$", x)) x <- str_to_title(x)   # 大文字のときだけ変換
```

1回目は変換され、2回目は条件に合わず変換されません。**入力によって経路が変わる**ので、`f(f(x)) ≠ f(x)` になりえます。

**入力を必ず同じ形（今回は大文字）に揃えてから処理する**と、冪等になります。

### Q4. 他の罠も知りたいのですが

この記事は連載の8本目です。

1. [一覧APIの暗黙の打ち切り](01-silent-truncation.md)
2. [カタログの二言語混在](02-bilingual-catalog.md)
3. [年の必須指定と3年上限](03-year-parameter.md)
4. [連結キーの桁数不定](04-composite-key.md)
5. [派生変数・期間の未指定](05-derived-dimensions.md)
6. [行は存在するが値が空](06-empty-rows.md)
7. [静的表の実体がHTML](07-static-table-html.md)
8. **州名の表記ゆれと脚注記号**（この記事）
9. 行政区画の変更（33→34→38州） ── 足せる量と足せない量
10. 公表基準の断絶（3月調査と9月調査） ── 検算が失敗したとき、何を疑うか

---

## この記事について

この記事は、日本計算機統計学会シンポジウムでの報告「検算駆動による生成AIとの協働 ── インドネシア公的統計WebAPIからの州別パネル構築とShinyによる可視化」の補足資料として作成しました。

本文中のコード、出力、数値は、すべて実際の作業記録に基づいています。うまくいかなかった過程も、そのまま残しています。

### 生成AIの利用について

**本記事の文章は、生成AI（Claude Opus 5、推論設定「高」）が、久保田による指示（プロンプト）と複数回のやりとりを経て生成したものです。** 構成・記述・コード解説はAIによる出力であり、掲載しているコード、コンソール出力、数値は、いずれも実際の作業記録に基づいています。内容は著者が確認したうえで公開しています。

この記事の主題そのものが「生成AIと協働してデータを取得した記録」であるため、記事の生成過程を隠す理由がありません。念のため明記しておきます。

- 著者：久保田貴文（多摩大学）
- 連絡先：kubota@tama.ac.jp
- Web：https://takafumikubota.jp/

この作業で用いた統計は、インドネシア中央統計庁（BPS）が公開するWebAPIから取得しました。
Layanan ini menggunakan API Badan Pusat Statistik (BPS).
（本サービスはインドネシア中央統計庁のAPIを使用しています。）
