# 罠05：派生変数・期間を指定しないと、全国合計が2倍になる

> **一行でいうと**
> 統計表は「1枚の表」ではなく、**いくつもの切り口が畳み込まれた立体**である。
> どの切り口を使うか言わずに足すと、**同じ人を何回も数える**。

**この罠の危険度：★★★★★（最高）**
理由は3つあります。エラーが出ません。行数も「なんとなく多いな」で済んでしまいます。そして何より、**倍になった数字は、それらしく見えます**。桁が変わるわけではないので、目視では気づけません。

---

## 目次

1. [前回までのあらすじ](#1-前回までのあらすじ)
2. [予備知識：統計表は「1枚の表」ではない](#2-予備知識統計表は1枚の表ではない)
3. [症状：全国合計が、公表値のちょうど2倍になった](#3-症状全国合計が公表値のちょうど2倍になった)
4. [なぜ2倍なのか](#4-なぜ2倍なのか)
5. [もう1つの次元：期間](#5-もう1つの次元期間)
6. [対処①：使う切り口を、明示的に選ぶ](#6-対処使う切り口を明示的に選ぶ)
7. [対処②：不変量で守る（本命）](#7-対処不変量で守る本命)
8. [表ごとに事情が違う ── 3つの実例](#8-表ごとに事情が違う--3つの実例)
9. [「どれを選ぶか」は、機械には決められない](#9-どれを選ぶかは機械には決められない)
10. [直ったことをどう確かめたか](#10-直ったことをどう確かめたか)
11. [あなたのAPIでも同じことが起きる](#11-あなたのapiでも同じことが起きる)
12. [チェックリスト](#12-チェックリスト)
13. [まとめ](#13-まとめ)
14. [付録A：コード全文](#付録aコード全文)
15. [付録B：よくある質問](#付録bよくある質問)

---

## 1. 前回までのあらすじ

- [罠01](01-silent-truncation.md)：カタログが500件で静かに切れていた
- [罠02](02-bilingual-catalog.md)：英語で絞ると、インドネシア語の表題が消えた
- [罠03](03-year-parameter.md)：エラーメッセージから、年の必須指定と3年上限が判明した
- [罠04](04-composite-key.md)：キーを切り分けず、組み立てる設計にした

ここまでで、**数値が正しく取れるようになりました。** キーの組み立ては原理的に正しく、値も NA ではなく入っています。

つまり、**手元には「正しい数値の集まり」があります。**

今回の罠は、その先です。**正しい数値を、間違った足し方をする**話です。

---

## 2. 予備知識：統計表は「1枚の表」ではない

### 紙の統計表を思い浮かべてください

政府の統計書を開くと、こういう表が載っています。

| 州 | 男 | 女 | 計 |
|---|---|---|---|
| アチェ | 2,632,694 | 2,648,620 | 5,281,314 |
| 北スマトラ | 7,196,123 | 7,219,268 | 14,415,391 |
| … | | | |

人間はこれを見て、迷いません。「計」の列を読めばいい、と分かります。**「男」と「女」と「計」を全部足したら2倍になる**ことは、説明されなくても分かります。

### ところがAPIは、全部を平らに並べて返す

BPS の API は、この表を**縦一列に**して返します。

| 州 | 派生変数 | 値 |
|---|---|---|
| アチェ | Male | 2,632,694 |
| アチェ | Female | 2,648,620 |
| **アチェ** | **Total** | **5,281,314** |
| 北スマトラ | Male | 7,196,123 |
| … | | |

こうなると、**「計」が特別扱いされません**。ただの1行として、他の行と同じ顔で並んでいます。

そして、何も考えずに `sum(value)` とやると、

```
男 + 女 + 計 = 計の2倍
```

**同じ人を2回数えます。**

### BPSでの呼び方

BPS の API では、この「切り口」を **`turvar`（派生変数）** と呼びます。表によって中身が違います。

| 表 | `turvar` の中身 |
|---|---|
| 人口 | Male / Female / **Total** |
| 貧困線 | Urban Area / Rural Area |
| 一人当たりGRDP | Current Market Price / 2010 Constant Market Price |
| 平均就学年数 | （None＝切り口なし） |

**表によって、切り口の意味がまったく違います。** 性別のこともあれば、都市／農村のこともあり、価格の基準のこともあります。

そして、もう1つ **`turtahun`（期間）** という切り口もあります。こちらは後述します（§5）。

つまり、1つの統計表は

```
地域 × 派生変数 × 年 × 期間
```

という**4次元の立体**です。ここから2次元の表を取り出すには、**残り2つの次元で「どれを使うか」を決めなければなりません。**

---

## 3. 症状：全国合計が、公表値のちょうど2倍になった

最初にこの罠に当たったのは、人口を取ったときでした。

州別の人口を合計して、公表の全国人口と比べます。これは[罠01](01-silent-truncation.md)以来ずっと使っている検算です。

```
こちらの合計 : 540,407,834
公表の全国値 : 270,203,917
```

**ちょうど2倍です。**

「2倍」という数字は、非常にありがたい手がかりでした。もし1.3倍とか0.9倍なら、原因の候補がたくさんあります（欠測、単位、範囲の違い…）。しかし**ちょうど2倍**は、原因を強く絞ります。

> **同じものを、2回数えている。**

そして、`turvar` の中身を見れば、答えは明らかでした。

```
Male + Female + Total = Total × 2
```

男と女を足すと計になる。そこに計も足しているので、ちょうど2倍。**算数のとおりです。**

### この罠の見え方

もし検算をしていなかったら、どうなっていたでしょうか。

- エラーは出ません
- 行数は 38州 × 3（Male/Female/Total）= 114行。「州が38だから、まあこんなものか」と見えます
- 欠測もありません
- 地図を描いても、**色の相対関係は変わりません**（全州が一律2倍なので）

**地図はまったく正常に見えます。** 2倍になっていることは、**外部の数字と突き合わせない限り、絶対に分かりません。**

> **相対値しか見ない可視化は、倍率の誤りを隠す。**

これは地図に限りません。相関係数も、順位も、比率も、一律の倍率では変わりません。**「絵が正しく見えること」は、数字が正しいことの証拠になりません。**

---

## 4. なぜ2倍なのか

もう少し丁寧に見ます。仮に3州だけの例で考えます。

| 州 | Male | Female | Total |
|---|---|---|---|
| A | 100 | 110 | 210 |
| B | 200 | 190 | 390 |
| C | 150 | 160 | 310 |

正しい全国計は `210 + 390 + 310 = 910` です。

しかし、全行を足すと

```
(100+110+210) + (200+190+390) + (150+160+310) = 1,820
```

**910 の2倍。** どの州でも `Male + Female = Total` が成り立つので、必ずぴったり2倍になります。

### 倍率が「ちょうど」でない場合もある

ここで注意が要ります。**いつも2倍とは限りません。**

- 切り口が3つ（都市／農村／計）なら、やはり2倍
- 切り口が4つ（都市男／都市女／農村男／農村女／計…）なら、組み合わせによって倍率が変わる
- **一部の州だけ「計」が欠けていたら、半端な倍率**になる

半端な倍率のほうが厄介です。「2倍」なら気づきますが、「1.87倍」だと「欠測のせいかな」と誤診しやすい。

> **きれいな倍率で外れたときは、むしろ運がいい。**

---

## 5. もう1つの次元：期間

派生変数（`turvar`）を直したあと、**同じことが別の次元で起きました。**

貧困率を取ったときです。全国の貧困率が、公表値のおよそ2倍になりました。

`turvar` は指定済みです。では何が重複しているのか。**`turtahun`（期間）**でした。

### インドネシアの貧困統計は、年2回公表される

BPS は貧困統計を、**3月と9月の年2回**公表しています。

| 期間ラベル | 意味 |
|---|---|
| `Semester 1 (March)` | 3月調査 |
| `Semester 2 (September)` | 9月調査 |
| `Annually` | 年次（年1回だった時代の系列） |

つまり、1つの州の1つの年に、**行が2つ（あるいは3つ）ある**わけです。

| 州 | 年 | 期間 | 貧困率 |
|---|---|---|---|
| アチェ | 2018 | Semester 1 (March) | 15.97 |
| アチェ | 2018 | Semester 2 (September) | 15.68 |

これを両方使ってしまうと、州の数が2倍に見え、合計も2倍になります。

### 表によってラベルが違う

さらに厄介なことに、**期間のラベルは表ごとに違います。**

| 表 | `turtahun` の中身 |
|---|---|
| 貧困率・貧困線（州別） | Annually / Semester 1 (March) / Semester 2 (September) |
| 失業率 | **February / August / Yearly** |
| 県別貧困線 | **Annual**（1種類だけ） |
| 人口 | Annual（1種類だけ） |

失業率は「3月・9月」ではなく「**2月・8月**」です。労働力調査（Sakernas）の実施時期が違うためです。

**表ごとに違う** ということは、「`Semester 1 (March)` を選ぶ」という設定を使い回せない、ということです。表ごとに確認が要ります。

---

## 6. 対処①：使う切り口を、明示的に選ぶ

まず素直な対処です。関数に「どれを使うか」を渡せるようにしました。

```r
to_stats_rows <- function(df, indicator,
                          turvar_pick = "Total",      # 派生変数
                          unit_mult = 1,
                          turtahun_pick = NULL) {     # 期間
  x <- bps_only_province(df)

  # 派生変数を選ぶ
  if (!is.null(turvar_pick) && any(nzchar(x$turvar_label))) {
    x <- filter(x, turvar_label == turvar_pick)
  }
  ...
}
```

そして、**指定し忘れたときに黙って進まない**ようにしました。

```r
labs <- unique(x$turtahun_label[nzchar(x$turtahun_label)])
if (length(labs) > 1) {
  if (is.null(turtahun_pick)) {
    stop("この表は期間が複数あります。turtahun_pick を指定してください:\n  ",
         paste(labs, collapse = "\n  "),
         "\n(年2回公表の指標では必須。指定しないと合計が倍になります)")
  }
  ...
}
```

**エラーメッセージに、選べる候補と、指定しなかったらどうなるかを書いてあります。** 「指定してください」だけでは、次に何をすればいいか分かりません。候補が出ていれば、その場で選べます。

> **止めるときは、次の一手が分かる形で止める。**

### 既定値の置き方

`turvar_pick = "Total"` を既定にしました。多くの表で「Total」が正解だからです。

ただし、これは**危うい親切**でもあります。既定があると、指定を忘れても動いてしまう。実際、後述する貧困線の表（§8）では、この既定のせいで一度失敗しました。

一方 `turtahun_pick` は既定を `NULL`（未指定）にし、**候補が複数あれば必ず止まる**ようにしました。片方は既定あり、片方は既定なし。**非対称ですが、意図的です。**

- `turvar` は「Total を選ぶ」が圧倒的に多く、既定があると作業が速い
- `turtahun` は表ごとにラベルが違い、**どれが正しいかは研究上の判断**（§9）なので、機械が勝手に決めるべきではない

---

## 7. 対処②：不変量で守る（本命）

さて、ここからが本題です。

**「選べるようにした」だけでは、まだ足りません。** なぜなら、

- 新しい表を追加するとき、また指定を忘れるかもしれない
- 指定したラベルが、その表には存在しないかもしれない（誤字、表記違い）
- 将来 BPS が新しい切り口を追加するかもしれない

**個別の対処は、個別にしか効きません。** 必要なのは、**どんな表でも通用する検査**です。

### 不変量を1つ決める

そこで、次の1文を決めました。

> **1つの州 × 1つの年 に対して、行はちょうど1つ。**

これは、この作業で作るパネルデータの**定義そのもの**です。切り口が何であれ、期間が何であれ、最終的な出力が「州×年のパネル」である以上、**これが破れていたら必ず間違い**です。

コードにするとこうなります。

```r
# --- 重複ガード ------------------------------------------------------
dup <- out |> count(prov_name, year) |> filter(n > 1)
if (nrow(dup) > 0) {
  cat("[!] 1州×1年に複数行が残っています(先頭10件):\n")
  print(as.data.frame(head(dup, 10)))
  stop("重複があります。turvar_pick / turtahun_pick を見直してください。")
}
```

たった5行です。しかし、これがあれば

- 派生変数の指定忘れ → 止まる
- 期間の指定忘れ → 止まる
- **まだ知らない第3の切り口** → **止まる**

最後が重要です。**原因を知らなくても捕まえられる**のが、不変量の強みです。

### 「症状を治す」と「病気を捕まえる」の違い

この2つの対処の性格の違いを、はっきりさせておきます。

| | 対処①（明示的に選ぶ） | 対処②（不変量で守る） |
|---|---|---|
| 何をするか | 既知の切り口を指定する | 結果の形を検査する |
| 効く範囲 | **指定した切り口だけ** | **すべての原因** |
| 未知の問題に | 効かない | **効く** |
| 書く量 | 表ごとに増える | **1回書けば終わり** |

対処①は必要です。しかし、**対処①だけでは、知らない罠は防げません。**

> **「原因を1つずつ潰す」より、「結果が満たすべき性質」を1つ決めるほうが、守備範囲が広い。**

### 検査を出力にも入れる

同じ考え方で、パネルの状態を出す関数にも列を1つ足しました。

```r
check_panel <- function(df) {
  df |>
    group_by(year) |>
    summarise(
      n_prov  = n_distinct(prov_name),
      n_rows  = n(),                       # n_prov と違えば重複がある
      total   = sum(value, na.rm = TRUE),
      n_na    = sum(is.na(value)),
      .groups = "drop"
    ) |>
    arrange(year)
}
```

`n_prov`（州の数）と `n_rows`（行の数）を**並べて出す**。この2つが違えば重複です。**目で見て一瞬で分かります。**

```
   year n_prov n_rows    total n_na
1  2018     34     34 14684181    0     ← 正常
2  2019     34     68 31286128    0     ← 重複あり
```

もともとは `n_prov` しか出していませんでした。それだと「34州ある、正常だ」と読めてしまいます。**1列足しただけで、見えるものが変わりました。**

---

## 8. 表ごとに事情が違う ── 3つの実例

「Total を選べばいい」で済まない例に、実際に3つ当たりました。

### 実例1：貧困線には「Total」が存在しない

貧困線の表（変数195）に `turvar_pick = "Total"` を指定したところ、**行がゼロになりました。**

調べると、この表の `turvar` は

```
Urban Area / Rural Area
```

**の2つだけ**でした。「Total」がありません。

つまり BPS は、州別の貧困線を「都市部」と「農村部」に分けてしか公表していない、ということです（[罠02](02-bilingual-catalog.md) で見つけた県別の表には合計がありました）。

**対処：合成しない。2本の別々の指標として扱う。**

なぜ合成しないか。都市部と農村部を合わせた値を作るには、**州別の都市／農村人口比**が必要ですが、それを持っていませんでした。無理に作れば、その比の推定誤差が全部入り込みます。

そして考えてみれば、**都市と農村の差そのものが情報**です。1本に潰す必要はありません。

```r
spec <- tibble::tribble(
  ~var_id, ~name,                 ~turvar_pick,   ~turtahun_pick,
     195,  "poverty_line_urban",  "Urban Area",   "Semester 1 (March) > Annually",
     195,  "poverty_line_rural",  "Rural Area",   "Semester 1 (March) > Annually",
)
```

**同じ表から、2本の指標を取り出しています。** 「1つの表＝1つの指標」という思い込みも、捨てる必要がありました。

### 実例2：GRDPは「名目か実質か」を選ぶ

一人当たりGRDP（変数288）の `turvar` は、こうでした。

```
Current Market Price / 2010 Constant Market Price
```

これは性別でも地域でもなく、**価格の基準**です。

- **Current（名目）**：その年の値段でいくらか
- **2010 Constant（実質）**：2010年の値段に換算するといくらか

どちらを選ぶかは、**技術的な問題ではなく、分析上の判断**です。

この作業では **Constant（実質）** を選びました。理由は、**16年分を並べて比較するから**です。名目のままだと、経済成長と物価上昇が混ざります。

コードには理由を書き残しました。

```r
# var 288 (GRDP): Current / 2010 Constant。
#   **年をまたいで比べるので Constant（実質）** を採る。
#   単位は Thousand Rupiahs なので ×1000 でルピアにする。
```

**「なぜそれを選んだか」を書かないと、後から見た人（と自分）は、選択があったことにすら気づけません。**

### 実例3：失業率は「2月か8月か」を選ぶ

失業率（変数543）の `turtahun` は、

```
February / August / Yearly
```

BPS の主系列は **8月** です。しかし、この作業では **2月** を選びました。

理由は、**分析に使う家計調査（SUSENAS）が3月調査だから**です。失業率を説明変数に使うなら、時点が近いほうが筋が通ります。

```r
# var 543 (失業率): February / August / Yearly。
#   SUSENAS が3月調査なので、時点の近い **February** に揃える。
#   （BPS の主系列は August。年次比較を重視するなら Yearly も可）
```

**主系列と違う選択をしている**ので、そのことも明記しました。論文で「失業率」と書くときは、2月時点であることを注記する必要があります。

---

## 9. 「どれを選ぶか」は、機械には決められない

3つの実例に共通することがあります。

> **どの切り口を使うかは、データの性質ではなく、研究の目的で決まる。**

- 貧困線を1本にまとめるか、2本のままにするか → **都市農村差を論じたいかどうか**
- GRDPを名目にするか実質にするか → **年をまたいで比べるかどうか**
- 失業率を2月にするか8月にするか → **どの調査と時点を揃えたいか**

どれも、**データを見ても答えは出ません。** 何をしたいかを知っている人にしか決められません。

### 生成AIとの協働という観点から

この作業は生成AIとの協働で行いましたが、**ここは人間が決めた部分**です。

AIができたのは、

- 表にどんな切り口があるかを調べて並べる
- 選んだ結果を必ず表示する
- 選ばれていなければ止める

つまり、**選択肢を漏れなく机の上に出し、選ばなかったことを許さない**ところまでです。

作業スクリプトには、その方針をそのまま書きました。

```r
# 【自動で決めることと、決めないこと】
# turvar / turtahun / 単位換算は「1つしか無ければそれ、複数なら優先順位」で
# 自動選択するが、**選んだ結果は必ず表示する**。
# 決められない場合はその指標だけスキップして報告する（黙って進まない）。
```

「1つしか無ければそれを使う」は自動化していい部分です。選択の余地がないからです。しかし**複数あるときは、必ず人間の指定を要求する**。

> **選択肢が1つなら機械が決めてよい。2つ以上あるなら、それは判断であって、判断は人間の仕事。**

これは、この連載を通じての分業の原則になりました。AIの側は「決めない」ことを積極的に実装する。止まるコードを書く。それが親切だと考えました。

### 補足：年によって公表形態が変わる場合

もう1つ、実データで踏んだ細かい問題があります。

貧困率は、**2012年より前は年1回（Annually）、それ以降は3月・9月の2回**でした。つまり「`Semester 1 (March)` を使う」と決めても、2010年・2011年には存在しません。

そこで `turtahun_pick` を**優先順位のリスト**として受け取れるようにしました。

```r
# turtahun_pick は **優先順位のベクトル** を受け取る。
# 年によって公表形態が変わる表があるため（例: 貧困率は 2010-2011 が
# 年1回、2012年以降が3月・9月の2回）、
#   c("Semester 1 (March)", "Annually")
# のように書くと、年ごとに「使える中でいちばん優先度の高いもの」を選ぶ。
# どの年にどれを使ったかは必ず表示する（黙って混ぜないため）。
chosen <- x |>
  distinct(year, turtahun_label) |>
  group_by(year) |>
  summarise(pick = {
    p <- turtahun_pick[turtahun_pick %in% turtahun_label]
    if (length(p)) p[1] else NA_character_
  }, .groups = "drop")
```

そして、**年によって違う期間を使った場合は、必ず表に出します。**

```r
if (length(unique(chosen$pick[!is.na(chosen$pick)])) > 1) {
  cat("[i] 年によって使った期間が違います:\n")
  print(as.data.frame(chosen))
}
```

系列の中に**異なる基準が混ざる**のは、それ自体が分析上の問題です（この問題を正面から扱うのが[罠10](README.md)です）。混ぜること自体は許すが、**混ぜたことを隠さない。**

---

## 10. 直ったことをどう確かめたか

### 確認その1：不変量を通るか

```
   year n_prov n_rows    total n_na
1  2010     33     33 237641326    0
2  2011     33     33 241990661    0
...
16 2025     33     33 284438782    0
```

**`n_prov` と `n_rows` が全年で一致。** 重複はありません。

### 確認その2：合計が公表値と一致するか

これが本命です。

| 年 | こちらの合計 | 公表値 | 一致 |
|---|---|---|---|
| 2010 | 237,641,326 | 237,641,326 | **完全一致** |
| 2020 | 270,203,917 | 270,203,917 | **完全一致** |

**1人単位で一致しました。**

（この一致に至るまでに、もう1つ別の罠 ── 新設州の二重計上 ── を越える必要がありました。[罠09](README.md) で扱います。）

### 確認その3：選んだ切り口が表示されるか

取得のたびに、何を選んだかが出るようにしました。

```
--- unemployment_rate ---
  turvar  : None
  turtahun: February / August / Yearly
  → 選択: February
```

**選んだ結果が画面に出る**ことが重要です。指定したつもりで指定できていない（ラベルの表記が違う、など）ときに、その場で気づけます。

### 確認その4：全国加重平均が公表値の近くにあるか

率や平均は単純合計できないので、人口で加重した全国値と比べます。

| 指標 | 年 | こちら | BPS公表 |
|---|---|---|---|
| 平均就学年数 | 2018 | 8.170 | 8.17 |
| 失業率（2月） | 2018 | 5.20 | 5.13 |
| 都市人口比率 | 2010 | 49.81% | 49.8%（SP2010） |

**ほぼ一致。** 失業率の 5.20 と 5.13 のわずかな差は、加重に使った人口の年次が微妙にずれることによるものと考えられます。この程度の差なら、**切り口の選択は正しい**と言えます。

もし切り口を間違えていれば、こんな小さな差では済みません。**「近い」ことが、選択が正しいことの証拠になります。**

---

## 11. あなたのAPIでも同じことが起きる

「小計と合計が同じ列に並んでいる」問題は、**統計データの世界では普遍的**です。

### よくある形

| データ | 混ざっているもの |
|---|---|
| 人口統計 | 男 / 女 / **計** |
| 経済統計 | 産業別 / **全産業** |
| 地域統計 | 各都道府県 / **全国** |
| 貿易統計 | 品目別 / **総額** |
| 家計調査 | 費目別 / **消費支出計** |
| 会計データ | 勘定科目 / **小計** / **合計** |

**「全国」行が混ざっている**問題は特に多く、この作業でも専用の除外関数を書きました。

```r
#' 州行だけ残す(全国行・県行を落とす)
bps_only_province <- function(df) {
  df |>
    filter(grepl("^[0-9]{4}$", vervar), substr(vervar, 3, 4) == "00") |>
    filter(!grepl("^INDONESIA$", trimws(region), ignore.case = TRUE))
}
```

**地域コードの規則（4桁・末尾00が州）と、名前による除外の両方**を使っています。片方だけだと漏れることがあるからです。

### e-Stat（日本の政府統計）の場合

日本の統計でも同じです。e-Stat のデータには、たいてい

- `cat01` に「総数」という項目が含まれる
- 地域コードに「全国」「〇〇県計」が含まれる
- 時間軸に「年度計」と「四半期」が混在する

そのまま集計すると、同じように倍になります。**「総数」という名前の行を除外する**のが定石ですが、**表によって名前が違う**（「総数」「計」「合計」「Total」）ので、名前で除くのは脆い。**コード体系で除く**ほうが安全です。

### 見分け方

いちばん簡単な検査は、**合計を外部の値と比べる**ことです。それができないときは、

- カテゴリの値を足して、「計」と一致するか確かめる
- カテゴリの数が「2の倍数＋1」のような形になっていないか見る
- **1つの識別子（州×年など）に、行が何本あるかを数える**

3つ目が、本記事の不変量チェックそのものです。

---

## 12. チェックリスト

### データを取ってきた直後に

- [ ] この表には、どんな切り口（次元）があるか一覧したか
- [ ] その中に「計」「総数」「Total」に相当するものはないか
- [ ] 期間・時点の切り口（年次／半期／月次）はないか
- [ ] **1つの識別子に、行がいくつあるか数えたか**

### 切り口を選ぶとき

- [ ] 選択肢が2つ以上あるなら、**選んだ理由を書いたか**
- [ ] その選択は、技術的都合ではなく**分析目的**から決めたか
- [ ] 主系列と違う選択をしたなら、そのことを明記したか
- [ ] **年によって選択肢が変わる**表ではないか

### 集計するとき

- [ ] **1識別子＝1行**の検査を通しているか（不変量）
- [ ] 合計を、外部の公表値と突き合わせたか
- [ ] ぴったり2倍・3倍になっていないか
- [ ] 率や平均を、単純合計していないか（→ [罠09](README.md)）

---

## 13. まとめ

### 何が起きたか

統計表は「地域 × 派生変数 × 年 × 期間」の4次元でした。派生変数（男／女／**計**）を選ばずに合計したため、全国人口が**ちょうど2倍**になりました。期間（3月／9月）でも同じことが起き、貧困率が2倍になりました。

エラーは出ず、地図も正常に見えました。**外部の数字と突き合わせなければ、永久に気づけません。**

### どう直したか

1. **切り口を明示的に選ぶ**（`turvar_pick` / `turtahun_pick`）。指定しなければ、候補を示して止まる
2. **不変量で守る**：1州×1年＝1行。原因を知らなくても捕まえられる
3. `check_panel()` に `n_rows` を足し、`n_prov` と並べて表示

### 一般化した教訓

> **相対値しか見ない可視化は、倍率の誤りを隠す。**
> 地図も相関も順位も、全体が2倍でも見た目は変わらない。

> **「原因を1つずつ潰す」より、「結果が満たすべき性質」を1つ決めるほうが、守備範囲が広い。**
> 不変量は、まだ知らない原因にも効く。

> **選択肢が1つなら機械が決めてよい。2つ以上あるなら、それは判断であって、判断は人間の仕事。**

最後の1つは、生成AIとの協働の原則としてそのまま使えます。AIの側の仕事は、**選択肢を漏れなく机に出し、選ばずに進むことを許さない**ことです。「気を利かせて決めておく」のは、この場面では親切ではありません。

---

## 付録A：コード全文

```r
#' bps_get_data() の結果を stats.csv 形式(prov_name, indicator, year, value)にする
#'
#' @param df         bps_get_data() の出力
#' @param indicator  付与する指標名
#' @param turvar_pick 使う派生変数のラベル(例 "Total")。NULL なら絞らない
#' @param unit_mult  単位換算の倍率(千人単位の表なら 1000)
#' @param turtahun_pick 使う期間のラベル(例 "Semester 1 (March)")。
#'   **貧困率や失業率は年2回公表されるため、ここを指定しないと1州1年に
#'   複数行が残り、合計が倍になる。** 複数ある表で未指定なら停止する。
#'
#' @section 重複ガード:
#'   最後に「1州×1年が1行だけ」を検査する。ここを通れば、
#'   turvar / turtahun の取りこぼしによる二重計上は起きない。
to_stats_rows <- function(df, indicator, turvar_pick = "Total", unit_mult = 1,
                          turtahun_pick = NULL) {
  x <- bps_only_province(df)

  # --- 派生変数(turvar)の選択 -----------------------------------------
  if (!is.null(turvar_pick) && any(nzchar(x$turvar_label))) {
    x <- filter(x, turvar_label == turvar_pick)
  }

  # --- 【重要】欠測を先に落とす --------------------------------------
  # 「行はあるが値が空」があるため、期間の判定より先に落とす。
  # 詳しくは罠06 を参照。
  x <- filter(x, !is.na(value))

  # --- 期間(turtahun)の選択 -------------------------------------------
  # turtahun_pick は **優先順位のベクトル** を受け取る。
  # 年によって公表形態が変わる表があるため
  #   c("Semester 1 (March)", "Annually")
  # のように書くと、年ごとに「使える中でいちばん優先度の高いもの」を選ぶ。
  labs <- unique(x$turtahun_label[nzchar(x$turtahun_label)])
  if (length(labs) > 1) {
    if (is.null(turtahun_pick)) {
      stop("この表は期間が複数あります。turtahun_pick を指定してください:\n  ",
           paste(labs, collapse = "\n  "),
           "\n(年2回公表の指標では必須。指定しないと合計が倍になります)")
    }

    chosen <- x |>
      distinct(year, turtahun_label) |>
      group_by(year) |>
      summarise(pick = {
        p <- turtahun_pick[turtahun_pick %in% turtahun_label]
        if (length(p)) p[1] else NA_character_
      }, .groups = "drop")

    bad <- chosen |> filter(is.na(pick))
    if (nrow(bad) > 0) {
      cat("[!] 指定した期間がどれも無い年（落とします）:",
          paste(bad$year, collapse = ", "), "\n")
    }
    # 年によって違う期間を使ったなら、必ず見せる（黙って混ぜない）
    if (length(unique(chosen$pick[!is.na(chosen$pick)])) > 1) {
      cat("[i] 年によって使った期間が違います:\n")
      print(as.data.frame(chosen))
    }

    x <- x |>
      inner_join(chosen, by = "year") |>
      filter(!is.na(pick), turtahun_label == pick) |>
      select(-pick)
  }

  out <- x |>
    mutate(prov_name = normalize_prov(region),
           indicator = indicator,
           value     = value * unit_mult) |>
    select(prov_name, indicator, year, value) |>
    arrange(year, prov_name)

  # --- 重複ガード（不変量）--------------------------------------------
  # 1州×1年＝1行。ここを通れば、切り口の取りこぼしによる
  # 二重計上は「原因が何であれ」起きない。
  dup <- out |> count(prov_name, year) |> filter(n > 1)
  if (nrow(dup) > 0) {
    cat("[!] 1州×1年に複数行が残っています(先頭10件):\n")
    print(as.data.frame(head(dup, 10)))
    stop("重複があります。turvar_pick / turtahun_pick を見直してください。")
  }

  out
}

#' 州行だけ残す(全国行・県行を落とす)
#' @note コード体系（4桁・末尾00）と名前の両方で判定する。
#'   片方だけだと漏れることがある。
bps_only_province <- function(df) {
  df |>
    filter(grepl("^[0-9]{4}$", vervar), substr(vervar, 3, 4) == "00") |>
    filter(!grepl("^INDONESIA$", trimws(region), ignore.case = TRUE))
}

#' 年ごとの州数・行数・合計・欠測を出す(目視検算用)
#' @note n_prov と n_rows が違えば重複がある。
check_panel <- function(df) {
  df |>
    group_by(year) |>
    summarise(
      n_prov  = n_distinct(prov_name),
      n_rows  = n(),
      total   = sum(value, na.rm = TRUE),
      n_na    = sum(is.na(value)),
      .groups = "drop"
    ) |>
    arrange(year)
}
```

---

## 付録B：よくある質問

### Q1. 「Total」の行を捨てて、男と女を足せばいいのでは？

それでも合いますが、**おすすめしません。**

- BPS が公表している「Total」は、丸めの都合で男＋女とわずかに違うことがある
- 切り口が3つ以上ある表では、どれを足せばいいのか自明でなくなる
- **公表値をそのまま使うほうが、出所が明確**

原則は「**公表されている値を、加工せずに使う**」です。自分で足し直すのは、公表値が無いときだけにします。

### Q2. 不変量は他にどんなものがありますか？

この作業で使ったものを挙げます。

| 不変量 | 何を捕まえるか |
|---|---|
| 1州×1年＝1行 | 切り口の取りこぼし（この記事） |
| 州数は 33 / 34 / 38 のいずれか | 行政区画の取り違え |
| 州別の合計＝公表の全国値 | 二重計上、欠落 |
| 合計は部分の間にある | 概念の取り違え |
| 州名の集合は年をまたいで一定 | 表記ゆれ |

**どれも「正しければ必ず成り立つこと」を1文で書いたもの**です。証明ではありませんが、破れたら必ず何かが間違っています。

### Q3. 止まるコードは、実行のたびに面倒では？

面倒です。しかし、**面倒なのは最初の1回だけ**です。指定を書けば、次からは止まりません。

一方、止まらないコードの「面倒」は、**数週間後に、原因不明の数字として返ってきます。** どちらの面倒を引き受けるかという話です。

### Q4. 他の罠も知りたいのですが

この記事は連載の5本目です。

1. [一覧APIの暗黙の打ち切り](01-silent-truncation.md)
2. [カタログの二言語混在](02-bilingual-catalog.md)
3. [年の必須指定と3年上限](03-year-parameter.md)
4. [連結キーの桁数不定](04-composite-key.md)
5. **派生変数・期間の未指定**（この記事）
6. 行は存在するが値が空 ── 可用性は行数で判定してはいけない
7. 静的表の実体がHTML ── Excelと称するファイルが開けない
8. 州名の表記ゆれと脚注記号 ── 地図の一部が灰色になる
9. 行政区画の変更（33→34→38州） ── 足せる量と足せない量
10. 公表基準の断絶（3月調査と9月調査） ── 検算が失敗したとき、何を疑うか

---

## この記事について

この記事は、日本計算機統計学会シンポジウムでの報告「検算駆動による生成AIとの協働 ── インドネシア公的統計WebAPIからの州別パネル構築とShinyによる可視化」の補足資料として作成しました。

本文中のコード、出力、数値は、すべて実際の作業記録に基づいています。うまくいかなかった過程も、そのまま残しています。

### 生成AIの利用について

**本記事の文章は、生成AI（Claude Opus 5、推論設定「高」）が、久保田による指示（プロンプト）と複数回のやりとりを経て生成したものです。** 構成・記述・コード解説はAIによる出力であり、掲載しているコード、コンソール出力、数値は、いずれも実際の作業記録に基づいています。内容は著者が確認したうえで公開しています。

この記事の主題そのものが「生成AIと協働してデータを取得した記録」であるため、記事の生成過程を隠す理由がありません。念のため明記しておきます。

- 著者：久保田貴文（多摩大学）
- 連絡先：kubota@tama.ac.jp
- Web：https://takafumikubota.jp/

この作業で用いた統計は、インドネシア中央統計庁（BPS）が公開するWebAPIから取得しました。
Layanan ini menggunakan API Badan Pusat Statistik (BPS).
（本サービスはインドネシア中央統計庁のAPIを使用しています。）
