# 罠03：年の指定は必須、しかも1回に3年まで

> **一行でいうと**
> **エラーメッセージは、仕様書である。**
> 「エラーが出た」で止まらず、「エラーが**何と言っているか**」を読むと、公開されていない仕様が手に入る。

**この罠の危険度：★★★☆☆**
罠01・罠02と違い、**ちゃんとエラーになります**。その意味では良心的です。ただし条件が1つあります。**エラーの中身を画面に出していれば**、の話です。

---

## 目次

1. [前回までのあらすじ](#1-前回までのあらすじ)
2. [予備知識：APIの「仕様書」はどこにあるのか](#2-予備知識apiの仕様書はどこにあるのか)
3. [投げかけ：「この表からデータを取って」](#3-投げかけこの表からデータを取って)
4. [返ってきた答え：`status: Error`（それだけ）](#4-返ってきた答えstatus-errorそれだけ)
5. [最初の問題は、APIではなくこちらにあった](#5-最初の問題はapiではなくこちらにあった)
6. [寄り道：デバッグ出力に秘密を混ぜない](#6-寄り道デバッグ出力に秘密を混ぜない)
7. [見えたエラー①：`'th' parameter is required`](#7-見えたエラーth-parameter-is-required)
8. [`th` とは何か ── 年に付けられた番号](#8-th-とは何か--年に付けられた番号)
9. [対応表は、推測せずにAPIに聞く](#9-対応表は推測せずにapiに聞く)
10. [ここでも罠01が顔を出した](#10-ここでも罠01が顔を出した)
11. [見えたエラー②：`maximum allowed number of years is 3`](#11-見えたエラーmaximum-allowed-number-of-years-is-3)
12. [年の書き方は3通りある](#12-年の書き方は3通りある)
13. [対処：3年ずつに割って、つなぎ直す](#13-対処3年ずつに割ってつなぎ直す)
14. [直ったことをどう確かめたか](#14-直ったことをどう確かめたか)
15. [取れなかった年を、黙って落とさない](#15-取れなかった年を黙って落とさない)
16. [あなたのAPIでも同じことが起きる](#16-あなたのapiでも同じことが起きる)
17. [チェックリスト](#17-チェックリスト)
18. [まとめ](#18-まとめ)
19. [付録A：コード全文](#付録aコード全文)
20. [付録B：よくある質問](#付録bよくある質問)

---

## 1. 前回までのあらすじ

- [罠01](01-silent-truncation.md)：統計表のカタログが500件で静かに切れていた。直したら1,816件あった
- [罠02](02-bilingual-catalog.md)：英語版のカタログにインドネシア語のタイトルが混ざっていて、英単語で絞ると消えた

この2つを越えて、**欲しい表の番号は分かりました**。あとはその番号を指定して、中身の数値を取ってくるだけです。

図書館でいえば、目録で本の請求番号を突き止めたところ。あとは書架に取りに行くだけ ── のはずでした。

---

## 2. 予備知識：APIの「仕様書」はどこにあるのか

APIには普通、説明書（ドキュメント）が用意されています。「この項目は必須です」「1回にこれだけまで指定できます」といったことが書いてあります。

**しかし、実際に書いてあるとは限りません。**

書いてあっても、更新が止まっていたり、インドネシア語だけだったり、必要なことが抜けていたりします。今回の BPS API も、公開されている説明からは読み取れないことがいくつもありました。

では、どうやって仕様を知るのか。**答えは、エラーメッセージです。**

APIは、間違った質問を投げると「何が間違っているか」を教えてくれます。この返答は、実は**最も信頼できる仕様書**です。理由は単純で、

> **説明書は人が書いたもの。エラーメッセージはプログラムが出したもの。**
> **実際に動いているのは、後者のほうです。**

説明書が古くなっていても、エラーメッセージは常に現在の実装を反映しています。

この記事は、**エラーメッセージを2つ読んだだけで、公開されていない仕様が2つ分かった**という話です。

---

## 3. 投げかけ：「この表からデータを取って」

投げかけはこうでした。

> **人間**：（表の番号を指定して）この表の数値を取ってきて。

**注記**：以下の「投げかけ」は実際のやりとりの要約です。一字一句そのままではありませんが、内容は変えていません。

AIが書いたのは、こういう素直なコードです。

```r
r <- bps_request("list", "model", "data", "lang", "eng",
                 "domain", "0000", "var", 1886)
```

意味は「`data` という種類の質問を、英語で、全国のデータについて、変数番号1886で投げる」です。

URLに直すと、こうなります。

```
https://webapi.bps.go.id/v1/api/list/model/data/lang/eng/domain/0000/var/1886/key/【鍵】
```

**何も間違っていないように見えます。** カタログを取るときと同じ形で、`model` を `list` から `data` に変えただけですから。

---

## 4. 返ってきた答え：`status: Error`（それだけ）

実行すると、こう出ました。

```
status: Error
```

**以上。それだけ。**

なぜエラーなのか。何が足りないのか。何も分かりません。

こういうとき、人はいろいろ想像します。

- 鍵が間違っているのか？
- この表は使えないのか？
- 通信が切れたのか？
- そもそも `data` という指定が違うのか？

**そして、想像したことを片っ端から試し始めます。** これが一番よくない進み方です。当てずっぽうを続けると、たまたま動いたときに「なぜ動いたか」が分からないまま先に進むことになるからです。

---

## 5. 最初の問題は、APIではなくこちらにあった

ここで立ち止まって、確認したことが1つあります。

> **本当に「status: Error」しか返ってきていないのか？**

答えは **No** でした。

BPS の応答は、こういう形をしています。

```json
{
  "status": "Error",
  "message": "'th' parameter is required",
  "data-availability": "not-available"
}
```

**`message` に理由が書いてあったのです。**

では、なぜ画面に出なかったのか。**こちらのコードが `status` しか表示していなかったから**です。

```r
# 当時の様子見用のコード（要約）
r <- bps_request(...)
cat("status:", r$status, "\n")     # ← status しか見ていない
```

APIは最初から答えを教えてくれていました。**こちらが耳を塞いでいただけ**です。

これは、罠01・罠02とは種類が違いますが、根っこは同じです。

| 罠 | 見えていなかったもの |
|---|---|
| 罠01 | カタログの4分の3 |
| 罠02 | 絞り込みで落とした20件 |
| **罠03** | **エラーメッセージそのもの** |

**「情報が無い」のではなく、「情報を表示していない」。** 3回続けて同じ形です。

### 対処：生の応答をそのまま見る道具を作る

そこで、応答を加工せずそのまま画面に出す関数を作りました。

```r
bps_debug <- function(var_id, th = NULL, domain = "0000", lang = "eng",
                      nchar_max = 1200) {
  args <- c("list", "model", "data", "lang", lang,
            "domain", domain, "var", var_id)
  if (!is.null(th)) args <- c(args, "th", th)
  url <- do.call(bps_url, as.list(args))

  cat("URL(キーは伏せた):\n  ",
      sub("/key/.*$", "/key/***", url), "\n\n", sep = "")

  txt <- paste(readLines(url, warn = FALSE, encoding = "UTF-8"), collapse = "")
  cat("生のレスポンス(先頭", nchar_max, "文字):\n", sep = "")
  cat(substr(txt, 1, nchar_max), "\n")
  invisible(txt)
}
```

やっていることは2つだけです。

1. **投げたURLを表示する**（どんな質問をしたのかを確認するため）
2. **返ってきた文字列を、解釈せずそのまま表示する**

**「解釈せずそのまま」が肝です。** プログラムが親切に整形すると、整形の過程で情報が落ちます。原因を探しているときは、生のまま見るのが一番速いのです。

---

## 6. 寄り道：デバッグ出力に秘密を混ぜない

`bps_debug` の中に、地味ですが重要な1行があります。

```r
sub("/key/.*$", "/key/***", url)
```

これは「URLの `/key/` より後ろを `***` に置き換える」という意味です。

### なぜこれが要るのか

BPS の API は、URLの末尾に**認証キー**（利用者ごとに発行される秘密の文字列）を付けます。

```
.../var/1886/key/abcdef0123456789abcdef0123456789
                 ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~ これが秘密
```

デバッグのためにURLを画面に出すと、**この秘密も一緒に出ます**。そして画面に出たものは、

- スクリーンショットに写る
- ログファイルに残る
- **エラーの相談で他人に貼り付けられる**
- 生成AIとの会話に貼り付けられる

最後の2つが、この作業では現実の危険でした。**うまくいかないとき、人はコンソールの出力をそのままコピーして貼ります。** そこにキーが混ざっていたら、その時点で漏れます。

BPS の利用規約には、次のように定められています。

- **トークンを第三者に渡したり貸したりしてはならない**（第3条B）
- **トークンの漏洩は、利用者自身の責任である**（第14条A）

つまり、漏らしたらこちらの責任です。

### この作業で決めたこと

| やったこと | 理由 |
|---|---|
| キーは `~/.Renviron` に置く | スクリプトに直接書かない。ファイルを他人に渡しても漏れない |
| デバッグ出力ではキーを `***` にマスク | 画面・ログ・貼り付けに乗らない |
| **キーを会話に一度も貼らない** | 生成AIとのやりとりを含め、一度も |

3つ目は、**生成AIと作業するときに特に効きます**。「エラーが出た」と言って出力を貼り戻すやり方は、この作業の基本動作でした。マスクしていなければ、何十回も貼るうちに1回はキー入りのものが混ざったはずです。

**「うっかり貼らないように気をつける」では防げません。** 最初からマスクしておく、という設計で防ぎます。

```r
bps_check_key <- function() {
  k <- tryCatch(bps_key(), error = function(e) NULL)
  if (is.null(k)) cat("[NG] キーが読めていません\n")
  else cat("[OK] キーを検出(先頭6文字:", substr(k, 1, 6),
           "... 長さ", nchar(k), ")\n")
  invisible(NULL)
}
```

キーが設定できているかの確認も、**先頭6文字と長さだけ**を出す作りにしました。これなら「設定されているか」は分かり、「何であるか」は分かりません。

---

## 7. 見えたエラー①：`'th' parameter is required`

さて、本題に戻ります。`bps_debug` で見えたのは、これでした。

```
URL(キーは伏せた):
  https://webapi.bps.go.id/v1/api/list/model/data/lang/eng/domain/0000/var/1886/key/***

生のレスポンス(先頭 1200 文字):
{"status":"Error","message":"'th' parameter is required","data-availability":"not-available"}
```

**`'th' parameter is required`** ── 「`th` という項目が必須です」。

一発で分かりました。年を指定していなかったのです。

### ここが面白いところ

カタログを取るとき（`model=list`）には、年の指定は要りませんでした。**同じ形のURLなのに、`model` を `data` に変えた途端に必須項目が増える**。

これは公開されている説明からは読み取りにくい部分です。しかしAPIは、**間違えた瞬間に教えてくれました**。

> **エラーメッセージは、無料で手に入る仕様書である。**

---

## 8. `th` とは何か ── 年に付けられた番号

`th` はインドネシア語の `tahun`（＝年）の略です。

ここで新しい疑問が出ます。**2018年のデータが欲しいとき、`th` に何を書けばいいのか。**

素直に考えれば `th=2018` です。しかし、そうではありませんでした。

BPS は、年に**内部の番号**を割り当てています。

| 年 | `th_id`（内部番号） |
|---|---|
| 2010 | 110 |
| 2011 | 111 |
| 2018 | 118 |
| 2020 | 120 |
| 2025 | 125 |
| 2026 | 126 |

**規則性が見えます。西暦から1900を引いた数です。**

```
2018 − 1900 = 118
2025 − 1900 = 125
```

おそらく、`th_id` を2桁で運用していた時代（1990年なら90、1999年なら99）の名残でしょう。2000年を超えたときに、100、101…と続けたわけです。**世の中の統計システムには、こういう歴史の積み重ねがよくあります。**

コードには、変換用の小さな関数を置きました。

```r
#' 西暦 → 年ID(確認済み: th_id = 西暦 - 1900。120=2020, 119=2019 …)
year_to_th <- function(years) as.integer(years) - 1900L
th_to_year <- function(th)    as.integer(th) + 1900L
```

**両方向の変換を用意する**のは、地味ですが効きます。取ってきたデータには `th_id` が入っているので、人間が読める西暦に戻す関数が必ず要るからです。

---

## 9. 対応表は、推測せずにAPIに聞く

ここで、**やり方の話**を1つ書いておきます。

上の対応表を、どうやって手に入れたか。**推測ではありません。APIに聞きました。**

BPS には「その表で使える年の一覧」を返すエンドポイントがあります。

```r
bps_list_th(195)
```

```
   th_id   th
1    126 2026
2    125 2025
3    124 2024
4    123 2023
5    122 2022
6    121 2021
7    120 2020
8    119 2019
9    118 2018
10   117 2017
11   116 2016
12   115 2015
13   114 2014
14   113 2013
```

**左が内部番号、右が西暦。対応表そのものが返ってきます。**

つまり、`th_id = 西暦 − 1900` という規則を発見する必要すらありませんでした。**一覧を取れば、答えが書いてある**のです。

### 学べること

> **対応表が必要になったら、まず「APIが対応表を持っていないか」を疑う。**

推測して当てにいくのは、対応表が無いと確かめてからで十分です。今回は規則性も見つけたので変換関数を作りましたが、**それは高速化のためであって、規則を推測したから使えたわけではありません**。

推測で当てた規則は、いつか外れます。たとえば BPS が半期のデータに `th_id` を割り当てるようになったら、`− 1900` は通用しなくなります。**一覧から読む方法を残しておけば、そのときも壊れません。**

だからコードには、両方を残しました。

- `year_to_th()`：速い。規則に依存する
- `bps_list_th()`：確実。APIに聞く

そして、**取得の前には必ず `bps_list_th()` でその表の収録年を確かめる**という手順にしました。存在しない年を要求しても意味がないからです。

---

## 10. ここでも罠01が顔を出した

この `bps_list_th()` にも、実は問題がありました。

**きっかり10件しか返ってこなかった**のです。

```r
bps_list_th(141)
```

```
（10行だけ返ってくる）
```

「この表は10年分しかないのか」── そう読むのが自然です。

しかし [罠01](01-silent-truncation.md) を通ったあとなら、別の見方ができます。

> **1ページ10件のAPIで、ちょうど10件。それは「10件しかない」ではなく「10件で切られている」かもしれない。**

そこで、この一覧にもページ送りを実装しました。結果、

| | 修正前 | 修正後 |
|---|---|---|
| var 141 の収録年 | **10年** | **19年** |

**ほぼ2倍**でした。

コードには、次に読む人（と、次の自分）に向けて、こう書き残しています。

```r
#' その変数で使える年の一覧(ID と表示ラベル)
#'
#' @note **この一覧も1ページ10件でページ送りされる。**
#'   ちょうど10件返ってきたら「それで全部」ではなく「切られている」と疑うこと。
#'   ここでは全ページを取得する。
bps_list_th <- function(var_id = NULL, domain = "0000", lang = "eng",
                        max_page = 50) {
  ...
}
```

**同じ罠は、必ず似た場所に複数あります。** 罠01でカタログを直したなら、**同じ形の一覧を全部点検する**。これは作業の型にできます。

---

## 11. 見えたエラー②：`maximum allowed number of years is 3`

`th` を付けて、あらためて投げます。16年分ほしいので、素直に16年を並べました。

```r
bps_request("list", "model", "data", "lang", "eng",
            "domain", "0000", "var", 1886,
            "th", "110;111;112;113;114;115;116;117;118;119;120;121;122;123;124;125")
```

返ってきたのは、また `Error` です。今度は `message` を表示するようにしてあるので、理由が見えます。

```
{"status":"Error","message":"maximum allowed number of years is 3", ...}
```

**「指定できる年は最大3年です」**

これも、公開されている説明からは読み取れなかった制約です。しかしAPIは、はっきり数字で教えてくれました。

### エラーメッセージから得られたもの（まとめ）

| エラー | 分かったこと |
|---|---|
| `'th' parameter is required` | `data` では年の指定が必須 |
| `maximum allowed number of years is 3` | **1回の要求で3年まで** |

**2つのエラーだけで、必要な仕様が全部そろいました。** ドキュメントを探し回るより、はるかに速い。

コードには、この事実の出どころを明記しました。

```r
# 1リクエストで指定できる年数の上限(BPS のエラーメッセージより)
BPS_TH_MAX <- 3L
```

`3` という数字をコードに直接書くのではなく、**名前を付けて、由来をコメントに残す**。こうしておくと、将来 BPS が上限を5年に変えたときに、直す場所が1か所で済みます。

---

## 12. 年の書き方は3通りある

`th` の書き方は、試したところ3通りありました。

| 書き方 | 例 | 意味 |
|---|---|---|
| 単一 | `"118"` | 2018年だけ |
| 範囲 | `"110:112"` | 2010年から2012年まで（連続） |
| 列挙 | `"110;115;120"` | 2010年、2015年、2020年（とびとび） |

範囲は `:`（コロン）、列挙は `;`（セミコロン）です。

**どちらを使うか。** 結論としては、**列挙（`;`）を採りました。** 理由は2つあります。

**理由1：連続とは限らない**

たとえば都市化率の統計は、2010年・2015年・2020年・2025年という**5年刻み**でした。範囲指定では表せません。

**理由2：3年ずつに割ったとき、境目が連続しない場合がある**

収録年に穴があると、3年ずつに割った結果が `2010, 2011, 2013` のようになります。列挙なら、そのまま書けます。

つまり、**列挙は範囲の上位互換**です。範囲でできることは列挙でもできて、逆はできない。**両方あるなら、汎用のほうを選ぶ。**

（一応、範囲を作る関数も残してあります。手で試すときには短くて便利だからです。）

```r
#' 西暦ベクトルから th パラメータ文字列を作る
th_range <- function(from, to) paste0(year_to_th(from), ":", year_to_th(to))
```

---

## 13. 対処：3年ずつに割って、つなぎ直す

やることは決まりました。

1. ほしい年を3年ずつの塊に割る
2. 塊ごとに要求を投げる
3. 返ってきたものを縦に積む

### 割る関数

```r
#' 年ベクトルを上限3年ずつのチャンクに割る
th_chunks <- function(years, size = BPS_TH_MAX) {
  ids <- sort(unique(year_to_th(years)))
  split(ids, ceiling(seq_along(ids) / size))
}
```

短いですが、3つの配慮が入っています。

```r
ids <- sort(unique(year_to_th(years)))
```

- `year_to_th()` で西暦を内部番号に変換
- `unique()` で**重複を除く**（同じ年を2回指定しても1回にする）
- `sort()` で**昇順に並べる**（順序がばらばらでも結果が同じになる）

**「呼び出し側が何を渡しても、同じ結果になる」**ようにしておくと、後の事故が減ります。`th_chunks(c(2011, 2010, 2011))` と `th_chunks(2010:2011)` は同じ結果になります。

```r
split(ids, ceiling(seq_along(ids) / size))
```

これが分割の本体です。`seq_along(ids)` は 1, 2, 3, 4, 5, … という連番、それを3で割って切り上げると 1, 1, 1, 2, 2, 2, 3, … となり、3個ずつのグループ番号になります。

16年（2010〜2025年）なら、こう割れます。

| 塊 | 年 | `th` の文字列 |
|---|---|---|
| 1 | 2010, 2011, 2012 | `110;111;112` |
| 2 | 2013, 2014, 2015 | `113;114;115` |
| 3 | 2016, 2017, 2018 | `116;117;118` |
| 4 | 2019, 2020, 2021 | `119;120;121` |
| 5 | 2022, 2023, 2024 | `122;123;124` |
| 6 | 2025 | `125` |

**6回の要求**で16年分がそろいます。

### 取得する関数

```r
bps_get_data <- function(var_id, years, domain = "0000", lang = "eng",
                         verbose = TRUE) {
  chunks <- th_chunks(years)
  res_list <- list()

  for (i in seq_along(chunks)) {
    th <- paste(chunks[[i]], collapse = ";")
    if (verbose) cat("取得中 [", i, "/", length(chunks), "] th=", th, " ... ", sep = "")

    r <- tryCatch(
      bps_request("list", "model", "data", "lang", lang,
                  "domain", domain, "var", var_id, "th", th),
      error = function(e) NULL
    )

    if (is.null(r) || !identical(r$status, "OK")) {
      msg <- if (is.null(r)) "リクエスト失敗" else as.character(r$message)
      if (verbose) cat("スキップ (", msg, ")\n", sep = "")
      next
    }

    d <- bps_parse_data(r, var_id = var_id)
    res_list[[length(res_list) + 1]] <- d
    if (verbose) cat(nrow(d), "行\n")
  }

  if (length(res_list) == 0) {
    warning("1件も取得できませんでした")
    return(NULL)
  }
  bind_rows(res_list)
}
```

実行するとこう見えます。

```
取得中 [1/6] th=110;111;112 ... 1656 行
取得中 [2/6] th=113;114;115 ... 1656 行
取得中 [3/6] th=116;117;118 ... 1656 行
取得中 [4/6] th=119;120;121 ... 1656 行
取得中 [5/6] th=122;123;124 ... 1656 行
取得中 [6/6] th=125 ... 552 行
```

**進み具合と、各回の行数が見えます。** これは飾りではありません。次の節で説明しますが、**この行数が異常を教えてくれる**ことがあります。

---

## 14. 直ったことをどう確かめたか

### 確認その1：全部の塊が通ったか

上の出力で、6回すべてが行数を返しています。「スキップ」が1つもありません。

### 確認その2：年がそろっているか

```r
table(raw$year)
```

```
2010 2011 2012 2013 2014 2015 2016 2017 2018 2019 2020 2021 2022 2023 2024 2025
 552  552  552  552  552  552  552  552  552  552  552  552  552  552  552  552
```

**16年、すべて552行ずつ。** 塊の境目（2012年と2013年、2015年と2016年…）で行数が変わっていないので、つなぎ目に問題は無さそうです。

### 確認その3：行数の意味が説明できるか

552行とは何か。内訳を出すと、

```
           kind   n
1 州(4桁末尾00)  38
2       県(4桁) 514
```

**38 + 514 = 552。** 州が38、県・市が514。数が説明できました。

**「行数が合っている」だけでは不十分で、「なぜその行数になるのかを説明できる」ところまで来て、はじめて確認したことになります。**

---

## 15. 取れなかった年を、黙って落とさない

`bps_get_data` には、意図的に入れた仕組みがあります。

```r
if (is.null(r) || !identical(r$status, "OK")) {
  msg <- if (is.null(r)) "リクエスト失敗" else as.character(r$message)
  if (verbose) cat("スキップ (", msg, ")\n", sep = "")
  next
}
```

**ある塊が失敗しても、全体を止めません。** その塊を飛ばして次に進みます。

これは一見すると危険な設計です。**黙って落とす**のは、罠01・罠02でさんざん批判した振る舞いだからです。

しかし、ここでは黙っていません。

```
取得中 [1/6] th=110;111;112 ... スキップ (該当データなし)
```

**理由付きで、その場で表示します。**

### なぜ止めずに続けるのか

年によってデータが無いのは、統計では**普通のこと**だからです。2010年は調査していない、2020年はコロナで中止した、といったことは実際に起こります。1年欠けただけで全体が止まると、かえって作業になりません。

**大事なのは、「落としたことが分かる」ことです。** 落とすこと自体は許容し、落としたら必ず言う。

そして実際、この表示が効いた場面がありました。**貧困線の取得です。**

```
取得中 [1/6] th=110;111;112 ... 1 行
取得中 [2/6] th=113;114;115 ... 234 行
```

エラーではありません。`status` は `OK` です。しかし **1行**。他の塊は234行なのに。

**この「1行」が異常の合図でした。** ここから「2010〜2012年は、この表に存在しない」という結論にたどり着きます（詳しくは別の記事で扱います）。

もし行数を表示していなければ、**エラーでもないので、誰も気づかなかった**はずです。

> **進捗表示は、飾りではなく検算の一部である。**

数字を出しておくと、人間が「あれ？」と思う機会が生まれます。**人間が異常に気づくためには、まず数字が目に入る必要があります。**

---

## 16. あなたのAPIでも同じことが起きる

### 「必須項目」と「1回あたりの上限」は、どのAPIにもある

| API | 制約の例 |
|---|---|
| **e-Stat（日本）** | `statsDataId` が必須。1回の取得件数に上限があり、続きは `startPosition` で取る |
| **世界銀行** | 国・指標・期間の指定。期間は `date=2010:2020` の形。1リクエストの件数に上限 |
| **Eurostat** | クエリが大きすぎると拒否される。次元を絞るか、分割する |
| **OECD** | URLの長さ制限に当たることがある |
| **各種の商用API** | 「1回100件まで」「1分60回まで」といったレート制限 |

**制約があること自体は問題ではありません。** 問題は、制約を知らずに一度に全部要求して、

- エラーで止まる（まだ良い）
- **切り詰められた結果が、正常な顔で返ってくる**（最悪）

の後者になることです。

### エラーメッセージの読み方

エラーが返ってきたら、次の順に見てください。

1. **本文（`message`、`error`、`detail` など）を必ず表示する。** ステータスだけ見ない
2. **数字が入っていたら、それは仕様である。** 「maximum ... is 3」の 3 は、定数として名前を付けて残す
3. **項目名が入っていたら、それは必須項目である。** 「'th' parameter is required」の `th`
4. **同じエラーを2回見たら、コードに書き込む。** 3回目は起きないようにする

### HTTPステータスだけを見ない

多くのAPIは、エラーでも `200 OK` を返し、本文の中で `status: Error` と言います。今回の BPS もそうでした。**通信としては成功しているので、通信ライブラリは何も文句を言いません。**

> **「通信が成功した」と「欲しい答えが返ってきた」は別のことです。**

---

## 17. チェックリスト

### 最初の1回目の要求で

- [ ] エラーの**本文**を表示する作りになっているか（ステータスだけになっていないか）
- [ ] 生の応答をそのまま見る手段があるか
- [ ] **その出力に秘密（APIキー、トークン）が混ざっていないか**
- [ ] 投げたURL（またはパラメータ）も表示しているか

### 制約が分かったら

- [ ] 上限値に**名前を付けて**定数にしたか（`BPS_TH_MAX <- 3L`）
- [ ] その由来をコメントに残したか（「エラーメッセージより」）
- [ ] 上限を超える要求は、自動で分割されるか

### 分割して取るとき

- [ ] 各回の**進捗と行数**を表示しているか
- [ ] 失敗した回を、**理由付きで**報告しているか
- [ ] 分割の境目で、値がおかしくなっていないか（年ごとの行数を数える）
- [ ] 重複した指定を渡しても、同じ結果になるか

### 取り終わったら

- [ ] 欲しかった年が**全部そろっているか**（`table(年)` で数える）
- [ ] 行数の意味を**説明できるか**（「552行＝州38＋県514」）

---

## 18. まとめ

### 何が起きたか

データを取ろうとしたら `status: Error` とだけ出て、理由が分かりませんでした。応答の中身を見たら `message` に理由が書いてあり、そこから2つの仕様が判明しました。

1. `data` では**年の指定が必須**（`th`）
2. **1回の要求で3年まで**

年の内部番号は「西暦 − 1900」でしたが、これは推測する必要がなく、**一覧エンドポイントが対応表を返してくれました**。

### どう直したか

1. 生の応答をそのまま見る `bps_debug()` を作った（**キーはマスク**）
2. 上限を定数 `BPS_TH_MAX <- 3L` にし、由来をコメントに残した
3. 年を3年ずつに割る `th_chunks()` を作り、自動で分割して取るようにした
4. 各回の**進捗と行数を必ず表示**するようにした

### 一般化した教訓

> **エラーメッセージは、無料で手に入る、常に最新の仕様書である。**
> ドキュメントは古くなるが、エラーメッセージは実装そのものが吐いている。

> **ステータスだけを見て、本文を見ないのは、耳を塞いでいるのと同じ。**

> **対応表が必要になったら、推測する前に「APIが持っていないか」を確かめる。**

> **進捗表示は飾りではない。行数が目に入るから、人間が異常に気づける。**

そして、この記事で唯一「事故の記録」ではないもの ── **秘密をマスクする1行**について。

> **「うっかり貼らないよう気をつける」では防げない。最初からマスクしておく設計で防ぐ。**

生成AIと作業すると、**コンソールの出力を貼り戻す回数が桁違いに増えます**。この作業でも何十回と貼りました。マスクを入れていなければ、そのどこかで確実に漏れていたはずです。**協働のやり方が変われば、気をつけるべき場所も変わります。**

---

## 付録A：コード全文

```r
# =====================================================================
# 年（th）の扱い
# =====================================================================

# --- 西暦と内部番号の変換 --------------------------------------------
# 確認済み: th_id = 西暦 - 1900。120=2020, 119=2019 …
# ただし規則に頼りきらず、bps_list_th() で実際の対応表も取れるようにしてある。
year_to_th <- function(years) as.integer(years) - 1900L
th_to_year <- function(th)    as.integer(th) + 1900L

# --- 範囲指定の文字列を作る（手で試すとき用） -------------------------
th_range <- function(from, to) paste0(year_to_th(from), ":", year_to_th(to))

# --- 1リクエストの年数上限 -------------------------------------------
# 出どころ: BPS のエラーメッセージ
#   "maximum allowed number of years is 3"
# 将来ここが変わったら、この定数だけ直せばよい。
BPS_TH_MAX <- 3L

# --- 年ベクトルを3年ずつに割る ---------------------------------------
# 重複を除き、昇順に並べてから割る。
# 呼び出し側が何を渡しても同じ結果になるようにするため。
th_chunks <- function(years, size = BPS_TH_MAX) {
  ids <- sort(unique(year_to_th(years)))
  split(ids, ceiling(seq_along(ids) / size))
}

# --- その表で使える年の一覧 ------------------------------------------
#' @note **この一覧も1ページ10件でページ送りされる。**
#'   ちょうど10件返ってきたら「それで全部」ではなく「切られている」と疑うこと。
bps_list_th <- function(var_id = NULL, domain = "0000", lang = "eng",
                        max_page = 50) {
  get_page <- function(p) {
    args <- c("list", "model", "th", "lang", lang, "domain", domain)
    if (!is.null(var_id)) args <- c(args, "var", var_id)
    args <- c(args, "page", p)
    do.call(bps_request, as.list(args))
  }

  first <- get_page(1)
  if (!identical(first$status, "OK")) {
    cat("status:", as.character(first$status),
        " message:", as.character(first$message), "\n")   # message も出す
    return(invisible(first))
  }

  info  <- bps_pageinfo(first)
  pages <- suppressWarnings(as.integer(info$pages))
  if (is.na(pages)) pages <- 1
  pages <- min(pages, max_page)

  out <- bps_rows(first)
  if (pages > 1) {
    for (p in 2:pages) out <- rbind(out, bps_rows(get_page(p)))
  }
  out
}

# --- 生の応答を見る（キーはマスク） ----------------------------------
bps_debug <- function(var_id, th = NULL, domain = "0000", lang = "eng",
                      nchar_max = 1200) {
  args <- c("list", "model", "data", "lang", lang,
            "domain", domain, "var", var_id)
  if (!is.null(th)) args <- c(args, "th", th)
  url <- do.call(bps_url, as.list(args))
  cat("URL(キーは伏せた):\n  ",
      sub("/key/.*$", "/key/***", url), "\n\n", sep = "")

  txt <- paste(readLines(url, warn = FALSE, encoding = "UTF-8"), collapse = "")
  cat("生のレスポンス(先頭", nchar_max, "文字):\n", sep = "")
  cat(substr(txt, 1, nchar_max), "\n")
  invisible(txt)
}

# --- 鍵の確認（中身は見せない） --------------------------------------
bps_key <- function() {
  k <- Sys.getenv("BPS_API_KEY", unset = "")
  if (!nzchar(k)) {
    stop("BPS_API_KEY が設定されていません。\n",
         "  usethis::edit_r_environ() で ~/.Renviron に\n",
         "  BPS_API_KEY=... を1行書いて R セッションを再起動してください。")
  }
  k
}

bps_check_key <- function() {
  k <- tryCatch(bps_key(), error = function(e) NULL)
  if (is.null(k)) cat("[NG] キーが読めていません\n")
  else cat("[OK] キーを検出(先頭6文字:", substr(k, 1, 6),
           "... 長さ", nchar(k), ")\n")
  invisible(NULL)
}

# --- 3年ずつに割って取得する -----------------------------------------
bps_get_data <- function(var_id, years, domain = "0000", lang = "eng",
                         verbose = TRUE) {
  chunks <- th_chunks(years)
  res_list <- list()

  for (i in seq_along(chunks)) {
    th <- paste(chunks[[i]], collapse = ";")
    if (verbose) cat("取得中 [", i, "/", length(chunks), "] th=", th, " ... ", sep = "")

    r <- tryCatch(
      bps_request("list", "model", "data", "lang", lang,
                  "domain", domain, "var", var_id, "th", th),
      error = function(e) NULL
    )

    # 失敗しても止めない。ただし必ず理由を言う。
    if (is.null(r) || !identical(r$status, "OK")) {
      msg <- if (is.null(r)) "リクエスト失敗" else as.character(r$message)
      if (verbose) cat("スキップ (", msg, ")\n", sep = "")
      next
    }

    d <- bps_parse_data(r, var_id = var_id)
    res_list[[length(res_list) + 1]] <- d
    if (verbose) cat(nrow(d), "行\n")   # ← この行数が異常の合図になる
  }

  if (length(res_list) == 0) {
    warning("1件も取得できませんでした")
    return(NULL)
  }
  bind_rows(res_list)
}
```

### 使い方

```r
# 鍵が読めているか（中身は表示されない）
bps_check_key()
#> [OK] キーを検出(先頭6文字: a1b2c3 ... 長さ 32 )

# この表に何年分あるか
bps_years(195)

# 16年分を取る（自動で6回に分割される）
raw <- bps_get_data(195, 2010:2025)

# 年ごとの行数を数える（検算）
table(raw$year)
```

---

## 付録B：よくある質問

### Q1. `th` が必須だと、なぜ最初から分からなかったのですか？

公開されている説明に、その記述を見つけられなかったからです。**説明が無いこと自体を責めても仕方がありません。** 大事なのは、説明が無いときに何をするかです。今回は「エラーを読む」で解決しました。

### Q2. 3年という上限は、変わる可能性がありますか？

あります。だから `BPS_TH_MAX <- 3L` という定数にして、由来をコメントに残しました。もし将来 `maximum allowed number of years is 5` というエラーを見たら、その1行を直せば済みます。

**「数字をコードに直接書かない」**のは、こういう場面のためです。

### Q3. 分割して取ると、遅くなりませんか？

なります。16年分で6回の通信です。しかし、**取れないよりは遥かにましです。**

どうしても速くしたい場合は、並列に投げる方法もあります。ただし、**相手のサーバーに負荷をかけることになるので、公的機関のAPIでは避けるべき**です。今回は、必要な回数を順番に投げる形にしました。

### Q4. `tryCatch` で失敗を握りつぶすのは、罠01で批判していた振る舞いでは？

鋭い指摘です。違いは**表示するかどうか**の一点です。

```r
# 罠のもと：黙って NULL を返す
tryCatch(..., error = function(e) NULL)

# 今回：NULL を返すが、その場で理由を表示する
if (is.null(r) || !identical(r$status, "OK")) {
  msg <- if (is.null(r)) "リクエスト失敗" else as.character(r$message)
  cat("スキップ (", msg, ")\n", sep = "")
  next
}
```

**握りつぶすこと自体が悪いのではなく、黙って握りつぶすのが悪い。** この区別は、この連載を通じて何度も出てきます。

### Q5. APIキーは、どこに置くのが正解ですか？

R の場合は `~/.Renviron` に1行書くのが標準です。

```
BPS_API_KEY=あなたの鍵
```

こうすると `Sys.getenv("BPS_API_KEY")` で読めます。**スクリプトには一切書かない**ので、スクリプトをそのまま他人に渡したり、GitHub に上げたりしても漏れません。

Python なら `.env` ファイルと `python-dotenv`、あるいは環境変数を直接使います。考え方は同じです。**秘密はコードの外に置く。**

### Q6. 他の罠も知りたいのですが

この記事は連載の3本目です。

1. [一覧APIの暗黙の打ち切り](01-silent-truncation.md)
2. [カタログの二言語混在](02-bilingual-catalog.md)
3. **年の必須指定と3年上限**（この記事）
4. 連結キーの桁数不定 ── キーを「分解しない」設計
5. 派生変数・期間の未指定 ── 全国合計が2倍になる
6. 行は存在するが値が空 ── 可用性は行数で判定してはいけない
7. 静的表の実体がHTML ── Excelと称するファイルが開けない
8. 州名の表記ゆれと脚注記号 ── 地図の一部が灰色になる
9. 行政区画の変更（33→34→38州） ── 足せる量と足せない量
10. 公表基準の断絶（3月調査と9月調査） ── 検算が失敗したとき、何を疑うか

---

## この記事について

この記事は、日本計算機統計学会シンポジウムでの報告「検算駆動による生成AIとの協働 ── インドネシア公的統計WebAPIからの州別パネル構築とShinyによる可視化」の補足資料として作成しました。

本文中のコード、出力、数値は、すべて実際の作業記録に基づいています。うまくいかなかった過程も、そのまま残しています。

### 生成AIの利用について

**本記事の文章は、生成AI（Claude Opus 5、推論設定「高」）が、久保田による指示（プロンプト）と複数回のやりとりを経て生成したものです。** 構成・記述・コード解説はAIによる出力であり、掲載しているコード、コンソール出力、数値は、いずれも実際の作業記録に基づいています。内容は著者が確認したうえで公開しています。

この記事の主題そのものが「生成AIと協働してデータを取得した記録」であるため、記事の生成過程を隠す理由がありません。念のため明記しておきます。

- 著者：久保田貴文（多摩大学）
- 連絡先：kubota@tama.ac.jp
- Web：https://takafumikubota.jp/

この作業で用いた統計は、インドネシア中央統計庁（BPS）が公開するWebAPIから取得しました。
Layanan ini menggunakan API Badan Pusat Statistik (BPS).
（本サービスはインドネシア中央統計庁のAPIを使用しています。）
