# 罠04：連結キーの桁数が固定ではない

> **一行でいうと**
> 桁数が保証されていない文字列を**切り分けよう**とすると、いつか必ず壊れる。
> 切らずに、**組み立てる**ほうを選ぶ。

**この罠の危険度：★★★★☆**
壊れ方が独特です。エラーも出ず、行数も正常で、**値だけが全部 NA になります**。しかも壊れるのは特定の表だけなので、他の表で動作確認していると気づけません。

---

## 目次

1. [前回までのあらすじ](#1-前回までのあらすじ)
2. [予備知識：普通のAPIはどう数値を返すか](#2-予備知識普通のapiはどう数値を返すか)
3. [BPSの返し方は、少し変わっている](#3-bpsの返し方は少し変わっている)
4. [キーの正体を突き止める](#4-キーの正体を突き止める)
5. [素直な発想：文字列を切り分ける](#5-素直な発想文字列を切り分ける)
6. [なぜ切り分けが破綻するのか](#6-なぜ切り分けが破綻するのか)
7. [発想の転換：切らずに、組み立てる](#7-発想の転換切らずに組み立てる)
8. [なぜこれで正しいと言えるのか](#8-なぜこれで正しいと言えるのか)
9. [落とし穴①：変数IDを返してこない表がある](#9-落とし穴変数idを返してこない表がある)
10. [落とし穴②：次元が空のときに1行できてしまう](#10-落とし穴次元が空のときに1行できてしまう)
11. [直ったことをどう確かめたか](#11-直ったことをどう確かめたか)
    - [11.5 この罠だけは、踏む前に避けられた](#115-補足この罠だけは踏む前に避けられた)
12. [あなたのAPIでも同じことが起きる](#12-あなたのapiでも同じことが起きる)
13. [チェックリスト](#13-チェックリスト)
14. [まとめ](#14-まとめ)
15. [付録A：コード全文](#付録aコード全文)
16. [付録B：よくある質問](#付録bよくある質問)

---

## 1. 前回までのあらすじ

- [罠01](01-silent-truncation.md)：カタログが500件で静かに切れていた
- [罠02](02-bilingual-catalog.md)：英語で絞ると、インドネシア語の表題が消えた
- [罠03](03-year-parameter.md)：エラーメッセージから、年の必須指定と3年上限が判明した

ここまでで、**欲しい表に、正しい質問を投げられるようになりました。**

`status: OK` が返ってきます。データも入っています。

**あとは、返ってきたものを読むだけ** ── そこが今回の話です。

---

## 2. 予備知識：普通のAPIはどう数値を返すか

多くのAPIは、数値を**行の配列**で返します。

```json
{
  "data": [
    { "province": "Aceh",           "year": 2018, "value": 5281314 },
    { "province": "Sumatera Utara", "year": 2018, "value": 14415391 },
    { "province": "Sumatera Barat", "year": 2018, "value": 5382000 }
  ]
}
```

この形なら、読むのは簡単です。1行が1つの観測値で、必要な情報がその行に全部入っています。プログラムは上から順に読むだけ。**迷う余地がありません。**

表計算ソフトの1行1行を、そのまま送ってもらっているのと同じです。

---

## 3. BPSの返し方は、少し変わっている

BPS の `data` エンドポイントは、こういう形で返ってきます（実際はもっと長いです）。

```json
{
  "status": "OK",
  "var": [ { "val": 1886, "label": "Population by Province and Sex",
             "unit": "Person" } ],
  "vervar": [ { "val": 1100, "label": "ACEH" },
              { "val": 1200, "label": "SUMATERA UTARA" },
              ... ],
  "turvar": [ { "val": 583, "label": "Male" },
              { "val": 584, "label": "Female" },
              { "val": 585, "label": "Total" } ],
  "tahun":  [ { "val": 118, "label": "2018" },
              { "val": 119, "label": "2019" } ],
  "turtahun": [ { "val": 0, "label": "Annual" } ],
  "datacontent": {
    "110018865831180": 2632694,
    "110018865841180": 2648620,
    "110018865851180": 5281314,
    "120018865831180": 7196123,
    ...
  }
}
```

構造を言葉にすると、こうです。

- **見出しの部分**：地域の一覧（`vervar`）、派生変数の一覧（`turvar`）、年の一覧（`tahun`）、期間の一覧（`turtahun`）が、別々の配列で入っている
- **数値の部分**：`datacontent` という**たった1つのかたまり**に、全部の数値が入っている

`datacontent` は、行の配列ではありません。**「変な文字列 → 数値」という対応表**です。

つまり、地域も年も派生変数も、**あの長い数字の文字列の中に埋め込まれている**わけです。

### なぜこんな形なのか

推測ですが、**通信量を減らすため**でしょう。行の配列で返すと、`"province"` `"year"` `"value"` という項目名が数千回繰り返されます。キーに全部詰め込めば、その繰り返しが消えます。合理的な設計です。

ただし、**読む側の仕事は増えます。**

---

## 4. キーの正体を突き止める

では、`110018865831180` とは何か。

上の応答に出てきた数字と見比べてみます。

| 出てきた数字 | どこにあったか |
|---|---|
| `1100` | `vervar` の val（ACEH） |
| `1886` | `var` の val（変数ID） |
| `583` | `turvar` の val（Male） |
| `118` | `tahun` の val（2018） |
| `0` | `turtahun` の val（Annual） |

並べてみると、

```
1100  +  1886  +  583  +  118  +  0   =  110018865831180
────    ────    ───    ───    ─
地域    変数    派生   年     期間
```

**ぴったり一致します。**

つまりキーの構造は、

> **キー ＝ 地域ID ＋ 変数ID ＋ 派生変数ID ＋ 年ID ＋ 期間ID を、数字のまま連結したもの**

コードのコメントには、実物の例をそのまま残しました。**説明より実例のほうが、後から読む人には速いからです。**

```r
# 【datacontent のキー構造(実物から確定)】
#   キー = vervar + var + turvar + th + turtahun  を数字のまま連結したもの
#
#   例 "630018865841180"
#        6300  … vervar   (地域コード。6300 = Kalimantan Selatan)
#        1886  … var      (変数ID)
#        584   … turvar   (派生変数。583=Male, 584=Female, 585=Total)
#        118   … th       (年ID。118 = 2018)
#        0     … turtahun (期間。0 = Annual)
```

**ここまでは順調です。** 構造が分かったので、あとは読むだけ ── に見えます。

---

## 5. 素直な発想：文字列を切り分ける

構造が分かったなら、次にやることは決まっているように思えます。

> キーを**切り分けて**、それぞれのIDに戻せばいい。

コードにすると、こんな感じです。

```r
# ※ これは動きません。なぜ動かないかを説明するための例です
parse_key <- function(k) {
  list(
    vervar   = substr(k,  1,  4),   # 最初の4桁が地域
    var      = substr(k,  5,  8),   # 次の4桁が変数
    turvar   = substr(k,  9, 11),   # 次の3桁が派生変数
    th       = substr(k, 12, 14),   # 次の3桁が年
    turtahun = substr(k, 15, 15)    # 最後の1桁が期間
  )
}
```

`110018865831180` に当てはめると、

```
1100 | 1886 | 583 | 118 | 0
```

**きれいに分かれます。** 15桁ちょうど。何も問題がないように見えます。

そして実際、**この表では動きます。** ここが罠です。

---

## 6. なぜ切り分けが破綻するのか

問題は、**桁数がどこにも保証されていない**ことです。

上の切り分けは「地域は4桁、変数は4桁、派生変数は3桁、年は3桁、期間は1桁」という前提に立っています。しかし、その前提を誰も約束していません。**たまたま、この表がそうだっただけ**です。

具体的に、崩れる場面を挙げます。

### 崩れ方1：変数IDの桁数が違う

変数IDは、表によって桁数が違います。

| 変数ID | 桁数 |
|---|---|
| `98`（ジニ係数） | **2桁** |
| `195`（貧困線） | **3桁** |
| `1886`（人口） | **4桁** |
| `2790`（人口・新系列） | 4桁 |

変数195の表を、「5〜8文字目が変数ID」という前提で切ると、

```
実際：  1100 + 195 + ... （14桁）
切り方：1100 | 1951 | 18… ← ずれる
```

**1文字ずれた瞬間、以降が全部ずれます。** そして、ずれたキーは `datacontent` に存在しないので、**値が引けません。**

### 崩れ方2：派生変数IDの桁数が違う

`turvar` の値も、表によって違います。人口の表では `583`〜`585`（3桁）ですが、派生変数が無い表では `0`（1桁）です。貧困線の表では別の番号が使われます。

### 崩れ方3：期間IDの桁数が違う

`turtahun` は「年次だけ」の表では `0`（1桁）ですが、3月・9月の2回公表される表では `1`, `2` などが入り、表によっては2桁になります。

### この壊れ方の、いちばん嫌な性質

**エラーが出ません。**

キーがずれると、`datacontent` から値が引けません。引けなかったら、プログラムは「その組合せの値は無い」と判断して `NA`（欠測）を入れます。

結果、こうなります。

| 見え方 | 実際 |
|---|---|
| 行数：正常（例：552行） | 正常 |
| 列：正常（地域名も年も入っている） | 正常 |
| **値：全部 NA** | **キーが1文字ずれていただけ** |

「この表にはデータが無いんだな」と読んでしまいます。[罠01](01-silent-truncation.md)・[罠02](02-bilingual-catalog.md) とまったく同じ、**「無いように見える」タイプの壊れ方**です。

そして最悪なのは、**表によって壊れたり壊れなかったりする**ことです。人口の表（変数1886、4桁）で動作確認して「よし動いた」と思い、貧困線の表（変数195、3桁）に移った瞬間に全部 NA になる。原因が「表が違うこと」だと気づくまでに、相当な時間を失います。

---

## 7. 発想の転換：切らずに、組み立てる

ここで方針を変えました。

> **キーを分解するのをやめる。代わりに、キーを組み立てて引く。**

考え方はこうです。

応答の中には、**その表で使われている地域・派生変数・年・期間の一覧が、すでに入っています**（§3の `vervar`, `turvar`, `tahun`, `turtahun`）。

ということは、**その全部の組合せ**を作れば、`datacontent` に入っているはずのキーが**すべて再現できます**。

```
地域(38通り) × 派生変数(3通り) × 年(3通り) × 期間(1通り) = 342通り
```

この342個のキーを自分で組み立てて、1つずつ `datacontent` から引く。**あるものは値が返り、無いものは NA になる。** それだけです。

### コードにすると

```r
# 応答に入っている各次元の一覧を、そのまま使う
ver <- .bps_dim(res$vervar,   "vervar",   "region")
tvr <- .bps_dim(res$turvar,   "turvar",   "turvar_label")
yr  <- .bps_dim(res$tahun,    "th",       "year_label")
ttr <- .bps_dim(res$turtahun, "turtahun", "turtahun_label")

# 全組合せを作る（直積）
grid <- expand.grid(vervar   = ver$vervar,
                    turvar   = tvr$turvar,
                    th       = yr$th,
                    turtahun = ttr$turtahun,
                    KEEP.OUT.ATTRS   = FALSE,
                    stringsAsFactors = FALSE)

# 組合せからキーを組み立てる
grid$key <- paste0(grid$vervar, varid, grid$turvar, grid$th, grid$turtahun)

# 引く。無ければ NA
dc <- res$datacontent
grid$value <- vapply(grid$key, function(k) {
  v <- dc[[k]]
  if (is.null(v)) NA_real_ else suppressWarnings(as.numeric(v))
}, numeric(1), USE.NAMES = FALSE)
```

`expand.grid()` は「全組合せの表を作る」関数です。エクセルでいうと、地域38行 × 派生変数3列 × … の全マス目を縦に並べたものだと思ってください。

そして、コメントにはこう書き残しました。

```r
# 桁数が固定でないため「キーを切り分ける」のではなく、
# **レスポンスに入っている vervar × turvar × th × turtahun の全組合せから
#   キーを組み立てる**。これなら桁数の曖昧さが原理的に生じない。
```

---

## 8. なぜこれで正しいと言えるのか

ここが、この記事でいちばん伝えたい部分です。

**「たまたま動く」と「原理的に壊れない」は、まったく違います。**

切り分ける方式は、**桁数についての推測**に依存しています。推測が当たっている限り動きますが、外れた瞬間に壊れます。そして、いつ外れるかは分かりません。

組み立てる方式は、**桁数を一度も使いません**。

- 変数IDが2桁でも4桁でも、そのまま連結するだけ
- 派生変数IDが1桁でも3桁でも、そのまま連結するだけ
- 将来 BPS が桁数を変えても、**こちらのコードは1文字も変えなくていい**

つまり、**曖昧さの原因そのものを、設計から取り除いた**わけです。

### 一般化するとこうなります

> **仕様が曖昧な部分があるとき、その曖昧さを「推測して吸収する」のではなく、「触らずに済む設計にする」。**

日常の例でいうと、封筒の中身を、封筒の厚みから推測して当てるのではなく、**送り主が同封してくれた明細書を読む**ようなものです。明細書（＝応答に入っている各次元の一覧）は最初からあったのに、こちらが厚み（＝桁数）で当てようとしていた。

### 副作用として得られたもの

この方式には、おまけがありました。

**「本当に無いのか、キーがずれているのか」が区別できるようになりました。**

組み立て方式なら、キーがずれる可能性がありません。したがって、値が NA だったら、それは**本当にその表にデータが無い**ということです。

この確実性は、後の作業で決定的に効きました。[罠06（行は存在するが値が空）](README.md) で「2010〜2011年の3月データは、行はあるが値が空」と結論できたのは、**キーのずれという可能性を最初から排除できていたから**です。もし切り分け方式のままだったら、「本当に空なのか、自分のキー生成が間違っているのか」を毎回疑わなければなりませんでした。

> **確実な土台を1つ作っておくと、その上で「おかしい」と言い切れるようになる。**

---

## 9. 落とし穴①：変数IDを返してこない表がある

さて、上のコードには `varid` という変数が出てきます。これはどこから来るのか。

素直に書けば、応答の中の `var$val` です。

```r
varid <- as.character(res$var[[1]]$val)
```

**これで動きました。** 人口でも、貧困率でも、カロリーでも。

そして、**貧困線（変数195）で落ちました。**

```
Error in `mutate()`:
! `var_id` must be size 1, not 0.
```

原因を調べると、**この表の応答には `var$val` が入っていなかった**のです。`label` や `unit` はあるのに、`val` だけが欠けている。

### 対処：要求したときの値にフォールバックする

考えてみれば、変数IDは**こちらが指定して要求したもの**です。応答が教えてくれなくても、**こちらは最初から知っています**。

```r
# NULL や長さ0を既定値で置き換える小さな道具
`%||%` <- function(a, b) if (is.null(a) || length(a) == 0) b else a

bps_parse_data <- function(res, var_id = NULL) {
  # 応答の val → 引数の var_id → NA の順に試す
  varid <- as.character(res$var[[1]]$val %||% var_id %||% NA)
  if (is.na(varid)) stop("var_id が特定できません（レスポンスにも引数にも無い）")
  ...
}
```

`%||%` は「左が空なら右を使う」という意味の、2行の道具です。R には標準で入っていないので自作しますが、多くのパッケージが同じものを持っています。

そして、**なぜこれが要るのか**をコメントに書き残しました。

```r
#' @param var_id 要求した変数ID。レスポンスの var$val が欠けている表があるため、
#'   フォールバックとして使う。**datacontent のキー組み立てに必須**なので、
#'   ここが NA になると1件も引けなくなる。
#'   実例: var 195（貧困線）は val が返らず、
#'   「`var_id` must be size 1, not 0」で落ちていた。
```

**実例の表番号とエラーメッセージを、そのまま書いておく。** 半年後に同じエラーを見たとき、検索して一発で見つかるようにするためです。

### この落とし穴の教訓

> **応答に入っているはずの項目が、常に入っているとは限らない。**
> **自分がすでに知っている情報は、応答に頼らない。**

変数IDは、こちらが指定したものです。それを応答から読み直すのは、**丁寧なようでいて、余計な依存を1つ増やしていた**わけです。

---

## 10. 落とし穴②：次元が空のときに1行できてしまう

もう1つ、この設計に埋め込んだ「保険」が、後で別の顔を見せました。

### 保険の中身

応答の次元（`vervar` など）が空だった場合に備えて、既定値で1行作るようにしてあります。

```r
# レスポンスの各次元を data.frame にする(要素が空でも落ちないようにする)
.bps_dim <- function(x, id_col, label_col, default_val = "0", default_label = "") {
  if (is.null(x) || length(x) == 0) {
    out <- data.frame(a = default_val, b = default_label,
                      stringsAsFactors = FALSE)   # ← 空なら1行だけ作る
  } else {
    ...
  }
  names(out) <- c(id_col, label_col)
  out
}
```

これは「期間の区別が無い表では `turtahun` が空で返ってくる」場合などを想定した処理です。空でも落ちずに、`turtahun = "0"` の1通りとして扱えます。

### それが、後で合図になった

貧困線の2010〜2012年を要求したとき、こういう出力が出ました。

```
取得中 [1/6] th=110;111;112 ... 1 行
取得中 [2/6] th=113;114;115 ... 234 行
```

**1行。**

なぜ1行なのか。組み立て方式の仕組みを思い出すと、行数は

```
地域の数 × 派生変数の数 × 年の数 × 期間の数
```

です。これが1になるということは、**全部の次元が1、つまり全部が空だった**ということです。`.bps_dim` の既定値が4つ並んで、1通りだけできた。

つまり、

> **「1行」は「データが1件あった」ではなく、「応答の中身が空だった」という合図。**

これが分かったおかげで、「2010〜2012年は、この表に存在しない」と即断できました。もしこの保険が無く、空のときにエラーで落ちる作りだったら、原因の切り分けに時間がかかったはずです。

**保険が、意図せず診断の道具になった**わけです。ただしこれは幸運でもあるので、コメントに意味を明記しておきました。次に読む人が「1行」を見て正しく解釈できるように。

---

## 11. 直ったことをどう確かめたか

### 確認その1：行数が説明できるか

人口の表（変数1886）を2018〜2020年で取ると、

```
取得中 [1/1] th=118;119;120 ... 342 行
```

342行。内訳は、

```
地域38 × 派生変数3（Male/Female/Total） × 年3 × 期間1 = 342
```

**掛け算が合いました。** 行数が説明できるということは、キーの組み立てが意図どおり動いているということです。

### 確認その2：値が入っているか

```r
summarise(df, n = n(), n_ok = sum(!is.na(value)))
```

キーがずれていれば、`n_ok` は 0 になります。0でなければ、少なくとも引けています。

### 確認その3：合計が公表値と合うか

これが本命の検算です。2020年の人口を、州別に合計して公表値と比べます。

```
274,090,607   ← こちらの合計
270,203,917   ← 公表の全国値
```

**合いませんでした。** 差は約389万人。

ただし、これはキーの問題ではありませんでした。**新設されたパプアの4州が、元の州と二重に数えられていた**のが原因です（[罠09：行政区画の変更](README.md) で扱います）。4州を除くと、

```
270,203,917 = 270,203,917   ← 完全一致
```

**1人単位で一致しました。**

ここまで来て初めて、「キーの組み立ては正しい」と言えます。行数が合っているだけでは足りません。**中身の数字が、外部の基準と一致すること。** それが検算です。

### 確認その4：桁数の違う表でも動くか

いちばん大事な確認です。**変数IDの桁数が違う表で試します。**

| 表 | 変数ID | 桁数 | 結果 |
|---|---|---|---|
| 人口 | 1886 | 4桁 | ✅ |
| 人口（新系列） | 2790 | 4桁 | ✅ |
| 貧困率 | 192 | 3桁 | ✅ |
| 貧困線 | 195 | 3桁 | ✅（`%||%` 追加後） |
| 平均就学年数 | 415 | 3桁 | ✅ |
| 失業率 | 543 | 3桁 | ✅ |
| 一人当たりGRDP | 288 | 3桁 | ✅ |
| 県別貧困線 | 624 | 3桁 | ✅ |

**桁数の違う表を、コードを変えずに全部通せました。** 切り分け方式なら、ここで作り直しが必要だったはずです。

---

## 11.5 補足：この罠だけは、踏む前に避けられた

正直に書いておくと、**この連載の中で、この罠だけは実際には踏んでいません。**

罠01（打ち切り）も罠02（二言語）も罠03（エラーの中身）も、**踏んでから気づきました**。しかしキーの切り分けについては、書く前に「これは危ない」と判断し、最初から組み立て方式を選びました（§9 の変数IDの件は、実際に落ちましたが）。

**なぜ、この1つだけ先に避けられたのか。** 理由ははっきりしています。

> **[罠03](03-year-parameter.md) で、生の応答をそのまま画面に出す道具（`bps_debug`）を作っていたからです。**

エラーの原因を探すために作った道具でしたが、副産物として**応答の全体像が目に入るようになりました**。`datacontent` のキーと、`vervar` や `turvar` の一覧を、**同じ画面で並べて見た**わけです。

並べて見えていれば、次の2つは自然に結びつきます。

- キーは、各次元のIDを連結したものらしい
- **その各次元の一覧は、同じ応答の中に全部入っている**

一覧が手元にあるなら、切り分ける理由がありません。組み立てればいい。**この結論は、生の応答を見た人には、ほとんど自明です。**

逆に言えば、応答を整形された結果でしか見ていなければ、この設計にはたどり着けなかったはずです。整形の過程で「各次元の一覧」は用済みとして捨てられ、手元にはキーと値しか残らないからです。そうなれば、**切り分ける以外の選択肢が見えなくなります。**

### 教訓としてはこうなります

> **原因を探すために作った道具が、次の設計を変えることがある。**

デバッグ用の道具は「問題が起きたときだけ使う応急品」と思われがちですが、実際には**理解を作る道具**です。今回でいえば、`bps_debug` の1本が、罠03を解決し、そのうえで罠04を未然に防ぎました。

### 生成AIとの協働という観点から

もう1点。生成AIと作業していると、**「動くコードが速く出てくる」ことが、かえって設計の検討を飛ばす圧力になります。**

切り分け方式は、書けば動きます。人口の表で試せば通ります。その時点で「できた」と言えてしまう。**動いたものを、わざわざ作り直す判断は、なかなか出てきません。**

そうならなかったのは、コードを書き始める前に、生の応答を見る段階があったからです。

> **設計の判断は、コードが1行も無いうちにしかできない。**
> 動くコードが目の前にあると、人もAIも、それを守る方向に働く。

この連載の他の記事は「壊れてから直した記録」ですが、この記事だけは「**壊れる前に選び直した記録**」です。そして、そうできた条件は、道具を1つ先に作っておいたことでした。

---

## 12. あなたのAPIでも同じことが起きる

「連結キー」という形式は珍しく見えますが、**同じ構造の問題は、どこにでもあります。**

### 同じ形の問題

| 場面 | 危ない前提 |
|---|---|
| 商品コードから分類を取り出す | 「最初の3桁が大分類」 |
| 郵便番号から地域を判定する | 「上3桁で市区町村が決まる」 |
| 学籍番号から入学年度を取る | 「最初の4桁が西暦」 |
| ファイル名から日付を取る | 「先頭8文字が YYYYMMDD」 |
| 統計表のIDから種類を判定する | 「先頭2文字が分野コード」 |

どれも**たいてい動きます**。そして、例外に当たった日に静かに壊れます。

### 見分け方

次の問いに「いいえ」があれば、切り分け方式は危険です。

- [ ] 各部分の桁数は、**仕様書に明記**されているか
- [ ] 桁数が変わらないことが、**保証**されているか
- [ ] 桁数が変わったとき、**エラーで気づける**か

3つ目が特に重要です。**気づけない壊れ方をするなら、その方式は避けるべき**です。

### 代わりにできること

1. **区切り文字があるなら、それで分ける**（`1100-1886-583` のような形）
2. **対応表があるなら、それを引く**（[罠03](03-year-parameter.md) の「対応表はAPIに聞く」と同じ発想）
3. **今回のように、組み立てる**
4. どうしても切り分けるしかないなら、**切った結果が既知の値の集合に入っているかを検査する**（例：切り出した地域コードが、応答の `vervar` 一覧に存在するか）

4番目は妥協案ですが、**少なくとも黙って壊れることは無くなります。**

---

## 13. チェックリスト

### 連結された識別子を扱うとき

- [ ] 各部分の桁数は、仕様で保証されているか
- [ ] 保証されていないなら、**切り分け以外の方法**を検討したか
- [ ] 応答の中に、部分の一覧（マスタ）が入っていないか
- [ ] 切り分けるしかない場合、結果を既知の集合と照合しているか

### 取得したデータを検査するとき

- [ ] **行数を、掛け算で説明できるか**（地域数 × 年数 × …）
- [ ] 値が全部 NA になっていないか（`sum(!is.na(value))` を見る）
- [ ] **桁数の違う対象でも動くか**を、少なくとも1つ試したか
- [ ] 合計を、外部の公表値と突き合わせたか

### 応答の項目を使うとき

- [ ] その項目は、**必ず入っている**保証があるか
- [ ] 入っていなかったときの代替はあるか
- [ ] **自分がすでに知っている情報**を、わざわざ応答から読み直していないか

---

## 14. まとめ

### 何が起きたか

BPS の数値は `datacontent` という1つのかたまりに入っており、キーは「地域＋変数＋派生変数＋年＋期間」を数字のまま連結したものでした。ところが**各部分の桁数は固定ではなく**、変数IDだけでも2桁から4桁までありました。

キーを切り分ける実装は、ある表では動き、別の表では**エラーも出さずに全部 NA** になります。

### どう直したか

**切り分けるのをやめ、組み立てる方式にしました。** 応答に入っている各次元の一覧から全組合せを作り、そこからキーを生成して引きます。桁数を一度も使わないので、**桁数が変わっても壊れません**。

あわせて2つの穴を塞ぎました。

1. **変数IDを返さない表**があった（変数195） → 要求時のIDにフォールバック（`%||%`）
2. **次元が空のとき1行できる**保険 → これが「1行」という異常の合図として役に立った

### 一般化した教訓

> **「たまたま動く」と「原理的に壊れない」は違う。**
> 曖昧さを推測で吸収するのではなく、**触らずに済む設計**を選ぶ。

> **自分がすでに知っている情報は、応答に頼らない。**

そしてもう1つ、この記事の後半で効いてくる話。

> **確実な土台を1つ作っておくと、その上で「おかしい」と言い切れるようになる。**

キーの組み立てが原理的に正しいと分かっているから、値が NA のときに「本当に無い」と断言できます。この確実性が無ければ、後続の罠（行はあるが値が空、公表基準の断絶）は、いつまでも「自分のバグかもしれない」という霧の中に留まっていたはずです。

**検算は、積み上がります。** 下の段が固いほど、上の段で強いことが言えます。

---

## 付録A：コード全文

```r
# =====================================================================
# bps_fetch.R : BPS WebAPI の data レスポンスを縦長に開く
# =====================================================================
# 【datacontent のキー構造(実物から確定)】
#   キー = vervar + var + turvar + th + turtahun  を数字のまま連結したもの
#
#   例 "630018865841180"
#        6300  … vervar   (地域コード。6300 = Kalimantan Selatan)
#        1886  … var      (変数ID)
#        584   … turvar   (派生変数。583=Male, 584=Female, 585=Total)
#        118   … th       (年ID。118 = 2018)
#        0     … turtahun (期間。0 = Annual)
#
# 桁数が固定でないため「キーを切り分ける」のではなく、
# **レスポンスに入っている vervar × turvar × th × turtahun の全組合せから
#   キーを組み立てる**。これなら桁数の曖昧さが原理的に生じない。
# =====================================================================

suppressPackageStartupMessages(library(dplyr))

# ラベルには HTML タグが入ることがある(州行が "<b>ACEH</b>" のように太字指定)。
# 州名の突き合わせに使うので、タグと余分な空白を落としてから使う。
.bps_clean_label <- function(x) {
  x <- gsub("<[^>]*>", "", x)
  x <- gsub("&nbsp;", " ", x, fixed = TRUE)
  trimws(gsub("[[:space:]]+", " ", x))
}

# レスポンスの各次元を data.frame にする(要素が空でも落ちないようにする)
# 空のときは既定値で1行だけ作る。結果として全次元が空だと「1行」になり、
# それが「応答の中身が空だった」という合図になる。
.bps_dim <- function(x, id_col, label_col, default_val = "0", default_label = "") {
  if (is.null(x) || length(x) == 0) {
    out <- data.frame(a = default_val, b = default_label,
                      stringsAsFactors = FALSE)
  } else {
    out <- do.call(rbind, lapply(x, function(e) {
      data.frame(a = as.character(e$val),
                 b = .bps_clean_label(as.character(e$label)),
                 stringsAsFactors = FALSE)
    }))
  }
  names(out) <- c(id_col, label_col)
  out
}

# NULL や長さ0を既定値で置き換える（レスポンスの欠けたフィールド対策）
`%||%` <- function(a, b) if (is.null(a) || length(a) == 0) b else a

#' data レスポンス1件を縦長 data.frame に開く
#'
#' @param var_id 要求した変数ID。レスポンスの var$val が欠けている表があるため、
#'   フォールバックとして使う。**datacontent のキー組み立てに必須**なので、
#'   ここが NA になると1件も引けなくなる。
#'   実例: var 195（貧困線）は val が返らず、
#'   「`var_id` must be size 1, not 0」で落ちていた。
bps_parse_data <- function(res, var_id = NULL) {
  if (!identical(res$status, "OK")) {
    stop("status=", as.character(res$status),
         " message=", as.character(res$message))
  }

  varid <- as.character(res$var[[1]]$val %||% var_id %||% NA)
  if (is.na(varid)) stop("var_id が特定できません（レスポンスにも引数にも無い）")
  unit  <- as.character(res$var[[1]]$unit  %||% "")
  vlab  <- as.character(res$var[[1]]$label %||% "")

  # --- 応答が持っている各次元の一覧 -----------------------------------
  ver <- .bps_dim(res$vervar,   "vervar",   "region")
  tvr <- .bps_dim(res$turvar,   "turvar",   "turvar_label")
  yr  <- .bps_dim(res$tahun,    "th",       "year_label")
  ttr <- .bps_dim(res$turtahun, "turtahun", "turtahun_label")

  # --- 全組合せ（直積）からキーを組み立てる ---------------------------
  grid <- expand.grid(vervar   = ver$vervar,
                      turvar   = tvr$turvar,
                      th       = yr$th,
                      turtahun = ttr$turtahun,
                      KEEP.OUT.ATTRS   = FALSE,
                      stringsAsFactors = FALSE)

  grid$key <- paste0(grid$vervar, varid, grid$turvar, grid$th, grid$turtahun)

  dc <- res$datacontent
  grid$value <- vapply(grid$key, function(k) {
    v <- dc[[k]]
    if (is.null(v)) NA_real_ else suppressWarnings(as.numeric(v))
  }, numeric(1), USE.NAMES = FALSE)

  # --- ラベルを貼り直して縦長にする -----------------------------------
  out <- grid |>
    left_join(ver, by = "vervar") |>
    left_join(tvr, by = "turvar") |>
    left_join(yr,  by = "th") |>
    left_join(ttr, by = "turtahun") |>
    mutate(var_id    = varid,
           var_label = vlab,
           unit      = unit,
           year      = suppressWarnings(as.integer(year_label))) |>
    select(var_id, var_label, unit,
           vervar, region, turvar, turvar_label,
           year, turtahun, turtahun_label, value)

  out
}
```

### 使い方と検算

```r
r <- bps_request("list", "model", "data", "lang", "eng",
                 "domain", "0000", "var", 1886, "th", "118;119;120")
d <- bps_parse_data(r, var_id = 1886)

# 行数が掛け算で説明できるか
nrow(d)                                   # 342
length(unique(d$vervar)) *
  length(unique(d$turvar)) *
  length(unique(d$th)) *
  length(unique(d$turtahun))              # 38 * 3 * 3 * 1 = 342  ← 一致

# 値が入っているか（全部 NA ならキーがずれている）
sum(!is.na(d$value))
```

---

## 付録B：よくある質問

### Q1. `expand.grid` で全組合せを作ると、行が増えすぎませんか？

増えます。しかし、実用上は問題になりませんでした。県別の表でも `552地域 × 1 × 3年 × 1 = 1,656行` 程度です。

もし地域が数万あるようなAPIなら、`datacontent` のキーを直接走査して、そこから逆に情報を取り出す方法もあります。ただしその場合も、**切り分けるのではなく「一覧と照合する」**（切り出した文字列が既知のIDと前方一致するか）という形にすべきです。

### Q2. 桁数を、応答から動的に決めればよいのでは？

たとえば「`vervar` 一覧の値の最大桁数を見て、それを使う」という方法です。**理屈は通りますが、脆いです。**

- 同じ表の中でも、地域コードの桁数が揃っている保証がない
- 前ゼロ（`0100` と `100`）の扱いで事故が起きる
- そもそも、**桁数を使わずに済む方法があるのに、わざわざ使う理由がない**

### Q3. 「たまたま動く」実装を、どうやって見分けるのですか？

**1つだけ、確実な質問があります。**

> **この実装が壊れるとしたら、どういう入力のときか。それを1つ挙げられるか。**

挙げられなければ、まだ理解していない可能性があります。今回の場合、「変数IDが3桁の表」という具体例を挙げられた時点で、切り分け方式は捨てるべきだと決まりました。

**壊れる条件を1つも思いつかない実装より、壊れる条件が分かっていて、それを回避してある実装のほうが安全です。**

### Q4. 他の罠も知りたいのですが

この記事は連載の4本目です。

1. [一覧APIの暗黙の打ち切り](01-silent-truncation.md)
2. [カタログの二言語混在](02-bilingual-catalog.md)
3. [年の必須指定と3年上限](03-year-parameter.md)
4. **連結キーの桁数不定**（この記事）
5. 派生変数・期間の未指定 ── 全国合計が2倍になる
6. 行は存在するが値が空 ── 可用性は行数で判定してはいけない
7. 静的表の実体がHTML ── Excelと称するファイルが開けない
8. 州名の表記ゆれと脚注記号 ── 地図の一部が灰色になる
9. 行政区画の変更（33→34→38州） ── 足せる量と足せない量
10. 公表基準の断絶（3月調査と9月調査） ── 検算が失敗したとき、何を疑うか

---

## この記事について

この記事は、日本計算機統計学会シンポジウムでの報告「検算駆動による生成AIとの協働 ── インドネシア公的統計WebAPIからの州別パネル構築とShinyによる可視化」の補足資料として作成しました。

本文中のコード、出力、数値は、すべて実際の作業記録に基づいています。うまくいかなかった過程も、そのまま残しています。

### 生成AIの利用について

**本記事の文章は、生成AI（Claude Opus 5、推論設定「高」）が、久保田による指示（プロンプト）と複数回のやりとりを経て生成したものです。** 構成・記述・コード解説はAIによる出力であり、掲載しているコード、コンソール出力、数値は、いずれも実際の作業記録に基づいています。内容は著者が確認したうえで公開しています。

この記事の主題そのものが「生成AIと協働してデータを取得した記録」であるため、記事の生成過程を隠す理由がありません。念のため明記しておきます。

- 著者：久保田貴文（多摩大学）
- 連絡先：kubota@tama.ac.jp
- Web：https://takafumikubota.jp/

この作業で用いた統計は、インドネシア中央統計庁（BPS）が公開するWebAPIから取得しました。
Layanan ini menggunakan API Badan Pusat Statistik (BPS).
（本サービスはインドネシア中央統計庁のAPIを使用しています。）
