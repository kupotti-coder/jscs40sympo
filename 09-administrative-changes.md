# 罠09：行政区画が変わる ── 足せる量と、足せない量

> **一行でいうと**
> 地域が分割されたとき、**足せる量**（人口）と**足せない量**（率・平均）では、
> まとめ方がまったく違う。取り違えても計算は成功し、**値だけが静かに壊れる**。

**この罠の危険度：★★★★★（最高）**
理由は3つ。エラーが出ません。桁も変わりません。そして、**間違った値のほうが「それらしい」**ことがあります。

---

## 目次

1. [前回までのあらすじ](#1-前回までのあらすじ)
2. [インドネシアの州は、増え続けている](#2-インドネシアの州は増え続けている)
3. [「分割しか起きない」という事実が効く](#3-分割しか起きないという事実が効く)
4. [なぜ33州に揃えるのか](#4-なぜ33州に揃えるのか)
5. [二重計上の発見 ── 389万人の差](#5-二重計上の発見--389万人の差)
6. [足せる量と、足せない量](#6-足せる量と足せない量)
7. [なぜ取り違えても気づけないのか](#7-なぜ取り違えても気づけないのか)
8. [実装：2種類の畳み込み](#8-実装2種類の畳み込み)
9. [重みの落とし穴①：畳み込む前の粒度で要る](#9-重みの落とし穴畳み込む前の粒度で要る)
10. [重みの落とし穴②：統合が起きない行には、重みは要らない](#10-重みの落とし穴統合が起きない行には重みは要らない)
11. [重みの落とし穴③：1つ欠けたら、群ごと落とす](#11-重みの落とし穴1つ欠けたら群ごと落とす)
12. [重みの落とし穴④：重みは「比」だけが効く](#12-重みの落とし穴重みは比だけが効く)
13. [直ったことをどう確かめたか](#13-直ったことをどう確かめたか)
14. [あなたのデータでも同じことが起きる](#14-あなたのデータでも同じことが起きる)
15. [チェックリスト](#15-チェックリスト)
16. [まとめ](#16-まとめ)
17. [付録A：コード全文](#付録aコード全文)
18. [付録B：よくある質問](#付録bよくある質問)

---

## 1. 前回までのあらすじ

- [罠01](01-silent-truncation.md)〜[罠03](03-year-parameter.md)：カタログと通信
- [罠04](04-composite-key.md)〜[罠07](07-static-table-html.md)：数値の読み取り
- [罠08](08-name-variants.md)：州名の表記ゆれ

ここまでで、**州別の数値が正しく取れ、地図とも結合できる**ようになりました。

残る問題は1つだけです。**州の数が、年によって違う。**

---

## 2. インドネシアの州は、増え続けている

まず事実から。

| 時点 | 州の数 | 何が起きたか |
|---|---|---|
| 2010年 | **33** | 出発点 |
| 2012年 | **34** | 北カリマンタン州が、東カリマンタン州から分離 |
| 2022年 | **38** | パプアが4州に、西パプアが2州に分割 |

2022年の分割は大きいものでした。

```
Papua       → Papua + Papua Selatan + Papua Tengah + Papua Pegunungan
Papua Barat → Papua Barat + Papua Barat Daya
```

**1つの州が4つになり、もう1つが2つになった。**

つまり、16年分のデータを並べようとすると、

```
2010-2011年 : 33州
2012-2021年 : 34州
2022-2025年 : 38州
```

**同じ「州別データ」なのに、単位が3回変わっている**わけです。

これをそのまま並べると、2022年から突然「州の数が増えた」ように見えます。人口の合計は変わらないのに、1州あたりの平均人口は減ります。**時系列として比較できません。**

---

## 3. 「分割しか起きない」という事実が効く

ここで、非常に重要な性質があります。

> **インドネシアの州は、分割のみで増えてきた。統合の実績がない。**

これは調べて確認した事実です。そして、この一方向性が**解き方を決めます**。

### 細かい → 粗い は復元できる

38州のデータがあれば、33州の値は作れます。子州を親州にまとめ直せばいいからです。

```
Papua + Papua Selatan + Papua Tengah + Papua Pegunungan  →  Papua（旧）
```

**足すだけです。情報の損失はありますが、正確です。**

### 粗い → 細かい は復元できない

逆はできません。33州時代の `Papua` の人口が分かっても、それを4つにどう配分すればいいかは**データからは決まりません**。

按分するには何らかの仮定（面積比、後年の人口比…）が要りますが、**仮定を置いた瞬間に、それは推定値であって観測値ではなくなります**。

### したがって

> **パネルを作るなら、最も粗い構成（33州）に揃えるのが唯一の厳密解。**

この判断は、コードの冒頭にそのまま書きました。

```r
# インドネシアの州は「分割」のみで増えてきた(統合の実績はない)。
# したがって:
#   ・細かい構成 → 粗い構成 は、子州を親州にまとめれば復元できる
#   ・粗い構成 → 細かい構成 は、按分の仮定なしには復元できない
# よってパネルは「最も粗い構成(33州)に揃える」のが唯一の厳密解。
```

---

## 4. なぜ33州に揃えるのか

「34州でもいいのでは？」と思うかもしれません。2012年以降なら34州がそろっています。

実際、34州版も作れるようにしてあります。

```r
# 33州構成に畳むための対応(子 → 33州時点の親)
to33 <- c(
  "Kalimantan Utara"  = "Kalimantan Timur",
  "Papua Selatan"     = "Papua",
  "Papua Tengah"      = "Papua",
  "Papua Pegunungan"  = "Papua",
  "Papua Barat Daya"  = "Papua Barat"
)

# 34州構成に畳むための対応(Kalimantan Utara は独立のまま残す)
to34 <- c(
  "Papua Selatan"     = "Papua",
  "Papua Tengah"      = "Papua",
  "Papua Pegunungan"  = "Papua",
  "Papua Barat Daya"  = "Papua Barat"
)
```

**選んだのは33州です。** 理由は、**分析に使う年が2011年だから**でした。

2011年時点では、北カリマンタン州はまだ存在しません。34州に揃えると、2010・2011年が使えなくなります。**本命の年を落としてまで、州を1つ増やす理由はありません。**

> **「どこに揃えるか」は、データの都合ではなく、分析の目的で決まる。**

これは[罠05](05-derived-dimensions.md) の「選択肢が2つ以上あるなら、それは判断であって人間の仕事」と同じです。

---

## 5. 二重計上の発見 ── 389万人の差

33州に畳む処理を書いて、いつもの検算をしました。**州別の人口を合計して、公表の全国人口と比べる。**

```
こちらの合計 : 274,090,607
公表の全国値 : 270,203,917
```

**389万人ちがう。**

差は1.4%。桁は合っています。「四捨五入かな」で済ませられる大きさではありませんが、「明らかにおかしい」とも言い切れない、**微妙な差**です。

### 原因

差の正体は、**新設された4州が、元の州と二重に数えられていた**ことでした。

2020年のデータには、38州の枠が用意されています。ところが 2020年時点では、パプアの4州はまだ分割前です。BPS の表では、

- `Papua` に、分割前の全体の値が入っている
- `Papua Selatan` など新設4州にも、**分割後の想定値**が入っている

つまり、**同じ人が2回数えられている**わけです。

### 確かめ方

新設4州を除いて合計し直しました。

```
270,203,917 = 270,203,917
```

**1人単位で完全一致。**

「だいたい合った」ではなく「ぴったり合った」。これが決定的でした。仮説（二重計上）が正しいことの、これ以上ない証拠です。

> **検算は「近い」より「一致する」ほうが、はるかに強い証拠になる。**

差が389万人あったとき、思いつく仮説は複数ありました。欠測、単位、集計範囲、丸め…。しかし「4州を除いたら1人単位で一致する」なら、**他の仮説はすべて消えます**。

### この罠が[罠05](05-derived-dimensions.md)と違う点

[罠05](05-derived-dimensions.md) の二重計上（男＋女＋計）は、**ちょうど2倍**になりました。分かりやすい。

今回は**1.4%**です。パプアは人口の少ない地域なので、二重に数えても全体への影響が小さい。**目立たないぶん、こちらのほうが危険です。**

そして、[罠05](05-derived-dimensions.md) で入れた不変量（1州×1年＝1行）は、**この罠を捕まえません**。重複しているのは行ではなく、**値の中身**だからです。`Papua` の行も `Papua Selatan` の行も、それぞれ1本ずつしかありません。

> **不変量は、それが見ている次元でしか守れない。**（[罠08](08-name-variants.md) と同じ教訓）

捕まえたのは、**外部の値（公表の全国人口）との照合**でした。

---

## 6. 足せる量と、足せない量

さて、ここからがこの記事の中心です。

畳み込みの方法は、**量の性格によって変わります**。

### 示量（extensive）── 足せる量

| 例 | 性質 |
|---|---|
| 人口 | 州を合わせれば、人数は足し算 |
| 世帯数 | 同上 |
| 貧困者数 | 同上 |
| GRDP の総額 | 同上 |
| 道路の総延長 | 同上 |

これらは、**「全体は部分の和」**という性質を持ちます。物理でいう示量変数（体積、質量）と同じです。

```
Papua（旧） = Papua + Papua Selatan + Papua Tengah + Papua Pegunungan
```

**単純に足すだけ。近似も仮定も要りません。厳密です。**

### 示強（intensive）── 足せない量

| 例 | 性質 |
|---|---|
| 貧困率 | 割合。足すと100%を超える |
| 平均就学年数 | 平均。足すと意味不明 |
| 一人当たりGRDP | 比率 |
| 貧困線 | 金額だが、代表値 |
| 都市人口比率 | 割合 |

これらは、**足してはいけません**。物理でいう示強変数（温度、密度）と同じです。

40℃の水と20℃の水を混ぜても、60℃にはなりません。**量で重みを付けた平均**になります。

```
貧困率（旧Papua）= Σ(各州の貧困率 × 各州の人口) / Σ(各州の人口)
```

**人口で重みを付けた平均。** これが正しい畳み方です。

### 「足すか、加重平均か」

この2つを見分けるのは、実は簡単です。

> **その量に「1人あたり」「率」「平均」「あたり」が付いているか。**

付いていれば示強。付いていなければ示量。

もう1つの見分け方は、

> **2つの同じ地域を合体させたら、値はどうなるべきか。**

- 人口100万の州を2つ合体 → **200万**（足す）
- 貧困率10%の州を2つ合体 → **10%**（変わらない）

**「変わらないはず」なら示強**です。

---

## 7. なぜ取り違えても気づけないのか

ここが罠の本体です。

**率を単純に足しても、プログラムは何も言いません。**

```r
# 誤り：率を足している
Papua_旧 = 22.5 + 18.3 + 30.1 + 25.7 = 96.6
```

貧困率96.6%。**数字としては成立してしまいます。**

しかも、**桁は変わりません**。10%が96%になるだけで、「％の値」としては読めてしまう。[罠05](05-derived-dimensions.md) の「ちょうど2倍」のような、はっきりした手がかりがありません。

さらに悪いことに、

- **地図の色の相対関係は保たれます**（畳み込まれた州だけが濃くなる）
- 33州すべてがそろっているので、**州数の検査は通ります**
- 欠測もありません

**「パプアの貧困率が異常に高い」という絵は、実はそれらしく見えてしまう**のです。パプアは実際に貧困率が高い地域なので、**間違った値が、事前の予想と整合してしまいます**。

> **間違った値が、事前の予想と整合するとき、それがいちばん危ない。**

だからコードの冒頭に、この注意を最初に書きました。

```r
# 【示量と示強を絶対に混同しないこと】
#   人口・世帯数・貧困者数 … 足せる → collapse_prov(how = "sum")
#   貧困率・平均支出・HDI  … 足せない → collapse_with_pop()（人口重み付き平均）
#   率を単純合算すると、州数ぶん膨らんだ意味の無い数字になる。
```

---

## 8. 実装：2種類の畳み込み

畳み込みの関数は、**どちらの方法かを引数で明示的に指定させる**ようにしました。

```r
#' 州構成を畳み込む
#'
#' @param to   33 または 34。38 を渡すと何もしない
#' @param how  "sum"(人口など足せる量) / "wmean"(率・平均。weight 必須)
#' @param weight  how="wmean" のときの重み列名(通常は人口)
collapse_prov <- function(df, to = 33, how = c("sum", "wmean"), weight = NULL) {
  how <- match.arg(how)
  ...
  if (how == "sum") {
    out <- grp |> summarise(value = sum(value), .groups = "drop")
  } else {
    if (is.null(weight)) stop("how='wmean' には weight 列名が必要")
    out <- grp |>
      summarise(value = sum(value * .data[[weight]]) / sum(.data[[weight]]),
                .groups = "drop")
  }
  out
}
```

**既定値を置きませんでした。**（正確には `match.arg` の先頭が `"sum"` ですが、呼び出し側では必ず明示しています。）

理由は[罠05](05-derived-dimensions.md) と同じです。**どちらを使うかは判断であり、判断は明示されるべき**だからです。既定値があると、指定を忘れても動いてしまいます。

そして `how = "wmean"` で `weight` を渡し忘れたら、**止まります**。加重平均を重みなしで計算する方法はないので、これは黙って通してはいけません。

---

## 9. 重みの落とし穴①：畳み込む前の粒度で要る

ここから、実装で実際に踏んだ落とし穴を4つ書きます。

**1つ目。重みは、畳み込む「前」の粒度で必要です。**

当たり前に聞こえますが、間違えやすい。2020年のパプアを畳むには、

```
Papua, Papua Selatan, Papua Tengah, Papua Pegunungan
```

**この4州それぞれの人口**が要ります。33州に畳んだあとの `Papua`（合計）ではありません。

つまり、**重み用の人口は、指標と同じ粒度で用意する必要がある**わけです。

しかし人口自体も、年によって粒度が違います。そこで、**「公表そのままの粒度」の人口**を作る関数を書きました。

```r
#' 公表そのままの粒度の人口（重み用）を作る
#'
#'   2010-2014 … 出来上がっている33州パネル（この期間の指標も33州構成なので、
#'               畳み込みが不要＝重みも33州で足りる）
#'   2015-2019 … var 1886（34州・千人単位）
#'   2020-2025 … var 2790（38州・人単位）
build_pop_weights <- function(years = 2010:2025, ...) { ... }
```

**3つの期間で、3つの異なる出所を使っています。** それぞれの期間で「その時代の公表粒度」に合わせるためです。

`unit_mult = 1000` の違いにも注意が要ります。var 1886 は千人単位、var 2790 は人単位です。**混ぜると1000倍ずれます。**

（ただし、後述するとおり**重みは比しか効かない**ので、この場合は結果に影響しません。とはいえ、混乱のもとなので揃えました。）

---

## 10. 重みの落とし穴②：統合が起きない行には、重みは要らない

**2つ目。これが最初の失敗でした。**

素朴に書くと、こうなります。

```r
# 素朴な実装（問題あり）
df |>
  left_join(pop, by = c("prov_name", "year")) |>
  filter(!is.na(pop)) |>                          # ← 重みが無い行を落とす
  collapse_prov(to = 33, how = "wmean", weight = "pop")
```

一見、正しそうです。しかし、これだと**重みが無い年が丸ごと消えます**。

カロリー摂取量の表を思い出してください（[罠07](07-static-table-html.md)）。この表は **2007年から**あります。しかし人口の重みは、2010年からしか作っていません。

2007〜2009年は、**そもそも33州構成でしか公表されていない**ので、**畳み込みが起きません**。統合しないなら、重みは要らないはずです。

しかし上の実装だと、重みが無いので `filter(!is.na(pop))` で落ちます。**3年分が、理由もなく消えます。**

### 直し方：統合が起きる行だけ、重みを要求する

```r
# 統合が起きるかどうかで、行を2つに分ける
grp_cols <- intersect(c("indicator", "year", ".target"), names(df))
df <- df |> group_by(across(all_of(grp_cols))) |> mutate(.k = n()) |> ungroup()

# (a) 統合が起きない行 … 重み不要。そのまま通す
pass <- df |>
  filter(.k == 1) |>
  mutate(prov_name = .target) |>
  select(-.target, -.k)

# (b) 統合が起きる行 … 人口重みが必要
merge_rows <- df |> filter(.k > 1)
```

`.k` は「この畳み込み先に、いくつの州がまとまるか」です。

- `.k == 1` … 1対1。**名前を付け替えるだけ。重みは不要**
- `.k > 1` … 複数がまとまる。**重みが必要**

コメントにも理由を残しました。

```r
#' 重みが要るのは **実際に統合が起きる州×年だけ**。
#' たとえば 2007-2012 のように、そもそも 33州構成でしか公表されていない年は
#' 統合が起きないので重みは不要（そのまま通す）。
#' 重みが無い年を一律に落とすと、古い年が丸ごと消える。
```

> **「必要な条件」を、必要な範囲にだけ課す。**
> 一律に課すと、条件が不要な部分まで巻き添えで落ちる。

---

## 11. 重みの落とし穴③：1つ欠けたら、群ごと落とす

**3つ目。これはもっと微妙で、危険です。**

平均就学年数（MYS）を取ったとき、2013年と2014年だけ**32州**になりました。33のはずが32。

原因は、**北カリマンタン州の重みが無かった**ことです。

MYS の表は2013年から北カリマンタン州を別掲していました。しかし重み用の人口は、**2015年からしか北カリマンタン州を別掲していません**（var 1886 の収録年の都合）。

つまり2013・2014年について、

```
Kalimantan Timur : 値あり、重みあり
Kalimantan Utara : 値あり、重み **なし**
```

### ここで素朴に「重みの無い行を落とす」と何が起きるか

北カリマンタン州の行が消え、東カリマンタン州だけが残ります。そして畳み込むと、

```
Kalimantan Timur（33州版） = Kalimantan Timur（34州版）の値そのまま
```

**北カリマンタン州の情報が、無かったことになります。**

しかも、**エラーも警告も出ません**。33州のうち1州の値が、本来より偏った値になるだけです。

**これは、州が丸ごと消えるより危険です。** 消えれば気づきますが、これは「それらしい値が残る」からです。

### 直し方：群ごと落として、報告する

```r
# 【重要】統合グループの一部だけ重みが無い場合、その州だけ落とすと
# 「片方の値がそのまま親州の値になる」という静かな誤りになる。
#   例: 2013年の Kalimantan Utara に重みが無いと、
#       Kalimantan Timur の値がそのまま33州版の値として残ってしまう。
# そうならないよう **グループ全体を落として報告する**。
# 結果として check_panel の州数が減るので、見れば気づける。
bad_grp <- joined |>
  group_by(across(all_of(...))) |>
  summarise(any_missing = any(is.na(pop)), .groups = "drop") |>
  filter(any_missing)

if (nrow(bad_grp) > 0) {
  cat("[!] 重みが欠けているため統合できなかったグループ（丸ごと落とします）:\n")
  print(as.data.frame(head(bad_grp, 20)))
  joined <- joined |> anti_join(bad_grp, by = ...)
}
```

**わざと、より大きく壊します。**

- 「片方だけ落とす」→ 33州そろうが、値が静かに間違う
- 「群ごと落とす」→ **32州になる。検査に引っかかる**

32州になれば、`check_panel()` の出力を見た瞬間に気づきます。実際、そうやって気づきました。

> **静かに間違うくらいなら、はっきり壊れたほうがいい。**

これは、この連載を通じていちばん逆説的な設計判断でした。**「動くようにする」より「壊れたと分かるようにする」ほうを選ぶ**場面があります。

---

## 12. 重みの落とし穴④：重みは「比」だけが効く

**4つ目。落とし穴というより、解決の鍵になった性質です。**

32州問題を直すには、2013・2014年の北カリマンタン州の重みが要ります。しかし、その年の人口は公表されていません。

**推定するしかないのか。** ここで、加重平均の性質を思い出します。

```
加重平均 = Σ(値 × 重み) / Σ(重み)
```

**重みを全部2倍しても、結果は変わりません。** 分子と分母で打ち消し合うからです。

つまり、**重みは絶対値ではなく「比」だけが効きます**。

「北カリマンタン州の人口が正確に何人か」は要りません。**「東カリマンタンと北カリマンタンの人口比がおよそいくつか」**さえ分かれば十分です。

### 後方補完

そこで、**最も古い年の人口比を、それ以前の年に当てる**ことにしました。

```r
#' 子州の重みを、古い年へ按分で補完する
#'
#' 重みは **相対比しか効かない**（加重平均の分母と分子で打ち消し合う）ので、
#' 「最も古い年の子州シェアを、それ以前の年の親州人口に当てる」近似で十分。
#' 値そのものを推定するわけではないので、誤差の影響は小さい。
extend_weights_backward <- function(w) {
  for (child in names(to33)) {
    parent <- unname(to33[child])
    y0 <- min(w$year[w$prov_name == child])       # 子州が現れる最初の年
    need <- setdiff(親の年[親の年 < y0], 子の年)   # 補完が要る年

    pc <- w$pop[w$prov_name == child  & w$year == y0]
    pp <- w$pop[w$prov_name == parent & w$year == y0]
    share <- pc / (pc + pp)                        # y0 時点のシェア

    # そのシェアを、それ以前の年の親州人口に当てる
    ...
    cat("[i] 重みを後方補完:", child, "→", ..., 
        sprintf("(%s のシェア %.3f を使用)", y0, share), "\n")
  }
}
```

実行すると、こう出ます。

```
[i] 重みを後方補完: Kalimantan Utara → 2013-2014 (2015 のシェア 0.187 を使用)
```

**何を、いつのシェアで補完したかを、必ず表示します。** これは推定を含む処理なので、黙って行ってはいけません。

### なぜこの近似が許されるか

3つの理由を、はっきりさせておきます。

1. **重みは比しか効かない**（上述）
2. **2年しか遡らない**。人口比が2年で大きく変わることはない
3. **重みの誤差は、結果への影響が小さい**。加重平均の値そのものを推定しているわけではなく、2つの値をどう混ぜるかを決めているだけ

3番目を補足します。仮にシェアが 0.187 ではなく 0.20 だったとしても、加重平均の結果は数%も動きません。**一方、シェアを使わなければ、その年のデータは丸ごと失われます。**

> **完全な値が無いとき、「近似して使う」か「使わない」かは、近似の誤差と、失うものの大きさを比べて決める。**

そして、**近似したことは記録する**。CSVには出典を書き、コンソールには補完の内容を出しました。

---

## 13. 直ったことをどう確かめたか

### 確認その1：人口の合計が公表値と一致するか（示量）

```
2010年 : 237,641,326  =  237,641,326  （公表値）
2020年 : 270,203,917  =  270,203,917  （公表値）
```

**2つの国勢調査年で、1人単位の完全一致。**

示量の畳み込み（単純和）が正しいことの、決定的な証拠です。

### 確認その2：州数が全年33か

```
   year n_prov n_rows      total n_na
1  2010     33     33  237641326    0
...
16 2025     33     33  284438782    0
```

**16年すべてで33州。** 32州問題も解消しています。

### 確認その3：率の全国加重平均が、公表値の近くにあるか（示強）

示強は合計に意味が無いので、**人口加重平均**を公表値と比べます。

| 指標 | 年 | こちら | BPS公表 |
|---|---|---|---|
| 平均就学年数 | 2018 | **8.170** | 8.17 |
| 失業率（2月） | 2018 | 5.20 | 5.13 |
| 都市人口比率 | 2010 | 49.81% | 49.8%（SP2010） |

**ほぼ一致。**

ここで大事なのは、**完全一致は期待していない**ことです。BPS の全国値は個票から直接推計されており、州別値の加重平均とは厳密には一致しません。コードにもそう書きました。

```r
# 率は「州の合計」に意味が無いので、人口加重平均を全国公表値と比べる。
# 一致しなくても構わない（BPS の全国値は個票からの直接推計なので、
# 州別値の加重平均とは厳密には一致しない）。**桁と水準が合うか**を見る。
```

> **検算ごとに、「どこまで一致すべきか」を先に決めておく。**

示量なら完全一致を要求します。示強なら「桁と水準」で判断します。この基準を先に決めておかないと、0.07 の差を見て慌てることになります。

### 確認その4：参照リストとの差分

```r
diff_prov(pl33, PROV33)
```

```
$データにあって参照に無い
character(0)
$参照にあってデータに無い
character(0)
```

**過不足ゼロ。**

---

## 14. あなたのデータでも同じことが起きる

### 行政区画は、どこの国でも変わる

| 国・地域 | 変化 |
|---|---|
| 日本 | 平成の大合併（1999-2010年、3,232 → 1,727市町村） |
| 日本 | 政令指定都市の追加、区の再編 |
| ドイツ | 東西統一（1990年） |
| 旧ソ連・旧ユーゴ | 国家の分裂 |
| イギリス | ローカル・オーソリティの再編 |

**日本の場合は、インドネシアと逆で「合併」が主**です。この場合は、

- 粗い（合併後）→ 細かい（合併前）は復元できない
- **細かい（合併前）→ 粗い（合併後）は復元できる**

方向は逆ですが、**原理は同じ**です。**一方向にしか変換できない**ので、**変換できる側に揃える**。日本なら「最新の市町村」に揃えることになります。

### 示量・示強の区別は、どこでも効く

| 示量（足せる） | 示強（足せない） |
|---|---|
| 人口、世帯数 | 人口密度、世帯人員 |
| 売上高、従業員数 | 利益率、一人当たり売上 |
| 患者数、死亡数 | 罹患率、致死率 |
| 総生産額 | 一人当たりGDP、生産性 |
| 面積 | 平均標高 |

**右側を単純に足したら、必ず間違いです。**

そして、右側を正しくまとめるには**重み**が要ります。何を重みにするかは、指標によって変わります。

- 貧困率 → **人口**
- 一人当たりGDP → **人口**
- 平均賃金 → **労働者数**（人口ではない）
- 農作物の平均単収 → **作付面積**（人口ではない）

> **重みは「その平均が、何あたりの平均か」で決まる。**

「1人あたり」なら人口、「1ヘクタールあたり」なら面積です。**指標の単位を見れば、重みは決まります。**

---

## 15. チェックリスト

### 地域の構成が変わるデータを扱うとき

- [ ] 変化は**分割**か**合併**か。方向を確認したか
- [ ] **どちらに揃えれば復元可能**かを確認したか
- [ ] 揃え先は、**分析の目的**から決めたか（データの都合ではなく）
- [ ] 新設地域が、元の地域と**二重に入っていない**か

### 畳み込むとき

- [ ] その指標は**示量**か**示強**か
- [ ] 示強なら、**重みは何か**（単位から決まる）
- [ ] 重みは**畳み込む前の粒度**で用意したか
- [ ] 統合が起きない行に、不要な条件を課していないか
- [ ] 統合グループの**一部だけ**が欠けたとき、どうなるか確認したか

### 検算

- [ ] 示量なら、合計を公表値と**完全一致**で照合したか
- [ ] 示強なら、加重平均を公表値と**水準で**照合したか
- [ ] **どこまで一致すべきか**を、検算の前に決めたか
- [ ] 全年で地域数が一定か
- [ ] 参照リストとの差分は両方向でゼロか

---

## 16. まとめ

### 何が起きたか

インドネシアの州は 33 → 34 → 38 と増えました。分割のみで統合がないため、**最も粗い33州に揃える**のが唯一の厳密解です。

畳み込むとき、新設4州が元の州と**二重計上**され、全国人口が389万人多く出ました。除くと**1人単位で完全一致**しました。

そして本題は、**足せる量と足せない量の区別**です。率を単純に足すと、貧困率96.6%のような値ができますが、**エラーは出ず、桁も変わらず、地図もそれらしく見えます**。

### どう直したか

1. **示量は和、示強は人口加重平均**。関数の引数で明示的に指定させる
2. 重みは**畳み込む前の粒度**で作る（期間ごとに出所を変えて）
3. **統合が起きない行には、重みを要求しない**（古い年が消えるのを防ぐ）
4. **統合グループの一部だけ欠けたら、群ごと落として報告**（静かな誤りを防ぐ）
5. 重みは**比しか効かない**性質を使い、**後方補完**で古い年を救う

### 一般化した教訓

> **間違った値が、事前の予想と整合するとき、それがいちばん危ない。**
> パプアの貧困率が異常に高くても、「そういうものだ」と思えてしまう。

> **静かに間違うくらいなら、はっきり壊れたほうがいい。**
> 32州になれば検査に引っかかる。33州のまま値が歪むと、誰も気づけない。

> **検算ごとに、「どこまで一致すべきか」を先に決めておく。**
> 示量は完全一致、示強は水準。基準が無いと、0.07 の差で慌てる。

> **重みは「その平均が、何あたりの平均か」で決まる。**

### 生成AIとの協働という観点から

この罠は、**AIが最も得意な部分と、最も苦手な部分が同居していました**。

**得意だった部分**：示量・示強の区別、加重平均の実装、重みの粒度合わせ、群ごと落とす処理の実装。これらは知識と実装の問題で、指示すれば正確に書けます。

**苦手だった部分**：**「32州になっている」という出力を、異常として扱うこと**でした。

32州という数字は、コンソールに出ていました。しかし前後には大量の出力が流れており、**AIはそれを「そういう結果」として次に進もうとしました**。止めたのは人間です。

> **数字が出ていることと、その数字が読まれていることは、別のことである。**

[罠08](08-name-variants.md) の「灰色に気づいたのは人間」と同じ構図です。異常を**検出する**仕組みはAIが作れますが、検出結果を**異常だと判断する**のは、まだ人間の側にありました。

だからこそ、**検出の結果を目立たせる**設計（`[!]` を付ける、州数を毎回表示する、補完した内容を必ず出す）が効いてきます。**人間が読む確率を上げる工夫**が、実質的な安全装置でした。

---

## 付録A：コード全文

```r
# =====================================================================
# prov_crosswalk.R : 州構成の畳み込み
# =====================================================================
# インドネシアの州は「分割」のみで増えてきた(統合の実績はない)。
# したがって:
#   ・細かい構成 → 粗い構成 は、子州を親州にまとめれば復元できる
#   ・粗い構成 → 細かい構成 は、按分の仮定なしには復元できない
# よってパネルは「最も粗い構成(33州)に揃える」のが唯一の厳密解。
#
# ただし畳み込みが厳密なのは **足し算できる量(人口・世帯数・金額の総額)**
# に限る。率・平均・指数は単純合算できないので、必ず重み付き平均にする。
# =====================================================================

# --- 分割の系譜 -------------------------------------------------------
# split_year は「その州が独立して統計に現れ始める年」。
prov_lineage <- tribble(
  ~child,              ~parent,             ~split_year,
  "Kalimantan Utara",  "Kalimantan Timur",  2012,
  "Papua Selatan",     "Papua",             2022,
  "Papua Tengah",      "Papua",             2022,
  "Papua Pegunungan",  "Papua",             2022,
  "Papua Barat Daya",  "Papua Barat",       2022
)

to33 <- c(
  "Kalimantan Utara"  = "Kalimantan Timur",
  "Papua Selatan"     = "Papua",
  "Papua Tengah"      = "Papua",
  "Papua Pegunungan"  = "Papua",
  "Papua Barat Daya"  = "Papua Barat"
)

to34 <- c(   # Kalimantan Utara は独立のまま残す
  "Papua Selatan"     = "Papua",
  "Papua Tengah"      = "Papua",
  "Papua Pegunungan"  = "Papua",
  "Papua Barat Daya"  = "Papua Barat"
)

#' 州構成を畳み込む
#' @param how  "sum"(足せる量) / "wmean"(率・平均。weight 必須)
collapse_prov <- function(df, to = 33, how = c("sum", "wmean"), weight = NULL) {
  how <- match.arg(how)
  stopifnot(all(c("prov_name", "year", "value") %in% names(df)))

  cw <- switch(as.character(to),
    "33" = to33, "34" = to34, "38" = character(0),
    stop("to は 33 / 34 / 38 のいずれか"))

  hit <- match(df$prov_name, names(cw))
  df$prov_name <- ifelse(!is.na(hit), unname(cw[hit]), df$prov_name)

  grp <- df |> group_by(across(any_of(c("prov_name", "indicator", "year"))))

  if (how == "sum") {
    out <- grp |> summarise(value = sum(value), .groups = "drop")
  } else {
    if (is.null(weight)) stop("how='wmean' には weight 列名が必要")
    out <- grp |>
      summarise(value = sum(value * .data[[weight]]) / sum(.data[[weight]]),
                .groups = "drop")
  }
  out
}
```

```r
# =====================================================================
# add_indicator.R : 重み付き畳み込み
# =====================================================================

#' 公表そのままの粒度の人口（重み用）を作る
#'   2010-2014 … 33州パネル（この期間は畳み込み不要なので33州で足りる）
#'   2015-2019 … var 1886（34州・千人単位 → ×1000）
#'   2020-2025 … var 2790（38州・人単位）
build_pop_weights <- function(years = 2010:2025,
                              panel_file = "out/population_33_2010_2025.csv") {
  out <- list()

  y0 <- intersect(years, 2010:2014)
  if (length(y0)) {
    out[[length(out) + 1]] <- readr::read_csv(panel_file, show_col_types = FALSE) |>
      filter(year %in% y0) |> select(prov_name, year, value)
  }
  y1 <- intersect(years, 2015:2019)
  if (length(y1)) {
    d <- bps_get_data(1886, y1)
    out[[length(out) + 1]] <- to_stats_rows(d, "pop_w", "Total", unit_mult = 1000)
  }
  y2 <- intersect(years, 2020:2026)
  if (length(y2)) {
    d <- bps_get_data(2790, y2)
    out[[length(out) + 1]] <- to_stats_rows(d, "pop_w", "TOTAL", unit_mult = 1)
  }

  w <- bind_rows(out) |>
    select(prov_name, year, pop = value) |>
    distinct(prov_name, year, .keep_all = TRUE)

  extend_weights_backward(w)
}

#' 子州の重みを、古い年へ按分で補完する
#'
#' 重みは **相対比しか効かない**（加重平均の分母と分子で打ち消し合う）ので、
#' 「最も古い年の子州シェアを、それ以前の年の親州人口に当てる」近似で十分。
#' 値そのものを推定するわけではないので、誤差の影響は小さい。
extend_weights_backward <- function(w) {
  add <- list()
  for (child in names(to33)) {
    parent <- unname(to33[child])
    yr_child  <- w$year[w$prov_name == child]
    yr_parent <- w$year[w$prov_name == parent]
    if (length(yr_child) == 0) next

    y0   <- min(yr_child)
    need <- setdiff(yr_parent[yr_parent < y0], yr_child)
    if (length(need) == 0) next

    pc <- w$pop[w$prov_name == child  & w$year == y0]
    pp <- w$pop[w$prov_name == parent & w$year == y0]
    if (length(pc) == 0 || length(pp) == 0) next

    share <- pc / (pc + pp)

    add[[child]] <- data.frame(
      prov_name = child, year = need,
      pop = w$pop[w$prov_name == parent & w$year %in% need] * share,
      stringsAsFactors = FALSE)

    # 推定を含む処理なので、必ず内容を表示する
    cat("[i] 重みを後方補完:", child, "→", paste(range(need), collapse = "-"),
        sprintf("(%s のシェア %.3f を使用)", y0, share), "\n")
  }
  if (length(add) == 0) return(w)
  bind_rows(w, bind_rows(add)) |> arrange(prov_name, year)
}

#' 率・平均を人口重み付きで畳む
#'
#' 重みが要るのは **実際に統合が起きる州×年だけ**。
#' そもそも33州構成でしか公表されていない年は統合が起きないので重みは不要。
#' 重みが無い年を一律に落とすと、古い年が丸ごと消える。
collapse_with_pop <- function(df, pop, to = 33) {
  cw <- switch(as.character(to), "33" = to33, "34" = to34,
               "38" = character(0), stop("to は 33 / 34 / 38"))

  hit <- match(df$prov_name, names(cw))
  df$.target <- ifelse(!is.na(hit), unname(cw[hit]), df$prov_name)

  grp_cols <- intersect(c("indicator", "year", ".target"), names(df))
  df <- df |> group_by(across(all_of(grp_cols))) |> mutate(.k = n()) |> ungroup()

  # (a) 統合が起きない行 … 重み不要。そのまま通す
  pass <- df |> filter(.k == 1) |>
    mutate(prov_name = .target) |> select(-.target, -.k)

  # (b) 統合が起きる行 … 人口重みが必要
  merge_rows <- df |> filter(.k > 1)
  if (nrow(merge_rows) == 0) return(pass |> arrange(year, prov_name))

  joined <- merge_rows |> left_join(pop, by = c("prov_name", "year"))

  # 【重要】統合グループの一部だけ重みが無い場合、その州だけ落とすと
  # 「片方の値がそのまま親州の値になる」という静かな誤りになる。
  #   例: 2013年の Kalimantan Utara に重みが無いと、
  #       Kalimantan Timur の値がそのまま33州版の値として残ってしまう。
  # そうならないよう **グループ全体を落として報告する**。
  # 結果として check_panel の州数が減るので、見れば気づける。
  bad_grp <- joined |>
    group_by(across(all_of(intersect(c("indicator","year",".target"), names(joined))))) |>
    summarise(any_missing = any(is.na(pop)), .groups = "drop") |>
    filter(any_missing)

  if (nrow(bad_grp) > 0) {
    cat("[!] 重みが欠けているため統合できなかったグループ（丸ごと落とします）:\n")
    print(as.data.frame(head(bad_grp, 20)))
    joined <- joined |> anti_join(bad_grp, by = intersect(names(bad_grp), names(joined)))
  }

  merged <- joined |>
    filter(!is.na(pop), !is.na(value)) |>
    select(-.target, -.k) |>
    collapse_prov(to = to, how = "wmean", weight = "pop")

  bind_rows(pass, merged) |> arrange(year, prov_name)
}

#' 検算：全国の人口加重平均
#'
#' 率は「州の合計」に意味が無いので、人口加重平均を全国公表値と比べる。
#' 一致しなくても構わない（BPS の全国値は個票からの直接推計なので、
#' 州別値の加重平均とは厳密には一致しない）。**桁と水準が合うか**を見る。
national_weighted <- function(df, pop) {
  df |>
    left_join(pop, by = c("prov_name", "year")) |>
    filter(!is.na(pop), !is.na(value)) |>
    group_by(year) |>
    summarise(weighted_mean = sum(value * pop) / sum(pop),
              simple_mean   = mean(value),
              n_prov        = n(), .groups = "drop")
}
```

---

## 付録B：よくある質問

### Q1. 33州に畳むと、新しい州の情報は失われませんか？

失われます。**そこは割り切りです。**

ただし、**元データは捨てていません**。38州のまま保存しておけば、2022年以降だけを対象にした分析では38州を使えます。33州版は「16年を通した比較のための版」です。

**目的ごとに別の版を作る**のが正解で、「1つの版で全部やろうとしない」のがコツです。

### Q2. 二重計上に気づくには、公表値との照合しかありませんか？

**ほぼそうです。** ほかに考えられるのは、

- 分割の年（2022年）の前後で、合計が不連続に増えていないか見る
- 新設州の値が、親州の値の一部として整合するか確かめる

しかしどちらも、**外部の値がなければ「どちらが正しいか」を決められません**。

> **内部の整合性だけでは、系全体がずれているときに気づけない。**

これが、この連載を通じて「公表値との照合」を最重要の検算に据えている理由です。

### Q3. 示強の指標で、人口以外を重みにすべき場面は？

あります。この作業でも1つ、**概念的に不正確なまま残した**ものがあります。

**都市部・農村部それぞれの貧困線**を、州の**総人口**で加重していました。本来、農村部の貧困線は**農村人口**で加重すべきです。

実際、これが数字に出ています。2018年の全国値で、

- 都市：こちら 413,891 / BPS 415,614 → ほぼ一致
- 農村：こちら 383,097 / BPS 361,496 → **+6% 高い**

農村人口の多い州の重みが相対的に小さくなり、都市寄りの州に引っ張られた、という説明がつきます。

**33州に畳む段階では影響は小さい**（畳むのはカリマンタンとパプアだけ）ので実用上は許容しましたが、**全国値を出すときは都市／農村別人口で加重し直すべき**です。これは既知の課題として記録しています。

### Q4. 他の罠も知りたいのですが

この記事は連載の9本目です。

1. [一覧APIの暗黙の打ち切り](01-silent-truncation.md)
2. [カタログの二言語混在](02-bilingual-catalog.md)
3. [年の必須指定と3年上限](03-year-parameter.md)
4. [連結キーの桁数不定](04-composite-key.md)
5. [派生変数・期間の未指定](05-derived-dimensions.md)
6. [行は存在するが値が空](06-empty-rows.md)
7. [静的表の実体がHTML](07-static-table-html.md)
8. [州名の表記ゆれと脚注記号](08-name-variants.md)
9. **行政区画の変更（33→34→38州）**（この記事）
10. 公表基準の断絶（3月調査と9月調査） ── 検算が失敗したとき、何を疑うか

---

## この記事について

この記事は、日本計算機統計学会シンポジウムでの報告「検算駆動による生成AIとの協働 ── インドネシア公的統計WebAPIからの州別パネル構築とShinyによる可視化」の補足資料として作成しました。

本文中のコード、出力、数値は、すべて実際の作業記録に基づいています。うまくいかなかった過程も、そのまま残しています。

### 生成AIの利用について

**本記事の文章は、生成AI（Claude Opus 5、推論設定「高」）が、久保田による指示（プロンプト）と複数回のやりとりを経て生成したものです。** 構成・記述・コード解説はAIによる出力であり、掲載しているコード、コンソール出力、数値は、いずれも実際の作業記録に基づいています。内容は著者が確認したうえで公開しています。

この記事の主題そのものが「生成AIと協働してデータを取得した記録」であるため、記事の生成過程を隠す理由がありません。念のため明記しておきます。

- 著者：久保田貴文（多摩大学）
- 連絡先：kubota@tama.ac.jp
- Web：https://takafumikubota.jp/

この作業で用いた統計は、インドネシア中央統計庁（BPS）が公開するWebAPIから取得しました。
Layanan ini menggunakan API Badan Pusat Statistik (BPS).
（本サービスはインドネシア中央統計庁のAPIを使用しています。）
